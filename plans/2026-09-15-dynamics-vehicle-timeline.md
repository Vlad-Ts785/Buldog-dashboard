# Динамика: лента жизни машины + деньги (гибрид A+B)

Дата: 2026-09-15. Ветка: `claude/interactive-machine-visualization-004437`.
Решение Влада 15.09: берём **гибрид A+B** и **переливаем историю статусов из Штатки на сервер**.

## Зачем

Страница «Динамика» сейчас показывает четыре числа на машину (в работе / ремонт / без заказа /
без водителя) и общий график тенденции. По ней нельзя посмотреть на машину и понять, КАК она
прожила период: когда встала, сколько простояла подряд, приносила ли деньги в рабочие дни.
Нужна визуальная картина «день за днём» плюс деньги рядом.

## Что утверждено (превью = контракт)

Три превью на реальных данных (16 машин, 01.06-15.09, ни одной выдуманной цифры):
- `examples/preview-dynamics-a-lenta-parka.html` - лента парка (эволюция текущей таблицы)
- `examples/preview-dynamics-b-pasport-mashiny.html` - паспорт одной машины
- `examples/preview-dynamics-c-matrica-prostoya.html` - матрица простоя (не берём, оставляем как
  источник идей: цена простоя, спарклайны справа, сортировка перестановкой)

**Гибрид**: экран A - основной (лента парка, сортировка, фильтры, полоса дня). Клик по машине
открывает паспорт из B (календарь-heatmap, подневные деньги с фоном статуса, водопад
выручка→ФОТ→топливо→запчасти→ВП, bullet план/факт).

Перед реализацией собирается ОДИН файл гибридного превью и утверждается Владом - иначе контракта
нет, а частичный перенос двух превью запрещён ГОСТом (раздел 0, п.6).

## Ограничения данных (выяснено разведкой 15.09, не обходить молча)

1. **Подневной валовой прибыли не существует.** Топливо и запчасти приходят от 1С только месячной
   строкой (`park_reports`). По дням есть: выручка и прибыль заказа (`orders_normalized` /
   `orders_archive`, госномер разбирается из поля `machine` через `normalizeGosServer_`), оплата
   водителю (`driver_payroll_trips`, только с 01.08), проходные (`avtodor_transactions`).
   В UI день и месяц обязаны быть разведены подписями: «выручка за день» против «ВП за месяц, 1С».
2. ~~**Статусы по дням (`vehicle_status_daily`) ведутся с 30.08.2026** - 16 дней из 107.~~
   **Снято 15.09 Фазой 1**: история из Штатки перелита, теперь 23.06-14.09 (84 дня) на
   сервере, `/api/vehicle_status_history` отдаёт всё одним куском, подмешивать Apps Script
   больше не нужно. Остаток: до 23.06 истории нет нигде.
3. **`plan` в 1С задан по выручке, не по ВП.** Bullet «план/факт по ВП» построить не из чего;
   подписывать честно «план по выручке», пока план по ВП не заведён отдельным полем.
4. Июль и август лежат И в `orders_archive`, И в `orders_normalized` с одинаковыми суммами -
   двойной счёт при склейке. Граница: по 30.06 - archive, с 01.07 - normalized.
5. Прибыль за январь-май недостоверна (`data_quality_warning`) - только выручка.
6. Машины с непустым `sprav_assets.department` исключены из всех расчётов парка.

## Фазы

### Фаза 1. История статусов из Штатки на сервер (СДЕЛАНО 15.09)
- [x] Прочитать `/root/yard-dashboard/README.md`, `git status` на сервере (чужие правки - sync-коммитом)
- [x] Сверить структуру листа «История_финансов» с `getVehicleHistory()` (`scripts/full_script_final.js:6950`)
- [x] Эндпоинт на сервере `POST /api/vehicle_status_history/import` (`checkWriteKey`), приём
      пачками, **только `INSERT ... ON DUPLICATE KEY UPDATE`**, никаких `DELETE`/`TRUNCATE`
- [x] `syncVehicleStatusHistoryToServer_()` в Apps Script по образцу `syncVehicleTypesToServer_()`
      (в репозитории, **НЕ задеплоена** - Влад 15.09: без явной команды не выкатывать)
- [x] Перед заливкой - бэкап `vehicle_status_daily`
      (`/root/backups/vehicle_status_daily-before-shtatka-import-20260915-184421.sql`)
- [x] Залить историю до 29.08 включительно; дни с 30.08 не трогать - залито 3583 строки,
      было 847/16 дней, стало 4430/84 дня (23.06-14.09 без пропусков)
- [x] Сверка: все 3583 строки против исходного JSON листа - 0 расхождений; подневно 5 машин - 0
- [x] Строка в `scripts/DEPLOY_LOG.md` и в README сервера

**Решения Фазы 1, которые влияют на Фазы 2-3:**
- Статусы сведены к кодам cron'а (`work`/`repair`/`no_order`/`no_driver`) на СЕРВЕРЕ
  (`SHTATKA_STATUS_MAP_` в `api/server.js`); пустой/неизвестный статус не пишется вовсе -
  140 таких строк отсеяно (5 машин, у которых Штатка статус не вела).
- Колонка `vehicle_status_daily.src` (`plan_segs` / `shtatka`) - видно происхождение строки.
- Машины с непустым `sprav_assets.department` из истории НЕ вырезаны: история - факт,
  `department` - текущий атрибут. Фильтровать нужно на ЧТЕНИИ, в Фазе 3 это решение принять
  явно (сейчас `/api/vehicle_status_history` не фильтрует ни историю, ни снимок).
- Границы охвата разные у разных машин (`К 115 ЕТ 797` - 63 дня, `У 660 ОХ 750` - 15) -
  лента обязана показывать «нет данных», а не «в работе», на днях без строки.

### Фаза 2. Гибридное превью A+B (утверждение Владом)
- [ ] Собрать `examples/preview-dynamics-hybrid.html`: экран A + паспорт B по клику
- [ ] Данные - те же реальные, после Фазы 1 пересобрать датасет, чтобы лента была полной
- [ ] Прогнать чек-лист приёмки `DESIGN_SYSTEM.md` раздел 10 целиком
- [ ] Показать Владу, получить «да» или правки

### Фаза 3. Перенос в живой дашборд
- [ ] Порт в `files/index.html`, страница `dynamics` (рендер - `renderDynamics()` :17547,
      таблица - `renderStatusTable()` :17628)
- [ ] Серверный эндпоинт подневных денег по машине (сейчас есть только Apps Script
      `?action=vehicle_orders_history`) - по схеме из п.1 ограничений
- [ ] Поблочная сверка живой страницы с превью, глазами, рядом
- [ ] Почистить мёртвый код страницы: `histChartFinance`/`histChartCosts` (:17502), `sortCols` (:17646)
- [ ] Починить расхождение порядка веток бакетизации статусов между таблицей (:17589) и графиком (:17809)
- [ ] Убрать молчаливое `else work++` - неизвестный статус должен считаться «прочее», а не работой

### Фаза 4. Цикл после фичи
- [ ] Найти ошибки, граничные кейсы (машина без `months`, месяц без плана, пустой период)
- [ ] Security review (страница admin-only, ПДн водителей в ленте не светить)
- [ ] Чек-лист ГОСТа раздел 10
- [ ] Ретроспектива

## Отложено осознанно

- Матрица простоя (вариант C) целиком - но «цена простоя» как метрика может переехать в паспорт
- План по ВП отдельным полем (сейчас план только по выручке)
- Подневное топливо/запчасти - источника нет, ждёт отдельной выгрузки 1С
