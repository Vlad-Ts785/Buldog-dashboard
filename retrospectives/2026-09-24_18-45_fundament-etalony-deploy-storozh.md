# Ретроспектива 24.09 - отдел машины (фаза Б) и фундамент: эталоны, deploy.sh, сторож

## Что сделано
- **Отдел машины с датой, фаза Б** (VPS 5585a27): сделка на машине, которая на дату сделки в другом
  отделе, - вне выручки тралов. Фильтр в единственной точке входа строк заказов (`readOrdersRawRows_`),
  формулы зарплат не трогались ни в одной копии. Сейчас эффект 0; механизм проверен на выдуманном
  переводе (июль −588 900, 5 менеджеров, граница по дате точная).
- **Этап 1 дорожной карты** (`plans/2026-09-24-foundation-safety-net.md`): эталоны `tests/golden/golden.js`
  (72 случая + отпечаток входных данных), `tests/unit.js`, `./deploy.sh` (ворота + автооткат, проверен
  в песочнице по 4 сценариям, первый боевой прогон зелёный), сторож `import/watchdog.js` (cron каждую
  минуту -> MAX директору).

## Что пошло не так
- **Мои проверочные скрипты 23-24.09 засчитали сотрудникам визиты.** Подписывал токены от их имени, а
  `checkSession` на каждый запрос пишет активность. Велиев «вошёл в систему» трижды, ни разу не входя.
  Нашёл, только когда проектировал эталоны и спросил себя, какие побочные записи делает запрос.
  Закрыто на уровне причины (`isServiceProbe_`), правило - в CLAUDE.md и памяти. Исправление самого
  журнала - решение Влада.
- В первых версиях `deploy.sh` было три ошибки, каждая ломала бы именно аварийный путь: откат задевал
  сам скрипт и логи под git; `git checkout` с отсутствующим путём молча ничего не делает;
  «последний удачный» ставился до сравнения. Все пойманы до прода: сам себя перечитал + песочница.
  Вывод - код, который срабатывает только при аварии, нужно ПРОГОНЯТЬ в аварии, а не читать.
- `pkill -f` убил собственный ssh-сеанс (строка команды содержала шаблон). Приём `[3]999`.

## Что взять дальше
- Любая перестройка этапов 2-5: `./deploy.sh` вместо ручного restart; красное = стоп.
- Прежде чем подписывать запросы от имени людей - вспомнить про побочные записи (активность,
  «прочитано», присутствие).
- Хвосты: внешний пульс (аккаунт Влада на healthchecks.io), 1.2 уборка сервера, 1.5 staging,
  подтверждение 6 дат перевода машин, исправление журнала входов.
