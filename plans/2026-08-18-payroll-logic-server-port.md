# Перенос расчётной (зарплатной) логики заказов на сервер

**Статус:** в работе
**Одобрено:** Влад, 2026-08-18 - "перенести расчёт на сервер... глобально мы переходим на
работу на сервере а не на гугл таблицах". См. `../ai-clone` память
`project_server_migration_strategy.md` и статус в `CLAUDE.md` ("Архитектура данных").

## Зачем и почему осторожно

Замер (см. `DEPLOY_LOG.md`, запись 227/229-230) показал: смена периода на "Обзор заказов"
занимает ~26 сек, из них ~15 сек - НЕ чтение данных (сервер уже отдаёт сырые заказы за
0.6 сек), а сама сборка (`aggregateOrdersRows`) внутри Apps Script - CPU, не I/O. Перенос
данных на сервер это не ускорит; ускорит только перенос самого РАСЧЁТА туда же (Node.js
на VPS быстрее V8-песочницы Apps Script на порядок для такой нагрузки).

Это САМАЯ рискованная часть всего переезда - от неё зависит зарплата людей. Прецеденты:
- Суркова дважды выпадала из `TRAL_LOGISTS` при малейшей правке списков → реальные потери
  в зарплате логиста.
- Порог "23%" (квалификация бонуса по марже найма) регрессировал дважды.

**Правило этого плана: НИКАКОГО "с виду работает - переключаю".** Каждая фаза - построчная
сверка нового расчёта против старого (уже проверенного, живого) на РЕАЛЬНЫХ данных, для
МНОГИХ периодов, не только одного месяца. Расхождение больше пары свежих заказов (как было
с сырыми данными) = стоп, разбираться, не переключать.

## Что переносим (Фаза 1 - заказы: зарплаты/планы/воронка менеджеров и логистов)

Функции в `scripts/full_script_final.js`, переносим 1-в-1 (без "улучшений" по дороге -
это отдельная и более поздняя задача, если вообще нужна):
- `aggregateOrdersRows(rows)` - ядро, ~350 строк, вся структура ответа (by_manager,
  by_logist, by_day, doc_status, funnel, lost_customers-заглушка и т.д.)
- `joinManagerPlans_(ss, result, monthKey)` + `getManagerPlans_(ss, monthKey)` - подтяжка
  плана из листа "Планы_менеджеров" (маленький, 796мс - НЕ бутылочное горлышко, но нужен
  для полноты ответа)
- `computeSalesFaktPlan_(periodOrders)` - факт/план продаж для верхней полосы
- `computeLostCustomers_(ss, currentRows, monthKey)` - использует АРХИВНЫЙ лист предыдущего
  месяца (`Заказы_YYYY-MM`) - для месяцев, где такого листа нет (январь-май, будущие после
  отключения старого канала) нужен эквивалент через `orders_archive`/`orders_normalized` на
  сервере, ищущий предыдущий месяц там же

НЕ переносим в этой фазе (сознательно, отдельная задача/не сейчас):
- `getGrossProfitForPeriod`/`getStaffData`/`aggregateFinHistoryForRange` (Штатка/История_
  финансов - "Валовая прибыль", "Маржа") - тоже в очереди по стратегии, но другой источник
  данных (не заказы), другой риск-профиль, отдельная фаза плана или отдельный план.
- Любые "улучшения" логики по дороге - если что-то выглядит как баг в текущей логике,
  фиксируем находку, но НЕ чиним попутно - меняем только площадку исполнения.

## Константы, которые должны остаться идентичными

`TRAL_MANAGERS`, `TRAL_LOGISTS`, `INTERNAL_CLIENTS` - уже скопированы в
`import-orders-raw.js`/`import-orders-jan-may.js` на сервере (с явным предупреждением о
регрессиях в комментарии). Для aggregateOrdersRows понадобятся ЕЩЁ константы изнутри неё
(пороги бонусов, `WAYBILL_SKIP_IF_REALIZ`, `FUNNEL_EXCLUDE_OTHER_PAYMENT_VARIANT`,
`FUNNEL_EXCLUDE_CASH_PAYMENTS`, любые проценты/пороги) - переносить ВМЕСТЕ с функцией,
одним диффом, не по памяти.

## Фазы

### Фаза 1a. Портирование (Node.js, без прод-трафика)
- [ ] Скопировать `aggregateOrdersRows`, `joinManagerPlans_`, `getManagerPlans_`,
      `computeSalesFaktPlan_`, `computeLostCustomers_` в новый файл на сервере
      (`/root/yard-dashboard/api/lib/orders-calc.js` или как модуль `server.js`) - максимально
      дословно, только синтаксис `Utilities.formatDate`/`ss.getSheetByName` заменить на JS-
      эквиваленты (даты - через уже проверенный `toDateStr`/дневной формат, планы - через
      SQL-таблицу-зеркало листа "Планы_менеджеров", см. Фазу 1b).
- [ ] Планы_менеджеров - новый одноразовый + затем регулярный (раз в день? Влад вносит
      руками раз в месяц, не нужен FTP) импорт листа в MySQL-таблицу `manager_plans`
      (month_key, manager, plan) - простой формат, низкий риск (просто SUM-таблица).

### Фаза 1b. Новый эндпоинт `/api/orders_computed?period=YYYY-MM`
- [ ] Использует уже существующий `/api/orders_raw` внутри себя (тот же источник, что и
      сейчас - normalized/archive автоматически), прогоняет через портированный расчёт,
      отдаёт JSON в ТОЧНО том же формате, что `action=orders_period`/`getOrdersData()`.

### Фаза 1c. Сверка (ОБЯЗАТЕЛЬНА до переключения хоть одного пользователя)
- [ ] Для каждого доступного периода (июнь, июль, август, январь-май - 8 месяцев) сравнить
      ПОЛНЫЙ JSON старого (`action=orders_period`/живой расчёт Apps Script) и нового
      (`/api/orders_computed`) - не только общие суммы, а КАЖДОЕ поле в `by_manager`/
      `by_logist`/`funnel_*`/пороговые флаги (`hired_margin_qualifies` и подобные).
- [ ] Отдельно - проверка на "сегодняшний" текущий месяц несколько раз в течение дня (чтобы
      поймать не только статичные архивы, но и живые донасчитывающиеся данные).
- [ ] Любое расхождение - записать в `DEPLOY_LOG.md`, разобрать причину, не считать
      "видимо, погрешность" без объяснения (см. `[[no-conclusions-without-row-level-
      verification]]`).

### Фаза 1d. Включение (постепенно, с мгновенным откатом)
- [ ] Тот же приём, что и для `orders_raw` - фронтенд/Apps Script сначала пробует сервер,
      тихий фолбэк на старый путь при любой ошибке/расхождении. Механизм включения -
      Script Property (как `YARD_API_KEY`), можно выключить одной командой без редеплоя
      фронтенда.
- [ ] Первую неделю - держать ОБА расчёта живыми параллельно, логировать оба результата
      (не показывать пользователю, только для последующей сверки), прежде чем доверять
      серверу единолично.

## Риски и как их снижаем

- **Денежный риск (зарплата/бонусы)** → построчная сверка Фазы 1c обязательна, никаких
  исключений "и так похоже".
- **Расхождение списков менеджеров/логистов между копиями** → уже есть прецедент (Суркова).
  Хранить константы только В ОДНОМ месте после переноса - как только серверный расчёт
  становится основным, Apps Script-копию списков можно считать вторичной (но не удалять,
  пока не отключим старый канал полностью).
- **`computeLostCustomers_` для месяцев без архивного листа** (январь-май, будущее без
  старого канала) → отдельная разработка эквивалента через MySQL до включения для этих
  периодов; для июня-августа архивные листы есть, там не блокирует.
- **Даты/instanceof Date** → уже наступали на эти грабли в этом переезде (см.
  `project_apps_script_date_instanceof_gotcha`) - `orders_raw` уже отдаёт чистые
  YYYY-MM-DD строки, портированный код должен ожидать СТРОКИ, не Date, с самого начала
  (не копировать `instanceof Date` проверки без надобности).

## Итог

*Заполняется по ходу работы.*

**2026-08-18, промежуточный чекпоинт (Фаза 1a почти готова):**
- Портированы 1-в-1: `aggregateOrdersRows` (755 строк, ядро), `joinManagerPlans_`/
  `getManagerPlans_` (без реального источника планов - см. ниже), `computeSalesFaktPlan_`.
  Файл: `/root/yard-dashboard/api/lib/orders-calc.js` на сервере - НЕ подключён ни к одному
  эндпоинту, только лежит и готов к сверке.
- Дымовой тест (не полная Фаза 1c!) на реальных данных - совпало с тем, что видел живой
  Apps Script для тех же данных несколько часов ранее: август (418 заказов, 31 285 120 ₽,
  маржа найма 29.3%, порог 23% выполнен, 9 менеджеров/6 логистов, Суркова на месте в списке
  логистов) и март-архив (668 заказов, 38 799 633 ₽ - совпадает с прямым SQL-запросом).
  Node считает за 15-16мс против многих секунд в Apps Script.
- НЕ сделано: `computeLostCustomers_` ещё не портирован (информационный список, не денежный -
  ниже риск, отдельным шагом). `manager_plans` таблица в MySQL не создана - `joinManagerPlans`
  проверен только на пустом массиве планов, реальные планы менеджеров ещё не переносились.
  Фаза 1c (построчная сверка КАЖДОГО поля против живого Apps Script, по всем 8 месяцам) - НЕ
  начата, это следующий шаг. НИЧЕГО не переключено, риск для боевых данных - нулевой.

**2026-08-18, чекпоинт 2 (Фаза 1c пройдена на 7 из 8 месяцев):**
- `manager_plans` в MySQL создана и наполнена (47 строк, июнь-август - в "Планы_менеджеров"
  раньше данных нет). Импорт: `import/import-manager-plans.js`, разовый (Влад вносит планы
  руками раз в месяц - можно перезапускать импорт вручную при необходимости, автоматического
  расписания нет, не нужно).
- **Построчная сверка `parity-check.js`** - портированный расчёт (`aggregateOrdersRows` +
  `joinManagerPlans` + `computeSalesFaktPlan`) против живого `getOrdersData()`/
  `getOrdersDataForPeriod()` (через временные `export_manager_plans`/`diag_full_orders`,
  использованы и убраны, v231→v233). Сравнивалось ПОЛНОСТЬЮ: summary (все ~30 полей, включая
  `hired_margin_qualifies`/порог 23%), `doc_status`, `doc_by_decade`, КАЖДЫЙ менеджер и логист
  в `by_manager`/`by_logist` (все поля, включая `hired_margin_qualified`/`own_profit`/
  `hired_extra_costs`), `by_hired_supplier`, `internal`, `long_funnel`, `managerPlans`.
  Результат:
  - Январь, февраль, март, апрель, май (архив `orders_archive`) - **0 расхождений** каждый.
  - Июль (живой `orders_normalized`) - **0 расхождений**.
  - Август (текущий, живой `orders_normalized`) - **0 расхождений**.
  - **Июнь - не проверен**: у него нет сырых данных на сервере вообще (ни `orders_normalized`,
    ни `orders_archive` его не покрывают - "дыра" между двумя каналами миграции). Живой
    Apps Script расчёт для июня стабильно не отвечал за 150 сек (архивный лист читается
    напрямую из Таблиц, без ускорения сервером - то же самое узкое место, что в замере
    тайминга, запись 227/229-230). Нужен отдельный разовый импорт июня в `orders_archive`
    (тот же приём, что январь-май) прежде чем можно будет сверить и его.
- НЕ сделано: `computeLostCustomers_` (список пропавших клиентов, информационный, не денежный)
  - следующий шаг. Июнь - импорт сырых данных + сверка. Фаза 1d (реальное включение с
  параллельным прогоном) - не начата, ничего не переключено, риск нулевой.

**2026-08-18, чекпоинт 3 (Фаза 1c ЗАВЕРШЕНА - 8 из 8 месяцев, дыра июня закрыта):**
- Импортирован июнь: лист "Заказы_2026-06" (эта же таблица, НЕ мегабаза; "живой" формат,
  105-122 колонки, тот же формат-по-имени-колонки, что июль-август) - `import/import-orders-
  june.js`. Записано в `orders_archive` через `DELETE WHERE month_key='2026-06'` + `INSERT`
  (НЕ `TRUNCATE` всей таблицы - январь-май остались нетронуты, проверено по счётчикам
  до/после: 3545 → 4492 строк, ровно +947 июня). 947 заказов, 62 279 274 ₽ - совпадает день-
  в-день с уже проверенным `month_summary.revenue` за июнь (62279274.25).
- Финальная сверка `parity-check.js` по июню (временный `diag_full_orders`, v235→v236) -
  **0 расхождений**, тем же полным набором проверок, что и остальные 7 месяцев.
- **ИТОГ: 8 из 8 месяцев - полное совпадение** портированного расчёта (`aggregateOrdersRows`
  + `joinManagerPlans` + `computeSalesFaktPlan`) с живым Apps Script, по каждому менеджеру,
  логисту, поставщику найма, порогу 23%, воронке документов. Все временные экспорты убраны.
- Осталось до Фазы 1d: `computeLostCustomers_` (не денежный, ниже риск) + само решение о
  включении (параллельный прогон, инструкция в разделе "Фазы" выше). Ничего не переключено.

**2026-08-18, чекпоинт 4 (`computeLostCustomers` портирован, Фаза 1a полностью готова):**
- `computeLostCustomers` добавлен в `orders-calc.js` - порт 1-в-1, с одним намеренным
  отличием от буквального копирования: вызывающий код сам передаёт `prevRows` (сырые строки
  предыдущего месяца), а не ходит в Google Sheets самостоятельно - оригинал делал
  `ss.getSheetByName(...)` внутри функции, порт этого не может и не должен (у него нет `ss`) -
  тот же принцип, что везде в этом переносе (сырые данные - через `/api/orders_raw`, который
  уже одинаково работает для любого месяца). Поймана и исправлена ДО тестов тонкость с часовым
  поясом: `today - new Date(c.last_date)` в оригинале - обычный `new Date()`, не "московский"
  `mskNow()` (та функция там, где нужно ИМЕННО календарное московское число, а не сырая
  разница меток времени) - использование `mskNow()` тут дало бы систематический сдвиг days_since на ~3 часа.
- Сверка по всем 8 месяцам (`parity-check-lost.js`) - **7 из 8 полное совпадение**. Август
  разошёлся на одном клиенте (ТРЕЙДСЕРВИС ООО, 4 заказа/169 000 ₽ у порта против 5/190 000 ₽ у
  живого) - разобрано ДО КОНЦА (не списано на "погрешность"): заказ №469223 (21 000 ₽,
  01.07.2026, пустая "Дата создания") есть в листе Google Таблиц, но отсутствует в FTP-
  выгрузке 1С, которая наполняет `orders_normalized`/`orders_archive`. Это НЕ баг порта - тот
  же класс проблемы, что уже фиксировали раньше (см. DEPLOY_LOG запись ~201, "Машина
  прикрытия" - FTP-отчёт 1С не всегда включает 100% строк, которые есть в ручной/email-
  выгрузке). Подтверждено отдельно: агрегат по июлю (`total_orders`/`total_amount`) сходится
  ИДЕАЛЬНО между сервером и Таблицей прямо сейчас (1022 строки, 65 993 967,18 ₽ один в один) -
  значит расхождение изолировано именно в этой узкой функции (список пропавших клиентов), не
  затрагивает ни одну денежную цифру нигде ещё. Не чиню эту дыру данных сейчас (не денежная
  логика, отдельная работа по сверке FTP-отчёта 1С целиком) - фиксирую находку и иду дальше.
- **ИТОГ Фазы 1a: ВСЯ расчётная логика заказов портирована** (`aggregateOrdersRows`,
  `joinManagerPlans`, `computeSalesFaktPlan`, `computeLostCustomers`). Денежная часть - 8/8
  месяцев без единого расхождения. Не денежная часть (пропавшие клиенты) - 7/8, одна известная
  и объяснённая причина расхождения, не в коде порта. Все временные диагностики убраны.
  Готово к Фазе 1d (реальное включение) или к code review (`/code-review ultra`) перед этим.

**2026-08-18, чекпоинт 5 (Фазы 1b и 1d ГОТОВЫ, включение НЕ сделано - ждёт решения Влада):**

*Фаза 1b - эндпоинт `/api/orders_computed?period=YYYY-MM`:*
- Отдаёт `{ period, source, prev_source, orders, sales, not_computed, computed_at }`, при
  `source==="archive"` добавляет `data_quality_warning` (тот же принцип, что
  `noteServerDataQuality_`). Отвечает за ~0.27 сек.
- `not_computed` явно перечисляет `profit`/`profit_tral`/`profit_long`/`special_trals_profit` -
  это "Валовая прибыль своего парка" из листов Штатка + История_финансов, они на сервер НЕ
  переносились, вызывающий обязан добрать сам. Сделано полем в ответе, а не комментарием,
  чтобы про это нельзя было молча забыть.
- Заодно устранено дублирование: `/api/orders_raw` и `/api/orders_computed` теперь используют
  ОДНУ функцию выбора источника (`readOrdersRawRows_`) - два независимых правила в одном файле
  уже однажды разошлись в этом проекте и стоили денег.
- Граничные случаи проверены: несуществующий месяц → 404, кривой период → 400, без ключа → 403,
  архивный месяц → предупреждение о качестве.
- Сверка САМОГО ЭНДПОИНТА (не только модуля) против сохранённых снимков живого Apps Script:
  январь, февраль, март, апрель, май, июль - **0 расхождений**; июнь - только ПОРЯДОК внутри
  групп с одинаковым `days_since` в `lost_customers` (те же 40 клиентов, значения идентичны,
  граница списка совпадает - разобрано отдельным скриптом; причина: `.sort()` по одному полю
  не задаёт порядок при равных значениях, а исходный порядок строк из MySQL и из Google Sheets
  разный; это свойство ОРИГИНАЛА, не привнесено портом - НЕ чинил, чтобы не менять логику
  молча, см. "Кандидат на улучшение" ниже); август - известный ТРЕЙДСЕРВИС (дыра в FTP-выгрузке).

*Фаза 1d - механизм включения (ВЫКЛЮЧЕН ПО УМОЛЧАНИЮ):*
- `fetchOrdersComputedFromServer_()` + флаг `USE_SERVER_ORDERS_CALC` в Script Properties.
  Управление - функции `enableServerOrdersCalc()` / `disableServerOrdersCalc()` /
  `serverOrdersCalcStatus()` (запускать из редактора Apps Script; имена намеренно БЕЗ `_` в
  конце - иначе не видны в списке "Выполнить").
- `serverCalcLooksSane_()` - санити-проверки ответа сервера ПЕРЕД тем, как ему доверять
  (правильный период, total_orders > 0, `hired_margin_qualifies` именно boolean, by_manager/
  by_logist массивы). Любое несоответствие → тихий фолбэк на локальный расчёт. Защита от
  "сервер отдал структурно неполный ответ, а дашборд показал неверную зарплату".
- `verifyServerOrdersCalc(period)` - ПОСТОЯННЫЙ инструмент (как `verifyKnownFixes`): считает
  ОБОИМИ способами на одних и тех же сырых данных и сравнивает денежные поля + каждого
  менеджера/логиста поимённо. Запускать после ЛЮБОЙ правки списков/порогов в любой из двух
  копий логики.
- **Приёмка пройдена** (временный `diag_phase1d`, v241, убран v242 - включал флаг на один
  запрос и гарантированно выключал через `finally`):
  - август: локально 5236мс → сервер 1141мс (**4.6x**), 464 заказа / 33 343 680 ₽ / 9 менеджеров /
    порог 23% = true - ИДЕНТИЧНО, `verifyServerOrdersCalc` вернул 0 проблем;
  - июль: локально 4544мс → сервер 531мс (**8.5x**), 1022 заказа / 65 993 967,18 ₽ / 10 менеджеров -
    ИДЕНТИЧНО, 0 проблем.
  - Флаг после обеих проверок остался выключенным (проверено полем `flagAfter`).

*Что осталось:*
- Кандидат на улучшение (НЕ баг, не делать молча): добавить вторичную сортировку по имени в
  `computeLostCustomers` - в ОБЕ копии сразу (Apps Script + сервер), чтобы порядок при равных
  `days_since` был детерминированным. Сейчас теоретически возможно, что на границе среза 40
  окажутся разные клиенты в зависимости от источника (на реальных данных пока не наблюдалось).
- "Валовая прибыль" (Штатка + История_финансов) - следующий крупный кусок глобального переезда,
  не начат.

## Включение (Фаза 1d) - СДЕЛАНО, 2026-08-18

Влад попросил включить лично ("Можешь сам её включить?", не нашёл функцию в редакторе -
известная причина: список "Выполнить" не обновляется без перезагрузки страницы редактора
после `clasp push`). Включено тем же временным секретным способом, что и весь этот перенос -
`PropertiesService.setProperty('USE_SERVER_ORDERS_CALC', 'on')` + немедленная сверка прямо на
реальном пути `getOrdersData()`.

**ИНЦИДЕНТ ПРИ ВКЛЮЧЕНИИ (важно для будущих сессий):** параллельная сессия в это же время
активно дорабатывала "Создание заявок План задание" (`createOrderPlanEntry_`) и трижды подряд
делала `clasp push` со своей локальной копии, которая НЕ включала мой более ранний пуш (v240-
242, Фаза 1d) - каждый раз это молча стирало `fetchOrdersComputedFromServer_`/
`verifyServerOrdersCalc`/`enableServerOrdersCalc`/`disableServerOrdersCalc`/
`serverOrdersCalcStatus`/`serverCalcLooksSane_` из живого кода (сама функция расчёта,
`aggregateOrdersRows` и т.д., не задета - только "переключатель" вокруг неё). Замечено СРАЗУ
(обязательный `clasp pull` + diff перед каждым push сработал ровно как задуман) - каждый раз
восстановлено вручную (те же готовые блоки кода, известные построчно) поверх их свежей работы,
проверено `grep` на все функции с обеих сторон + `node -c` на синтаксис, передеплоено. Три
раза подряд, потому что параллельная сессия трижды пушила в этот короткий промежуток - не
инцидент с потерей данных (продакшн-деплой `/exec` ни разу не отдавал сломанный код реальным
пользователям - проверено `clasp deployments` в процессе, битым был только черновик в
редакторе между `clasp push` и следующим `clasp deploy`), но урок: если ДРУГАЯ сессия активно
и часто пушит в тот же файл прямо сейчас - normальный "запушил один раз и разошлись" протокол
может потребовать несколько попыток подряд, не сдаваться и не пропускать sync-check ни на
одной итерации, даже когда кажется, что уже "точно всё сделал".

**Финальное состояние подтверждено** (`diag_final_check`, использован и убран, v246→v247):
флаг `"on"`, реальный `getOrdersData()` отдаёт 464 заказа / 33 343 680 ₽ / порог 23% = true,
`verifyServerOrdersCalc()` - 0 расхождений. Дашборд СЕЙЧАС считает "Обзор заказов"/"По
менеджерам"/"По логистам"/"Зарплата" через сервер, с автоматическим фолбэком на Apps Script
при любой аномалии в ответе сервера.
