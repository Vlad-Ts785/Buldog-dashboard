# План: Зарплата машинистов на дашборде

**Дата:** 2026-09-03
**Автор:** Клод (по прямой просьбе Влада)
**Статус:** в работе

## Цель

Добавить на дашборд страницу «Зарплата → По машинистам» с данными о начислениях/
удержаниях водителей тралов и длинномеров - тем же путём, что уже есть «Зарплата →
По менеджерам».

## Контекст

Влад параллельно с приложением сотрудников попросил эту фичу как «очень важную вещь».
Настроил ЖИВЬЁМ (03.09, при мне) две ежедневные выгрузки из 1С через FTP-рассылку
отчётов (тот же механизм, что уже несёт заказы/ДЗ/поступления/парк):
- Отчёт «Расшифровка начислений машинистам», отфильтрован по подразделению.
- Два отдельных сценария рассылки - «Водители тралов» и «Водители длинномеров» -
  каждый раз в день, оба льются в `ftp://api.yardhub.ru/uploads_payroll/` под тем же
  логином `receipts_upload`, что уже использует канал поступлений.
- Формат - .xlsx, папка на VPS `/home/receipts_upload/uploads_payroll/` уже создана и
  принимает файлы (первый тестовый файл получен и разобран 03.09 в 16:53 МСК).

**Структура файла (разобран вручную, `openpyxl`):** иерархический 1С-отчёт с
группировками, НЕ плоская таблица:
```
Отдел (3 строки-рефрена: Автотранспортный блок / Автоколонна / Водители тралов)
  Машинист (ФИО) - строка с итогами за период
    [пустая строка - якорь блока "Штрафы ГИБДД"]
      деталь: дата/дата, "Ввод начислений и удержаний водителям NNNNN от ...", сумма
    Машина (марка+модель+госномер) - строка с итогами по этой машине
      деталь: дата, "Таблица заказов NNNNN от ...", оборудование, адрес, км за МКАД,
              часы, сумма
```
Колонки по машинисту (I-U): Км за МКАД, Часы, Работа по заказам, Выслуга лет,
Километраж за МКАД, Начисления (=сумма первых трёх), Выплата аванса/Наличка,
Штрафы ГИБДД, Прочие удержания, Удержания (=сумма трёх), Итого (=Начисления-Удержания),
НДФЛ, К начислению (=Итого-НДФЛ).

Строка 2 листа - "Период: DD.MM.YYYY - DD.MM.YYYY", строка 3 - "Отбор: Подразделение
Равно "X"" (по этой строке различаем тралы/длинномеры в файле, НЕ по имени файла -
шаблон имени в 1С может быть одинаковым для обеих рассылок).

**UI-решение (не переспрашивал у Влада - совпадает с уже established паттерном):**
НЕ отдельное всплывающее окно-каталог, как в 1С на скриншоте, а тот же tab-bar canon,
что уже дважды использован в дашборде («Справочники» - showDirTab, «Поступления» -
showReceiptsTab): страница «Зарплата» получает вкладки сверху «По менеджерам» /
«По машинистам», контент переключается снизу. Однообразно с остальным дашбордом
(см. память feedback_clone_existing_visual_pattern).

## Фазы

### Фаза 0. MVP - только сводка по машинисту (~1.5-2ч)
Осознанно НЕ парсим вложенную детализацию (штрафы построчно, рейсы по машинам) в этой
фазе - только итоговая строка на каждого машиниста, как «по менеджерам» показывает
итог, не каждый заказ. Деталь - Фаза 2, если понадобится.

**03.09, найдено при реализации - важная поправка к контексту выше.** Влад подтвердил
вживую: в 1С ВСЕ машинисты (и тралы, и длинномеры) заведены под ОДНИМ отделом "Водители
тралов" - ограничение 1С, не наша ошибка разбора. Классификацию трал/длинномер делаем НА
СВОЕЙ стороне - по тому, на какой технике человек РЕАЛЬНО работал В ЭТОМ МЕСЯЦЕ
(`fleet_assignments`, пересечение периода, не "текущий момент" - для прошлого месяца
текущий водитель машины часто уже другой человек) + `vehicle_types`. Значит НЕ два
department-фильтра, а ОДИН источник (department_raw - только справочное поле), развилка
тралы/длинномеры - вкладка на фронтенде, не сортировка по 1С-полю.

Также раскрыт НАДЁЖНЫЙ признак строки-машиниста в файле - Excel outline level
(`row_dimensions.level`/`!rows[r].level` в SheetJS с опцией `cellStyles:true`) = 3.
Regex на "похоже на ФИО" (как задумывалось изначально) ЛОМАЕТСЯ на нестандартных именах
(например, "Алиев Байрам Фазаил оглы" - тюркская частица отчества с маленькой буквы) -
отброшен полностью в пользу outline level.

- [x] Таблица MySQL `driver_payroll_summary` (month_key, driver_name, 13 денежных полей,
      department_raw - справочное, source_file, imported_at) - PK (month_key,
      driver_name), БЕЗ department в ключе (см. поправку выше).
- [x] Импорт-скрипт `import-payroll.js` на VPS: сканирует
      `/home/receipts_upload/uploads_payroll/`, читает ячейки НАПРЯМУЮ по диапазону листа
      (`XLSX.utils.decode_range`/`encode_cell`, НЕ `sheet_to_json` - та даёт другой сдвиг
      индексов, чем `!rows[]`, ловушка, поймана и задокументирована в самом скрипте),
      строки-машинисты - `level === 3`. Санити-гвард - сумма машинистов ДОЛЖНА совпасть с
      итоговой строкой файла (`level === 2`), иначе импорт НЕ пишет данные - сработал
      дважды при отладке, поймал реальные пропуски строк.
- [x] cron `*/10 * * * *`, тот же паттерн, что import-debt-status.js/import-park-report.js.
- [x] `/api/driver_payroll?month=Y` (checkSession, admin-only) - джойнит
      `driver_payroll_summary` с классификацией трал/длинномер (см. поправку выше, мода
      по количеству назначений за месяц), отдаёт `{driver_name, ...13 сумм,
      vehicle_bucket}`, отсортировано по net_amount убыв. `vehicle_bucket` = "не
      определено", если пересекающих месяц назначений не нашлось - честно, не гадаем.
- [x] Вкладка «По машинистам» на странице «Зарплата»: тот же tab-bar canon, что
      «Справочники»/«Поступления» (не отдельное окно, как в 1С). Фильтр-чипы
      Все/Тралы/Длинномеры/Не определено по `vehicle_bucket` (наша классификация, НЕ
      1С-отдел). Сортируемая таблица (canon DESIGN_SYSTEM.md, проверена численно в обе
      стороны в браузере). Заголовок страницы "Зарплата - коммерческий отдел" ->
      "Зарплата" (уже не только коммерческий отдел). Коммит `49fa3ce`.
- [x] Проверка на реальном файле (август) - 55 машинистов, сумма сошлась с итоговой
      строкой файла до копейки (1.86e-9, плавающая точка). Классификация: 30 трал/17
      длинномер/8 не определено (ожидаемо для августа - `fleet_assignments` начала жить
      только с 30.08, для более старых периодов истории назначений физически ещё нет;
      должно закрыться само для будущих месяцев).

**Фаза 0 закрыта целиком (03.09, за один заход).** Импорт+API+фронтенд готовы и
проверены на реальных данных. НЕ подключён реальный CORS-тест с боевого сервера (только
синтаксис/рендер с тестовыми данными в браузере, локальный CORS блокирует прямой вызов
API с localhost) - первая настоящая проверка должна пройти на живом `yardhub.ru`.

**03.09 вечером - Влад: "страница зарплата машинистов пустая отчёт не подгрузился".**
Два бага в `/api/driver_payroll` на сервере (не в этом репо): (1) дефолт месяца при
открытии без параметра брал ТЕКУЩИЙ календарный (сентябрь) вместо последнего РЕАЛЬНО
загруженного (август) - зарплата машинистов не живой поток, как заказы, а разовый отчёт
за закрытый период, "текущий месяц" был неверной моделью дефолта с самого начала; (2)
`available_months` терялся ([]) именно тогда, когда он нужнее всего - когда у "текущего"
месяца 0 строк, фронтенд не мог даже предложить переключиться на август. Оба исправлены
(коммит на VPS `82c7076`, systemctl restart), см. README сервера, запись 03.09 вечер.

**РЕШЕНО 04.09 (было "не решено" 03.09 вечером).** Влад лично сверил дашборд с 1С по
Ставчуку Ф.Ф. за август - расхождение ровно на сумму одного рейса от 02.09.2026,
попавшего в "августовскую" выгрузку ("у него вот эта перевозка, которая в таблице заказов
000474005 от 2.09, она попала сюда. А я смотрю август"). Подтверждено на всём файле:
"Период: 01.08.2026 - 31.12.2026" - ФИКСИРОВАННЫЙ диапазон, отчёт реально копит данные с
01.08 до дня выгрузки, НЕ строго за календарный месяц. Масштаб - 21 из 55 машинистов
имели сентябрьские рейсы внутри августовской выгрузки (37 штук), 16 - штрафы вне августа
(18 штук). Фикс (VPS-коммит `b74ff01`): `import-payroll.js` теперь строго отрезает всё,
что не входит в календарный `month_key` (по настоящей дате рейса/штрафа из детализации,
не по колонке-метке файла), пересчитывает Работа по заказам/Км МКАД/Часы/Штрафы
ГИБДД/Итого/НДФЛ/К начислению от этого; `/api/driver_payroll_detail` тоже фильтрует по
настоящей дате строки. Ставчук после фикса - 463 380,14 ₽, совпало с 1С до копейки (было
478 380,19). Влад: "должно быть чётко ... если август, только август" - теперь так и
работает.

### Фаза 1. Первая настоящая рассылка по расписанию (~15 мин, ждёт события)
Пересмотрено (03.09, Влад: "нам уже надо раскидать как нам надо") - тралы/длинномеры
больше НЕ два разных 1С-отдела/файла, это ОДНА рассылка на всех, классификация целиком
на нашей стороне (см. поправку в Фазе 0). Значит "различать 2 department" больше не
актуально - ждём только первого НЕ ручного прихода по расписанию.
- [ ] Дождаться первого настоящего (не ручного тестового) прихода отчёта по расписанию -
      подтвердить, что cron `*/10мин` подхватывает файл автоматически, санити-гвард
      по-прежнему проходит на реальных ежедневных данных.
- [x] Проверить на живом `yardhub.ru` (не localhost - там CORS блокирует прямой вызов
      API), что вкладка открывается и данные корректны для реального пользователя-admin -
      частично: попап-навигация и рендер проверены (03.09), сами данные показывались
      пустыми из-за бага дефолта месяца выше - исправлено, ждёт повторной проверки Влада.
- [x] Уточнить у Влада/1С, относится ли "Период: 01.08.2026 - 31.12.2026" к строго
      месячным данным или к нарастающему итогу - ПОДТВЕРЖДЕНО 04.09: нарастающий итог,
      исправлено (см. выше).

### Фаза 2. Детализация (03.09 вечером, Влад: "нужен полный расклад как в отчёте")
Структура level 4/5 отчёта разобрана и провалидирована на реальном файле (0 расхождений
на 55 машинистах - см. коммент в шапке `import-payroll.js` на сервере): блок "Штрафы
ГИБДД" (0 или 1 на машиниста, всегда первым) и блоки по машинам (0..N - меняли технику в
течение месяца), под каждым - построчная детализация. Новые таблицы на сервере
(`driver_payroll_fines`, `driver_payroll_trips`) с тем же сани-гвардом, что и сводка -
сумма детализации ДОЛЖНА точно совпасть с колонкой сводки для каждого машиниста, иначе
импорт не пишет данные. Коммит на VPS `a97b8a8`.
- [x] Штрафы ГИБДД построчно (документ-основание, сумма, дата).
- [x] Рейсы по машинам (адрес, оборудование, км, сумма) - клик по машинисту раскрывает.
      Тот же уровень детализации, что у «По менеджерам» (детальная карточка). Модалка
      переиспользует `.crm-modal-overlay`/`.crm-modal` (тот же паттерн, что попап выбора
      раздела «Зарплата»). Новый `/api/driver_payroll_detail?month=&driver=` - лениво по
      клику, не грузим детализацию всех машинистов сразу.
- [x] Заодно (Влад, тем же сообщением): колонка "Удержания" в сводной таблице теперь
      сворачивает свои 3 слагаемых (Аванс/Штрафы/Проч.) по умолчанию - таблица была
      "слишком широкая". Раскрывается кликом по стрелке в заголовке колонки.
- [x] Проверено на живом `yardhub.ru` Владом лично (04.09) - именно эта проверка вскрыла
      находку с сентябрьским рейсом внутри августа (см. выше). Синтетическими данными в
      браузере проверялась только вёрстка/логика до этого.

**04.09, доп. правки по итогам живой проверки Влада:**
- [x] Модалка была "нечитабельно... должна быть шире, гораздо шире" - расширена
      (960px -> `min(1280px,96vw)`).
- [x] Колонка "Документ" сокращена до "№471882 · 05.08" вместо полного "Таблица заказов
      000471882 от 05.08.2026 9:06:18" (время и служебные слова убраны, `mdDocShort_`).
- [x] Новые колонки "Груз" и "Стоимость заказа" в таблице рейсов - JOIN с
      `orders_normalized` по номеру заказа (извлечён из текста документа при импорте,
      `driver_payroll_trips.order_number`). 765 из 769 рейсов августа нашли пару (4 -
      вероятно ушли в `orders_archive` при закрытии месяца, не подтягиваем оттуда - просто
      "—"). "Стоимость заказа" - это ДРУГИЕ деньги, чем "Сумма" машиниста (цена клиенту, а
      не оплата водителю) - не путать. VPS-коммит `08cfb98`.
- [x] **Первая версия фильтра (текстовые `<input>` под заголовком) - ОТКЛОНЕНА Владом:**
      "какую-то чушь сделал ... обычная просто фильтрация, как в Excel. Нажал,
      отфильтровал ... по возрастанию, по убыванию" - переделано на канон "Сортируемая
      таблица" (клик по заголовку = сортировка), закреплено в `DESIGN_SYSTEM.md` явным
      запретом на текстовые input-фильтры под заголовком, чтобы не наступать снова.
      Документ - только голый номер, без даты/значка. Оборудование/Сумма/НДФЛ убраны из
      таблицы рейсов (осталось "К начислению"). Стоимость заказа - последней колонкой,
      после неё - новая "Процент" (доля зарплаты от стоимости заказа), >20% - красным.
      Подвал таблицы по машине - "Итого по машине" (К начислению) + сумма "Стоимость
      заказа" + "средний процент" (агрегат, не среднее арифметическое).
- [x] **Найден и исправлен реальный баг, не опечатка UI - Влад заметил вживую: "почему у
      него 0% показывает".** Колонка K листа ("Работа по заказам"), которую брал как
      сумму рейса, пустая у длинномеров ("Длнй до 50тн" в файле) - их реальная сумма
      рейса лежит в S ("Итого"), собрана из L+M вместо K (контекстно другое назначение
      тех же физических колонок для другой техники). Подтверждено на всём файле:
      SUM(S по рейсам) == Начисления машиниста, 0 расхождений на 55 - старый гвард ложно
      совпадал сам с собой. Заодно ДОГЛУБИЛО вчерашний фикс месячных границ - у
      машинистов-длинномеров с рейсами вне месяца раньше вычиталось 0 (K было пустым),
      теперь полная сумма - итог по 55 машинистам за август 8376842.14 -> 8314348.14.
      VPS-коммит `34893d6`.
- [x] Сортировка "Процент" в детализации не работала ("нажимаю ... не скачет") - значение
      считалось только при отрисовке, нигде не хранилось на объекте рейса. Добавлен
      спецкейс в `mdSortVal_`.
- [x] **Новый бакет "Файмонвиль"** - закрытый список 4 тягачей (В 318 АО 997 - уточнено
      у Влада напрямую, номер совпадал с двумя машинами; В 776 ЕТ 797; М 800 АЕ 797;
      О 894 ХМ 797), приоритет над трал/длинномер. Фильтр-чип + фиолетовый бейдж
      (`--purple`). VPS-коммит `1b6504c`.
- [x] В модалке детализации - "Заказов" (число рейсов, после "Начисления") и общий
      процент рядом с "К начислению" (агрегат по всем машинам, >20% - красным).
- [x] В сводной таблице - новая колонка "Процент" (тот же агрегат, но на уровне API -
      `/api/driver_payroll` считает `order_pct` через JOIN с `orders_normalized`, не
      требует открывать детализацию по каждому). VPS-коммит `92d3ef9`.
- [x] Клик по ФОТ машины в "Технике" ведёт в детализацию этого водителя (визуально ячейка
      как была, только курсор). **Именно этот переход вскрыл чужой баг:** на Е 079 ЕТ 797
      за август стоял "Шленсков", реально Ходанов/Титаев - `/api/vehicle_drivers` брал
      водителя только из `slot = 1`, открытые записи жили на slot 2/3, сервер отдавал
      пустоту, Apps Script откатывался на устаревший лист. Третий случай класса бага
      "Пантелеев занят" (31.08); grep нашёл 5 мест на сервере, все исправлены, история
      статусов пересобрана за 30.08-02.09. VPS-коммит `d4a53b8`, память
      `project_fleet_assignments_slot_hardcode_bug_class`.

## Критерии готовности (Фаза 0)

- [ ] Вкладка «По машинистам» открывается, показывает реальные цифры за август по
      тралам, совпадающие с исходным .xlsx.
- [ ] Работает для обеих ролей просмотра (тралы/длинномеры), если ко времени приёмки
      уже пришёл второй файл - иначе честно показывает "нет данных" для длинномеров.
- [ ] Нет ошибок импорта в `import_runs` за последний прогон.
- [ ] DEPLOY_LOG.md/README VPS обновлены.

## Риски

- **Хрупкость распознавания строк** - иерархия в Excel определяется НЕ явным
  признаком, а тем, какие колонки заполнены/пусты в конкретной строке. Один
  нестандартный случай (машинист без единой поездки за период, отдел без машинистов)
  может сломать парсинг молча. Митигация - санити-гвард (Ревизор-паттерн, как у
  snapshot-vehicle-status.js): сумма всех распознанных машинистов ДОЛЖНА совпадать с
  итоговой строкой отдела - если нет, импорт этого файла падает в errors, не пишет
  частично неверные данные.
- **Деньги реальных людей** - зарплата, не премия/бонус поверх. Ошибка в парсинге
  напрямую искажает то, что человек видит про свою зарплату. Не полагаться на
  визуальную проверку "выглядит похоже" - строго сверять суммы построчно с исходным
  файлом при первой реализации.
- **Имя файла может быть одинаковым для обеих рассылок** - различать ТОЛЬКО по
  содержимому (строка "Отбор"), не по имени.

## Открытые вопросы

- Нужна ли эта страница НЕ только admin, но и самим водителям (личная страница, по
  аналогии с "Зарплата" для менеджеров)? Не спрошено - Фаза 0 делает только admin-вид.

---

## Итог

*Заполняется в конце работы над планом.*
