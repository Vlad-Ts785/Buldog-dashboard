# План: страница "Глобальная статистика" - ключевые метрики по месяцам + динамика за год

**Дата:** 2026-08-04
**Автор:** Клод (с Владом)
**Статус:** завершён

## Цель

Новая страница дашборда: выбрать любой месяц (не только текущий) и увидеть ключевые цифры
бизнеса за него (выручка, валовая прибыль по сегментам, выполнение планов), плюс график
этих же метрик по всем месяцам года одним взглядом - без переключения между вкладками.

## Контекст

Повод (Влад, 2026-08-04): на "Панели" карточка "Валовая прибыль" в начале августа временно
показала июльские цифры (пока не настроена рассылка августовского "Отчёта парк" - это
отдельный, уже решённый вопрос на стороне 1С, не баг дашборда). Разбирая это, Влад
сформулировал более широкую потребность: "эти ключевые данные важны, хочу видеть их по
каждому выбранному месяцу и в динамике по году".

Что уже есть и можно переиспользовать:
- Переключатель периода на "По менеджерам"/"Заказах" (план от 2026-06-30) - `changePeriod()`,
  `getOrdersDataForPeriod()`, `getGrossProfitForPeriod()`, `getAvailablePeriods()`. Уже умеет
  доставать факт/план продаж и ВП за произвольный архивный месяц.
- `computeSalesFaktPlan_(ordersData)` (2026-08-04, сегодняшний фикс) - общий расчёт
  факта/плана без задвоения внутренних, один источник для текущего и архивного периода.
- `aggregateFinHistoryForRange()` - уже умеет брать ВП по сегментам (тралы/длинномеры) за
  любой диапазон дат, источник - `Данные_1С_история` (архив по месяцам) с фолбэком на
  посуточные снимки `История_финансов`.
- Chart.js уже подключен и используется (графики на "Динамике", мини-график ДЗ на Панели).

Чего не хватает: сейчас "по каждому месяцу" данные достаются КАЖДЫЙ РАЗ заново (разбор
архивного листа заказов на лету + пересчёт ВП по диапазону дат). Для одного выбранного
месяца это нормально (так и работает "По менеджерам"), но для графика по ГОДУ (до 12 точек
разом) пересчитывать всё на лету при каждом открытии страницы - медленно и не нужно, эти
цифры не меняются задним числом (кроме текущего месяца). Нужен лёгкий кэш - сводная таблица
"месяц -> ключевые цифры", которая копится по ходу дела и не пересчитывается с нуля каждый раз.

## Фазы

### Фаза 1. Бэкенд: сводный лист по месяцам (~1 час)
- [x] Новый лист `История_месяцев`: Месяц | Выручка | План продаж | ВП | ВП тралы |
      ВП длинномеры | ФОТ | Топливо | Запчасти | Штрафы | Проходные | Прибыль найма |
      Выручка найма | Обновлено. "ВП план" сознательно НЕ включён - `PROFIT_PLAN` на фронтенде
      сейчас захардкожен на один месяц (не отслеживается по месяцам в данных), тащить его
      в историческую таблицу было бы неверно - см. пометку в коде на будущее.
- [x] `saveMonthSummary_(ss, monthKey)` - через `computeMonthSummary_` (чистая функция):
      `getOrdersDataForPeriod` + `computeSalesFaktPlan_` + `getGrossProfitForPeriod`
      (расширен затратами по категориям). Работает единообразно и для текущего, и для
      архивного месяца - `getGrossProfitForPeriod`/`aggregateFinHistoryForRange` уже сами
      решают, откуда брать данные. Идемпотентно, ищет строку по месяцу перед записью.
- [x] В `runAll()` - вызов после `saveFinancialHistory()` (данные уже свежие на этот момент)
- [x] `backfillMonthSummaries()` - без `_` на конце, проходит по `getAvailablePeriods()` + текущий месяц

### Фаза 2. Бэкенд: эндпоинт для фронтенда (~20 мин)
- [x] `action=global_stats` -> `{ months: [...] }`, читает `История_месяцев`, сортирует по месяцу
- [x] Текущий месяц - live `computeMonthSummary_`, подменяет строку из кэша в ответе

### Фаза 3. Фронтенд: страница "Глобальная статистика" (~1.5 часа)
- [x] Пункт в сайдбаре (🌐, сразу после "Панель")
- [x] ~~Выбор месяца + 4 карточки~~ - ПЕРВАЯ ВЕРСИЯ (селектор месяца + 4 карточки на один
      месяц) отклонена Владом целиком ("ты не старался... не хочу выбирать из месяцев, хочу
      на одной странице одной загрузкой видеть всё"). Переделано: селектор убран, 4 карточки
      убраны, вместо них - таблица "Разбивка по месяцам" (каждый месяц строкой, 15 колонок,
      строка "Итого" снизу, новые месяцы сверху)
- [x] График по накопленным месяцам (Chart.js line) - выручка/ВП/план пунктиром, названия
      месяцев в человекочитаемом виде ("Июль 2026", не "2026-07")
- [x] Загрузка `global_stats` один раз при первом заходе, БЕЗ живого пересчёта текущего
      месяца на бэкенде (был лишний getOrdersData+getGrossProfitForPeriod при каждом
      открытии страницы - именно это делало её медленной, убрано отдельным фиксом)

### Фаза 4. Проверка и деплой (~20 мин)
- [x] Проверено на моках в браузере, затем на реальных данных Влада (лог `runAll()` после
      каждого фикса) - месяцы сходятся, суммы бьются с "По менеджерам"
- [x] `backfillMonthSummaries()` запущена Владом дважды (первый раз пропустила текущий
      месяц - отдельный фикс; после - `cleanupMonthSummaries()` для дублей от бага с датами)
- [x] Деплой по протоколу, версии v138-v140 в `DEPLOY_LOG.md`

## Критерии готовности

- [x] Все накопленные месяцы видны сразу, без выбора - таблица + итоговая строка
- [x] График по году строится без ручного пересчёта, из кэша `История_месяцев`
- [x] Текущий месяц на графике не отстаёт больше чем на один прогон `runAll()` (осознанный
      компромисс ради мгновенной загрузки, см. риски)
- [x] Цифры сходятся с "По менеджерам"/Панелью за один и тот же месяц (сверено вживую)

## Риски

- ВП по парку для архивного месяца зависит от `Данные_1С_история` - если за какой-то месяц
  этот архив не был создан вовремя (отчёт парка не был настроен, как ровно сейчас с августом),
  `История_месяцев` для него останется пустой/неточной. Не глушить молча - показывать явно
  "нет данных за месяц", а не 0 или устаревшую цифру.
- `saveMonthSummary_` на каждом `runAll()` - лишняя нагрузка на таблицу, если считать текущий
  месяц через полный `getOrdersDataForPeriod`-путь (разбор архива) вместо `getOrdersData`/
  `getSummaryData`, которые и так уже посчитаны в этом же прогоне - использовать готовое,
  не пересчитывать заново.
- `backfillMonthSummaries()` может быть медленной на большом числе месяцев сразу (Apps Script
  6-минутный лимит на выполнение) - если понадобится, разбить на несколько запусков по частям.

## Открытые вопросы (закрыты Владом, 2026-08-04)

- Набор метрик: **Выручка, Затраты (по категориям), ВП (тралы/длинномеры) + планы, Маржа
  найма**. Дебиторку НЕ включать - у неё уже есть своя история на вкладке ДЗ.
- "Динамика по году" = всё, что накопилось в `История_месяцев` (сейчас с июня 2026), без
  привязки к календарному году.
- Прошлые года - вне скоупа, подтверждено.

---

## Итог

- **Реализовано целиком:** да, после одного полного редизайна фронтенда (см. Фаза 3).
- **Что сделано:** лист `История_месяцев` + `computeMonthSummary_`/`saveMonthSummary_`/
  `backfillMonthSummaries`, `action=global_stats`, страница "Глобальная статистика" - таблица
  по всем месяцам + график, без выбора периода. Отдельно (не по плану, всплыло по ходу) -
  `monthKeyFrom_`/`cleanupMonthSummaries` против конвертации дат Таблицами, убран живой
  пересчёт текущего месяца ради скорости.
- **Что не сделано и почему:** "ВП план" по месяцам не тащится в кэш - `PROFIT_PLAN` на
  фронтенде не отслеживается по месяцам, тащить как есть было бы неверно (см. Фаза 1).
- **Что добавить в бэклог:** если понадобится "план ВП" в динамике - сначала завести
  поддержку плана ВП по месяцам (аналог `Планы_менеджеров`), потом добавлять в эту таблицу.
- **Уроки:** первая версия фичи "как я её себе представил" разошлась с тем, что хотел Влад
  (селектор месяца vs "всё сразу, без кликов") - при следующей похожей задаче ("хочу видеть
  X по Y") сначала спросить формат представления (таблица/карточки/график, один экран или
  с выбором), а не угадывать по умолчанию. Класс бага "текст в дату" в Google Sheets уже
  был в памяти проекта и всё равно повторился в НОВОМ коде того же дня - при любой новой
  колонке вида "YYYY-MM" закладывать утиную типизацию сразу, не дожидаясь симптома.
