# План: «План задание» v2 - своя система заявок вместо Google Таблицы

**Дата:** 2026-09-10
**Автор:** Клод (по задаче Влада)
**Статус:** черновик - ждёт ответов на «Решения Влада» (раздел 9) и превью-мастерской
**Сроки (Влад):** тестовая работа с **20.09.2026**, полный переход с **01.10.2026**

## 1. Что решил Влад (10.09) - и что это отменяет

Дословно: «наполовину работать в системе и наполовину в таблице - это не будет работать.
Это уже точно, потому что очень долго опрашивается Google-табличка... никто так не будет
работать в неудобном интерфейсе. Сейчас нужно абсолютно заново, как будто с нуля,
продумать всю идею... с 1 октября полностью перейти на работу в этом задании.»

Это ОТМЕНЯЕТ правило от 16-18.08 (память `feedback_gradual_transition_sheets_to_dashboard`:
«Таблица остаётся основным инструментом на неопределённый срок»). Память переписана тем же
днём. Вариант Б от 18.08 (форма в дашборде, пишущая в Google-лист через Apps Script) -
признан тупиковым: медленно (Sheets API + CacheService 4 ч), хрупко (7 багов на одном
помесячном копировании, см. `plans/2026-08-18-order-plan-create-orders.md`), и не даёт
логистам живой работы.

Побочный выигрыш: помесячное копирование таблицы (`duplicateForNextMonth()`) исчезает как
класс задач вместе с таблицей.

## 2. Ключевая находка: вторая половина системы уже построена

«Планировка» (`plans/2026-08-29-logistics-planning-board.md`) - уже на сервере и в бою:
`plan_segs` (рейс на машине: госномер, kind, start_hour, length_hours, manager, logist,
route_from/to, customer, trip_mode, needs_data), поллинг 7 с, presence, история
`plan_segs_history`, сцепки `fleet_assignments`, справочники `sprav_*`, десктоп-лента в
`files/index.html` (модуль `LP`) и телефон `files/plan-m.html`. В её Фазе 4 стоит
невыполненный пункт: **«Стыковка с План задание менеджеров: заявка менеджера -> отрезок на
машине»**.

Сегодня рейс на ленте создаёт САМ ЛОГИСТ, вручную перепечатывая заказчика/маршрут/время из
Google-листа менеджера. Новый «План задания» - это недостающая **передняя половина** того же
конвейера, а не третья отдельная система:

```
МЕНЕДЖЕР                      СЕРВЕР (api.yardhub.ru)              ЛОГИСТ
создаёт заявку  ───────────>  plan_orders (новая таблица)  ──────> очередь заявок
(экран «Мои заявки»)                 │ order_id                     в Планировке
                                     ▼                                  │ drag на машину
видит: машина/водитель <─────  plan_segs (уже есть) <───────────────────┘
+ данные на пропуск            + fleet_assignments (уже есть)
+ уведомление Telegram         + sprav_people (водители, уже есть)
```

**Связь = одно поле:** `plan_segs.order_id` (nullable). Заявка - родитель, рейс на машине -
ребёнок. Одна заявка может породить несколько рейсов (несколько машин / несколько
загрузок / наёмник + своя машина). Статус «машина назначена» у заявки - производный от
наличия рейса, не вводится руками.

## 3. Два экрана (принципиальное разделение - Влад)

### 3.1 Менеджер - «Мои заявки»
Что нравится сейчас - сохраняем (Влад: «как видит менеджер, в принципе, как сейчас более-
менее устраивает»): день-вкладки / выбор даты, таблица заявок дня, «+ Новая заявка» в
шторке, «Повторить», редактирование, статус прямо из таблицы, копия данных водителя.
Что меняется: всё мгновенно (сервер, не Sheets API), статус машины/водителя приходит сам
из Планировки, уведомление «водитель назначен» (Telegram + плашка), адрес - только
нормализованный (раздел 5). Видит СВОИ заявки (админ - любого менеджера, как сейчас).

### 3.2 Логист - экран «Заявки» (таблица дня) - РЕШЕНИЕ ВЛАДА 10.09
Влад: «им падают заявки... все логисты должны видеть все заказы... регулярно ставят машины,
могут менять, переставлять, менять местами... живая система». На вопрос «очередь в ленте с
drag или отдельный экран-таблица» Влад выбрал **отдельный экран-таблицу заявок**.

- **Таблица заявок дня** - живая (поллинг 7 с, как Планировка), видят ВСЕ логисты ВСЕ
  заявки (тралы/длинномеры/наёмка - один груз могут отработать разные). Строка = заявка:
  № дня, время подачи, заказчик, тип техники, груз, A -> Б, менеджер (код), статус,
  **колонка «Машина»**. Фильтры: тип техники, «без машины», мой сегмент. Ментальная модель
  та же, что 5 лет в Google-листе (таблица дня, логист заполняет свою часть) - без
  переучивания.
- **«Поставить машину»** - клик в колонку «Машина» -> пикер: свободные в это время
  первыми (тот же паттерн пикера, что в plan-m 4.5: поиск, «Свободны сейчас / На других
  машинах / Все»), с водителем и прицепом из `fleet_assignments`. Подтвердил - на сервере
  создался `plan_seg` с `order_id` (заказчик/маршрут/менеджер/время - из заявки), в
  Планировке отрезок появился сам. **Заменить / снять / добавить вторую машину** - тем же
  пикером; снял последнюю - заявка снова «без машины». Логист подставляется автоматически
  (кто ставит), правится как в plan-m.
- **Наёмная техника** - не отдельная сущность на ленте: в том же пикере пункт «Наёмник» ->
  поля компания / госномер / водитель (как колонка M листа сегодня). Заявка получает
  пометку «наёмник: …», рейс на ленте не рисуется. (Виртуальные строки наёмников на ленте -
  бэклог после 01.10, если понадобится.)
- **Планировка остаётся как есть** - логист по-прежнему может двигать/растягивать
  созданный из заявки отрезок на ленте; изменения времени/машины на ленте отражаются на
  заявке (одна запись `plan_segs`, два представления). Очередь-панель в ленте с drag -
  НЕ строим (решение Влада), возможный бэклог.
- Телефон: та же таблица в `plan-m` (вкладка «Заявки») - Фаза 5.

### 3.3 Общее для обоих
Карточка заявки (шторка, канон канбана) - одна и та же компонента для менеджера и логиста,
различаются только редактируемые поля по роли. История изменений («Кан изменил 12 мин
назад») - по образцу `plan_segs_history`.

## 4. Модель данных (сервер, MySQL, ручной SQL-скрипт - конвенция сервера)

`plan_orders` - заявка менеджера. Поля - 1:1 из живого Google-листа (колонки A-R, пара
строк на заказ) плюс координаты и служебные:

| Группа | Поля |
|---|---|
| Идентичность | `id` (PK, auto), `service_date` DATE, `day_no` (номер внутри дня - менеджеры так ссылаются: «третья за 5-е»), `created_by`, `created_at`, `updated_by`, `updated_at`, `deleted_at` (мягкое удаление) |
| Менеджер | `manager_name`, `customer` (текст) + `customer_key` (ключ контрагента 1С/CRM, nullable), `customer_contact` (ФИО/телефон - ПДн, только по роли) |
| Груз | `equipment_type` (Трал/Длинномер/Панелевоз/Тент/Любая модификация - строгий список из «Справочника»), `cargo`, `gabarit` (Габарит/Негабарит), `rework_terms`, `documents` (Путевой лист / +ТТН), `note`, `cash` флаг |
| Точки | `load_address`, `load_lat`, `load_lon`, `load_contact`, `unload_address`, `unload_lat`, `unload_lon`, `unload_contact` (нормализованный текст + координаты + id провайдера - раздел 5) |
| Время | `service_time` (TIME, nullable = «уточнить») |
| Деньги | `price` DECIMAL, `payment_status` (список из «Справочника») |
| Статус | `status` ENUM: `new` / `confirmed` (Подтверждено) / `cancelled` (Отбой) / `not_closed` (Не закрыто) - те же три, что в листе; `assigned` - ПРОИЗВОДНЫЙ (есть живой seg) |
| Логист | `hired_company`, `hired_vehicle`, `hired_driver` (наёмная техника - см. вопрос 2), `invoice_issued` и остальные галочки логиста из колонок O-R листа (значения уточнить у логистов при переносе, не гадать) |

`plan_orders_history` - полный снимок строки перед любым overwrite/delete (тот же приём,
что спас заявку «894» 30.08).

`plan_segs` - добавить `order_id` (nullable, индекс). Ничего существующего не менять -
контракт `/api/plan/segs` потребляют два клиента (память
`project_plan_segs_fleet_state_shared_contract`), поле только добавляется.

## 5. Умный ввод адресов (Влад: «не от балды... нормальные адреса либо координаты»)

Сейчас в калькуляторе - Nominatim (OSM) + OSRM, без ключей. Влад: «в Яндексе адреса ищутся
гораздо лучше... это важно», но на вопрос о провайдере 10.09 выбрал **оставить OSM бесплатно**
(без ключей и тарифов). Значит качество добираем внутри бесплатного контура:
- **серверный прокси** `GET /api/geo/suggest?q=` / `/api/geo/geocode` - один общий для
  «План задания» И калькулятора (чиним обе точки разом), с кэшем повторных адресов
  (заказчики ездят по одним точкам) и уважением лимитов Nominatim (1 запрос/с, User-Agent);
- **два бесплатных источника в цепочке**: Photon (komoot, поверх OSM, заметно лучше
  переносит опечатки и неполные адреса) -> Nominatim; плюс нормализация запроса под РФ
  («г.», «обл.», «р-н», регион по умолчанию - Москва/МО, где 90% рейсов);
- поле хранит: текст как выбрал человек + нормализованный адрес + lat/lon + id объекта;
  **координаты вручную** - допустимы (карьер, поле, стройплощадка без адреса) - вставил
  «55.75, 37.62» или ссылку Яндекс/Google-карт - распарсили;
- если через месяц живой работы OSM всё равно «не находит» - переключение на платный
  провайдер меняет ТОЛЬКО прокси на сервере, экраны не трогаются (для этого он и прокси).

Задел, который это открывает (Влад, «идеи вслух», НЕ в этом плане): адреса + тип техники +
цена менеджера -> автосверка с прайсом (`.business/products/pricing.md`).

## 6. Связи со справочниками (где это уместно)

| Что | Откуда | Как |
|---|---|---|
| Заказчик | контрагенты 1С (`orders_normalized.customer` distinct + `debt.by_customer`) + CRM-сделки | автокомплит строго «начинается с», новый - как введён (уже так в v1 формы) |
| Менеджер | ростер `main_payload.orders.roster_managers` / `access_users` | код 2-3 буквы по правилу Планировки |
| Логист | ростер логистов | подставляется кто поставил (правило plan-m 05.09/07.09) |
| Машина/водитель/прицеп | `fleet_summary` + `fleet_assignments` + `sprav_people` | из назначения на ленте; «данные на пропуск» = готовый `lpCopyText_` |
| Тип техники, документы, условия переработки, статус оплаты | лист «Справочник» таблицы - перенести в `plan_dictionary` (или константы) | строгие списки, как валидация листа |
| Наёмная компания | лист «Наемники» -> `sprav_legal_entities`? (сейчас там только СВОИ юрлица) | вопрос 2 |

## 7. Уведомления (сегодня их даёт Apps Script таблицы - обязаны сохраниться)

- Смена статуса заказа -> Telegram в группу диспетчеров (`-1003179055158`, не секрет) -
  переносится на сервер (бот тот же, токен в `.env` сервера).
- Машина назначена/заменена/снята -> уведомление менеджеру (Telegram + плашка на его
  экране) с готовым текстом «данные на пропуск» - то, ради чего менеджеры сейчас дёргают
  логистов (раздел «База водителей» плана Планировки).
- «Заявка для водителя» (формула колонки N) -> кнопка «Скопировать заявку для водителя»
  на карточке (тот же WhatsApp-формат).

## 8. Фазы и календарь (10 рабочих дней до теста)

| Фаза | Дни | Что | Готово когда |
|---|---|---|---|
| **0. Решения + превью** | 10-11.09 | ответы Влада (раздел 9); 4 агента-персоны (логист тралов, логист наёмки, менеджер, эргономист ГОСТа) -> мастерская-превью двух экранов (артефакт) -> Влад выбирает. **Превью = контракт** | превью утверждено |
| **1. Сервер** | 12-14.09 | `plan_orders`(+history), `plan_segs.order_id`, `/api/plan/orders|order|order_status|order_delete`, гео-прокси, Telegram-уведомления, разовый импорт сентябрьских заявок из Google-листа (тест стартует на реальных данных) | смоук по SSH живыми токенами |
| **2. Менеджер** | 14-17.09 | экран «Мои заявки» нативно (форма/список/статус/повтор/копия), адресный компонент | сквозной сценарий менеджера в браузере |
| **3. Логист** | 16-19.09 | экран «Заявки» (таблица дня, живая), пикер «Поставить машину» -> seg с `order_id`, наёмник полями, отражение назначений на заявке и на ленте | заявка -> машина -> у менеджера видно |
| **4. Приёмка + старт теста** | 19-20.09 | чек-лист ГОСТа разд. 10, доступы по ролям, лист остаётся ЧИТАЕМЫМ (архив), **20.09 тест** | люди работают, замечания ежедневно |
| **5. Доводка + переход** | 21-30.09 | правки по тесту, очередь заявок в `plan-m` (логисты в поле), отключение записи в Google-лист, **01.10 переход**, лист - только чтение | ни одной заявки в листе за 1-3.10 |

**Вне этого плана (явно):** автосверка цены с прайсом, приложение водителя, статусы парка
из Планировки для статистики (отдельный план), перенос августовских/старых заявок (остаются
в листах-архивах).

## 9. Решения Влада - ПРИНЯТЫ 10.09

1. **Логист ставит машину** - в отдельном экране-таблице заявок (не drag в ленте). См. 3.2.
2. **Наёмная техника** - вопрос снят решением 1: поля на заявке (компания/госномер/водитель),
   без строки на ленте. Виртуальные строки наёмников - бэклог после 01.10.
3. **Адреса** - остаёмся на бесплатном OSM, качество добираем прокси+Photon+нормализацией
   (раздел 5). Платный провайдер - только если живая работа покажет, что OSM не тянет.
4. **Тест 20-30.09** - все сразу в новой системе, Google-лист только для чтения,
   сентябрьские заявки импортируем заранее.

## 10. Риски

- **10 дней.** Реально только при жёсткой v1 (раздел 8) и превью, утверждённом за день,
  не за неделю. Всё «идеи вслух» - в бэклог сразу.
- **`files/index.html` 29 тыс. строк, параллельные сессии.** Менеджерский экран - замена
  существующей вкладки `order-plan` на месте; очередь логиста - внутри модуля `LP`. Мелкие
  коммиты, слияние с `main` перед каждым push (память `feedback_parallel_sessions_same_project`).
- **ПДн** (контакты заказчиков, данные водителей) - только через сервер по роли, в
  публичный фронтенд не зашиваются; выдача в журнал.
- **Экономность** (память `feedback_economical_browser_automation`): проверка через текст и
  живые токены по SSH, скриншот - только финальная сверка; ни одного цикла «подожди-скриншот».
- **Люди.** 5 лет в таблице. Смягчение: v1 повторяет ИХ поля и ИХ слова (глоссарий
  `.business/assets/glossary.md`), не переименовывает привычное.

## 11. Критерии готовности к 01.10

- [ ] Менеджер создаёт заявку быстрее, чем в листе (замер на реальной заявке)
- [ ] Логист видит новую заявку в очереди не позже 7 с, ставит машину одним жестом
- [ ] Менеджер видит машину/водителя и копирует данные на пропуск, не звоня логисту
- [ ] Telegram-уведомления по статусу и назначению приходят как раньше
- [ ] Чек-лист ГОСТа (разд. 10) пройден целиком на обоих экранах
- [ ] За 1-3.10 в Google-листе ноль новых заявок
