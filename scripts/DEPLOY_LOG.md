# Лог деплоев Apps Script (Дашборд_Apps_Script)

Одна строка на версию. Цель - чтобы любая сессия перед деплоем видела, что уже
задеплоено и кем, и не откатывала чужую работу вслепую.

**Правило перед КАЖДЫМ `clasp push`:** `clasp pull` -> `diff --strip-trailing-cr` с
`scripts/full_script_final.js` в git HEAD этого репозитория. Если diff показывает
что-то, чего нет в последнем коммите репозитория - это чужая недокоммiченная работа,
её нужно сначала сохранить (sync-коммит), и только потом вносить свои правки. После
`clasp push` + `create-version` + `redeploy` - сразу добавить строку в эту таблицу
(лог начат 2026-07-15, более ранние версии не восстановлены задним числом).

**Примечание (2026-08-14):** начиная примерно с этой точки номера в таблице разошлись с
реальными версиями Apps Script на 1 (где-то в ночь на 14 августа был незалогированный
деплой) - сверяй `clasp list-versions`, если нужна точность, не только эту таблицу.

| Версия | Дата | Что изменилось |
|---|---|---|
| 180 (реальный, по clasp list-versions) | 2026-08-14 | Влад сверил historical мега-базу (январь-май 2026) с БДР финансистов ("Операционный бюджет тралного отдела") - выручка систематически ниже БДР. По ходу разбора нашли и исправили: короткое слово "БАЗА" в INTERNAL_CLIENTS случайно резало реального клиента "АВТОБАЗА 2020 ООО" (Влад подтвердил: "Автобаза 2020 это клиент, остальные внутренние") - убрано и здесь, и в мега-базе (client_history_normalize.js), "БАЗА ДМД" (точное совпадение) осталась. Плюс эмодзи 💰 внутри столбика "Поступления" на графике (Влад: "прям внутри полоски столбика"). Полная сверка выручки с БДР ещё НЕ завершена - остаётся необъяснённый разрыв (~2.2М за январь даже после сложения всех категорий фильтрации), гипотеза - нестрогая сортировка по дате в сыром листе мега-базы, проверяется новой verifyDateSortingAndFullRevenue() (в мега-базе, ещё не запущена). |
| 103 | 2026-07-13 | ДЗ: гарантийный платёж/депозит закрывают долг как аванс |
| 104 | 2026-07-14 | ДЗ: фикс бага master-строк ("Поступление..." ошибочно считалось контрагентом), фильтр по диапазону просрочки, таблица гарантий, ручной статус взыскания |
| 105 | 2026-07-15 | ДЗ: таблица гарантий - только депозиты (не платежи), drill-down "что изменилось" по клику на стрелку тренда, карточка контрагента (статус + свободный комментарий, по образцу карточки машины) |
| 106 | 2026-07-16 | ДЗ: drill-down "что изменилось" по клику на стрелку тренда конкретного юрлица (не только общая сумма), 4-й блок KPI "по статусам" с фильтром по клику, корзина "Мёртвая" (600+ дней) в блоке "Просрочка", новый статус "В графике" |
| 107 | 2026-07-16 | ДЗ: взаимозачёт - колонка "Сумма нашего долга" (L) из отчёта 1С теперь вычитается из баланса контрагента (реальные деньги за встречные услуги типа аренды спецтехники, которые 1С ещё не разнесла по актам перевозок) |
| 108 | 2026-07-16 | ДЗ: два новых статуса - "Поручение" и "Вх. претензия" |
| 109 | 2026-07-16 | ДЗ: исключить контрагентов без документов от реального менеджера (только master-строки - банковские поступления/списания, фиктивный долг от отрицательного аванса) |
| 110 | 2026-07-16 | ДЗ: расшифровка чем покрыт долг в детализации документов (аванс/гарантийный платёж/гарантийный депозит/взаимозачёт по отдельности), новая колонка "Сумма нашего долга" в ДЗ_данные |
| 111 | 2026-07-16 | ДЗ: автостатус "В графике" для долгов 0-30 дней без ручного статуса, после 30 дней снова пусто (не пишется в ДЗ_Статусы, пересчитывается на каждый запрос) |
| 112 | 2026-07-16 | Панель: карточка "Дебиторская задолженность" вместо "Поступления" (сумма 0-90 дней + мини-график, новая колонка "ДЗ 0-90 дней" в История_ДЗ). ДЗ: статус "Банкрот" + кнопка "Показать банкротов" (по умолчанию скрыты везде, включая график истории - исключаются навсегда на бэкенде при записи) |
| 113 | 2026-07-16 | Фикс: "Заказов" на "Водителях" показывал "—" для водителей вне топ-25 по компании (дефолтный вид без выбора периода брал усечённый D.orders.by_driver вместо полного driverOrderCounts) |
| 114 | 2026-07-16 | Фикс: "Заказов" на "Водителях" суммировал всех однофамильцев в один счётчик (ключ был по фамилии, стал по фамилии+имени) |
| 115 | 2026-07-16 | Воронка документов: если есть "Реализация", заказ считается "готово" даже без флагов путёвки/проведения (WAYBILL_SKIP_IF_REALIZ, легко откатить) |
| 116 | 2026-07-16 | Проблемные заказы: сортировка по дате (от раннего к позднему), новые колонки "Водитель"/"Наёмник" |
| 117 | 2026-07-16 | Воронка документов: исключить служебные строки ("Вариант расчёта" = "Прочее" - смена водителя, тех. записи), новая колонка в Заказы_данные, FUNNEL_EXCLUDE_OTHER_PAYMENT_VARIANT легко откатить |
| 118 | 2026-07-16 | Воронка документов: исключить и наличные расчёты ("Вариант расчёта" = "Наличный" - структурно без путёвки/проведения/реализации), FUNNEL_EXCLUDE_CASH_PAYMENTS легко откатить |
| 119 | 2026-07-17 | Воронка документов: строка сверки (всего/в воронке/внутренние/наличные/прочее), без двойного счёта. Проблемные заказы: колонка "Остаток" заменена на "Оплата" |
| 120 | 2026-07-17 | ДЗ: отрицательный аванс/гарантия/взаимозачёт по юрлицу больше не создаёт фиктивный долг (ЮУСК ООО: 313 000 -> верные 210 000) |
| 121 | 2026-07-17 | Обзор заказов: "Топ грузов" убран, вместо него карточка "Заказов" на всю ширину с выручкой/ВП/маржой найма/средним чеком/% проблемных по сегментам (тралы/длинномеры) |
| 122 | 2026-07-17 | Фикс: ВП по длинномерам всегда была 0 - классификатор сегмента (getSummaryData) читал ненадёжную колонку "Тип техники" (detectType из названия машины), исправлено на "Тип из Штатки" - тот же источник, что "Статус парка"/"Все тралы" |
| 123 | 2026-07-17 | По менеджерам: стрелка динамики "сегодня vs вчера" по заказам и выручке + drill-down по клику (список заказов, созданных сегодня) |
| 124 | 2026-07-17 | ДЗ: колонка "Дней должник" в drill-down "что изменилось со вчера". Синхронизирована чужая незакоммiченная работа (Битрикс24 CRM, action=marketing) - была задеплоена напрямую через clasp в live, минуя git |
| 125 | 2026-07-17 | Фикс: триггер автозапуска migrateShtatkaGridToHistory() передавал объект события вместо года - дата в Штатка_история портилась на "[object Object]". Плюс разовые функции: обобщённая cleanupUndefinedShtatkaHistory() и новая checkShtatkaHistoryGaps() (проверка пропусков за 11-12 июля) |
| 126 | 2026-07-17 | Фикс: "Статус парка" на Панели показывал 0 везде - getFleetStatus() искал текстовые фразы "В работе"/"Ремонт" в статусе, а схема давно сменилась на короткие коды (0-5/Р/В/РВ, переход на Штатка_история 2026-07-01), функцию тогда не обновили |
| 127 | 2026-07-18 | Revert версии 126 - неверный диагноз. Реальная причина нулей: Влад случайно переименовал лист в отдельной "рабочей Штатке", откуда лист "Штатка" в дашборд-таблице тянет данные через IMPORTRANGE - сломался #REF!, getStaffData() отбрасывал все строки. Статус там текстовый ("В работе"/"Ремонт"), код вернули как было |
| 128 | 2026-07-18 | Фикс: карточка "Заказов" (Тралы/Длинномеры) не обновляла "Валовая прибыль (свой парк)" при выборе прошлого периода - оставалась от текущего месяца. getGrossProfitForPeriod() переписан на aggregateFinHistoryForRange (та же методика, что "Парк техники" за диапазон), теперь с разбивкой по сегментам |
| 129 | 2026-07-18 | Новая страница "Оргструктура" вместо "Системы решений" (скрыта, не удалена) - интерактивное дерево 4 звеньев, поиск по имени, водители автоколонн скрыты за кнопкой. Штатка (веб-страница/календарь статусов) удалена полностью - фронт и бэкенд-обработчики shtatka_grid/set_status/set_driver; миграция в Штатка_история оставлена |
| 130 | 2026-07-19 | Фикс: тралы/длинномеры в ремонте без единого заказа за период пропадали из "Парк техники" - writeParkHistoryForMonth_ пропускал строку архива при нулевой выручке/ВП, aggregateFinHistoryForRange строил список машин только из найденных строк истории, не из Штатки |
| 131 | 2026-07-19 | ДЗ: фикс стрелки тренда "Общая ДЗ"/по юрлицам - сравнивала отфильтрованное "сегодня" (без долгов 600+ дней) с полным "вчера" из истории (давало ложное -24% при факте +213 857), плюс выбор дня сравнения по дате, а не по индексу в массиве. Кнопки День/Неделя в "Что изменилось" - action=debt_changes_period&days=N, getDebtData(ss, compareDaysBack) |
| 132 | 2026-07-19 | Фикс: "Выполнение плана продаж" показывало факт 29 912 408 из 70 000 000, хотя по составу 14.7 (Ахтамова) + 15.2 (Гусейнова) + 2.2 (внутренние) = 32.1М. salesPlan включает план "Внутренние" (5 млн), а salesFakt складывался только из by_manager - внутренние рейсы ведут логисты, которых нет в TRAL_MANAGERS. Прибавляем только ту часть внутренних, которой нет в суммах менеджеров (без задвоения) |
| 133 | 2026-07-24 | Фикс: "куда-то пропали все данные" - Панель/Обзор заказов/По менеджерам обнулились. normalizeReport() и normalizeOrders() чистили Нормализованные_данные/Заказы_данные ДО проверки, распознались ли новые строки из отчёта 1С - при битом/пустом отчёте лист оставался пустым (стёрт, ничем не заменён) до следующего успешного прогона. Теперь проверка идёт первой, лист трогается только когда есть что записать. Синхронизирован также живой Bitrix24-код другой сессии (не в git до этого коммита) |
| 134 | 2026-07-24 | Настоящая причина того же обнуления: splitOrdersRawByMonth() относит строки отчёта заказов к месяцу по колонке "Начало работ" - у заказов без начатого рейса (обычное дело в середине месяца) она пустая, и строка молча выпадала из ВСЕХ месячных корзин целиком. В "Заказы_сырые" из десятков строк письма осталась 1. Добавлен фолбэк на "Дата создания" |
| 135 | 2026-07-24 | Фикс регрессии от версии 134: заказ без "Начало работ" с "Датой создания" уже в августе создал бакет "2026-08", который стал "самым поздним месяцем в данных" - именно так раньше выбирался "живой" месяц (monthsPresent[length-1]), поэтому живая таблица получила 1 строку августа, а реальный июль (928 строк) уехал в архив. Живой месяц теперь = реальный календарный месяц (Utilities.formatDate(new Date())), а не самый поздний месяц, который встретился в файле |
| 136 | 2026-07-24 | Фикс: "Service Spreadsheets timed out" на каждом шаге runAll() после импорта парка - ручной запуск наложился на уже идущий/только что прошедший прогон (плюс таблица была открыта в браузере), несколько скриптов одновременно долбили одну таблицу на 2000+ строк. LockService.getScriptLock() на весь runAll() - если лок занят, функция выходит вместо параллельного захода |
| 137 | 2026-08-04 | Три фикса перехода на новый месяц на "По менеджерам": (1) верхняя полоса "Выполнение плана продаж" всегда читала живой факт/план текущего месяца вместо выбранного периода - вынесено в computeSalesFaktPlan_(ordersData), общее для getSummaryData и action=orders_period; (2) calcPaceRatio_() для закрытого прошлого периода экстраполировал полный факт месяца от сегодняшнего дня (967% прогноз) - теперь ratio=1 для любого периода, кроме текущего календарного месяца; (3) getAvailablePeriods() исключает текущий месяц из списка архивов - убрал дублирующий и падающий с ошибкой пункт "2026-08" |
| 138 | 2026-08-04 | Новая страница "Глобальная статистика" (см. plans/2026-08-04-global-stats-page.md) - выбор месяца + выручка/ВП(тралы/длинномеры)/затраты(5 категорий)/маржа найма + график по накопленным месяцам. Новый лист История_месяцев (кэш, не пересчитывается на лету), action=global_stats, saveMonthSummary_ на каждом runAll(), разовая backfillMonthSummaries() - Владу нужно запустить вручную |
| 139 | 2026-08-04 | Фикс: backfillMonthSummaries() пропускал текущий месяц ("нет данных") - computeMonthSummary_ читал заказы только через getOrdersDataForPeriod (архивный лист "Заказы_YYYY-MM"), для ещё не заархивированного текущего месяца такого листа нет. Теперь для текущего календарного месяца - getOrdersData(ss) (живой лист) |
| 140 | 2026-08-04 | Фикс "Глобальной статистики": месяцы в История_месяцев превращались в мусорные Date.toString() (Таблицы молча конвертировали "2026-07"), из-за чего ломалась и сортировка, и проверка на дубли (август записался дважды). monthKeyFrom_() - утиная типизация на запись и на чтение, текстовый формат колонки, разовая cleanupMonthSummaries(). Убран живой пересчёт текущего месяца в action=global_stats (была причина медленной загрузки) - теперь только чтение кэша. Фронтенд: убраны 4 KPI-карточки, названия месяцев - "Июль 2026" вместо "2026-07" |
| 141 | 2026-08-06 | Зарплата: (1) регрессия порога 23% - снова считался по каждому заказу вместо компании за месяц целиком, починено; (2) Отдел Рыщанова - вся логистика (Кан/Махура/Сильчев/Прус-Роскошный/Васин) одним блоком вместо отдельной карточки "Логисты"; (3) Васин - особая формула 2.5% от ВП длинномеров по компании, не от личных заявок; (4) сотрудники без выручки за период (уволенные) больше не рендерятся; (5) приказ №01/07/26 - Рыщанову +1% ВП трёх конкретных тралов, гейт по дате до 01.11.2026; (6) таблица "Наёмная техника" - "Затраты" переименовано в "Стоимость найма", добавлена новая колонка "Затраты" = вознаграждения+спецразрешение+НДС-корректировки (выведено обратным счётом из "Прибыль" 1С, сверено день-в-день с живым заказом); (7) % комиссии своего парка у менеджеров теперь считается от выручки минус эти же три статьи затрат, не от валовой выручки. См. plans/2026-08-06-salary-logist-dept-and-margin-fix.md |
| 142-147 | 2026-08-06/10 | Не залогировано этой сессией (задеплоено параллельными сессиями напрямую через clasp, мимо этого файла) - живой файл сверен `diff --strip-trailing-cr` с git HEAD (9770fb5) перед версией 148 и оказался идентичен, расхождений/чужой недокоммiченной работы не найдено |
| 148 | 2026-08-10 | Личная страница логиста (Прус-Роскошный и остальные) - `logist_detail` в aggregateOrdersRows (маржа по поставщикам его сделок, воронка документов по поставщику, список сделок), `getLogistView_`/роль `logist` в doGet. См. plans/2026-08-10-logist-personal-page.md |
| 149 | 2026-08-11 | Личная страница: шапка ФИО+должность (positionLabel), переключатель "Свои продажи"/"Общие сделки" у логиста (общекорпоративная маржа по поставщикам/воронка документов/сделки, зарплата не переключается). Попутно: глобальная воронка документов по поставщику расширена до полной (было только no_waybill), унифицирован источник no_waybill между личной и общей воронкой. Плюс правки Влада по статусам ДЗ ("Долг отдела кранов"/"Долг отдела экскаваторов", коммит 06416b7) - задеплоены вместе, были только в git, не в live. См. plans/2026-08-10-logist-page-toggle-and-header.md |
| 150 | 2026-08-11 | Личная страница: выбор периода "Текущий месяц"/"Прошлый месяц" (Влад: "нужно сделать ещё выбор периода"). getManagerView_/getLogistView_ разобраны на buildManagerView_/buildLogistView_ + getManagerViewForPeriod_/getLogistViewForPeriod_; новый action=my_page_period для ролей manager/logist (та же фильтрация "только свои данные"). Admin-предпросмотр использует уже существующий orders_period. См. plans/2026-08-11-my-page-period-selector.md |
| 151 | 2026-08-11 | Личная страница логиста-длинномерщика (Васин Максим) - принципиально другая, чем у наёмных логистов: ВП по каждой машине (aggregateFinHistoryForRange, как "Техника"), воронка путевых листов, несданные путёвки по водителям, все сделки по длинномерам (свой парк + наём, независимо от исполнителя), расчёт зарплаты (calcVasin). Новый action=long_haul_detail (getLongHaulDetail_). Попутно 2 фикса: диспетчер личной страницы больше не блокирует рендер при пустых by_manager_detail/logist_detail (норма для Васина), admin-дропдаун строится из ростеров by_manager/by_logist, а не detail-карт. См. plans/2026-08-11-vasin-long-haul-page.md |
| 152 | 2026-08-11 | Ускорение страницы Васина: убран дублирующий полный пересчёт заказов (buildLogistView_ теперь прикладывает long_haul к уже загруженному orders вместо повторного getOrdersData) и лишний HTTP round-trip (renderMyPageVasin стала синхронной - logist-роль читает D.orders.long_haul сразу из логина/my_page_period, admin на текущем месяце собирает бандл из уже загруженных D.orders/D.vehicles/D.summary без единого доп. запроса). Плюс блок "Выполнение плана ВП (длинномеры)" с прогнозом (planProgressBar, calcPaceRatio_). См. plans/2026-08-11-vasin-perf-and-forecast.md |
| 153 | 2026-08-11 | Страница Васина: прогноз выполнения ВП по каждой машине (переиспользован driverForecastStatus_ с "Водителей" - Идёт по плану/Добавить заказов/Загрузить срочно/Простой-разобраться), блок "Задачи на сегодня" между KPI и таблицей машин (максимум 5: топ-3 водителя без путевых + отстающие по плану машины), убрана вся информация по наёмной технике (новое поле own_long_amount, сделки только собственного парка). См. plans/2026-08-11-vasin-per-vehicle-forecast-tasks.md |
| 154 | 2026-08-11 | Исключена машина в аресте (госномер А 840 КО 797, SCANIA) из всех расчётов - единая точка EXCLUDED_VEHICLE_GOS/isExcludedVehicleGos_ в getStaffData (состав парка) и aggregateFinHistoryForRange (защита от остатков в истории), каскадом убирает её из статуса парка/Техники/Водителей/страницы Васина. Плюс уточнены слоты "Задачи на сегодня" - 2-3 = "добавить заказы" (прогноз >80%), 4-5 = "простой - разобраться" (прогноз <=80%, только если есть выручка, машины с нулевой выручкой не получают задачу). См. plans/2026-08-11-vasin-per-vehicle-forecast-tasks.md |
| 155 | 2026-08-12 | ДЗ: 1С 12.08 сдвинула колонку "Менеджер" в отчёте (F->E) - parseDebtRawRows_ читал пустой row[5] на каждой строке документа, hasRealManagerDoc никогда не выставлялся, весь отчёт схлопывался в 0 клиентов, importDebtReport() падал ДО sheet.clear() - ДЗ_данные тихо замерла на старых цифрах несколько дней, отсюда пустой "Что изменилось". Фикс: cleanManagerName_(row[4]) \|\| cleanManagerName_(row[5]) (переживёт обратный сдвиг). Плюс saveDebtHistory(): dataForTotal - сумма в История_ДЗ теперь тоже исключает "Долг отдела кранов/экскаваторов" (как и фронтенд), иначе стрелка тренда "Общая ДЗ" сравнивала разные по составу суммы и не билась со списком "Что изменилось" (баг найден Владом 12.08, повод для расследования). Обнаружено и продиагностировано через Влада: письмо оказалось непрочитанным, ручной ран алл в редакторе показал реальную ошибку импорта в логе. |
| 156 | 2026-08-12 | Личные страницы: зарплата переставлена сразу после KPI-блоков (Васин, наёмные логисты). Новая личная страница менеджера (на примере Савитка) - план продаж + таргет по рабочим/выходным дням (weekendRatio_), детальный расчёт зарплаты (calcMgr, те же компоненты, что вкладка Зарплата), блок с 4 вкладками (Топ заказчиков / Пропавшие клиенты за 3 месяца сорт. по выручке / ДЗ конкретно этого менеджера с фильтрами по дням/сумме, общее ядро с global ДЗ / Воронка документов). 4 старых KPI-блока убраны. Новый action=manager_lost_customers, buildManagerView_ прикладывает ДЗ менеджера. См. plans/2026-08-12-manager-page-redesign-and-salary-position.md |
| 157 | 2026-08-12 | ДЗ и Воронка документов на странице менеджера - переиспользовано общее ядро дашборда вместо изобретения заново. ДЗ: убран текстовый ввод мин. суммы (двойной слайдер диапазона .range-slider), разворачивание строки контрагента через buildDebtDocsHtml_ (то же ядро, что global ДЗ - документы/даты/юрлицо/сверка), цвет просрочки (ageCls) и разбивка по юрлицам (byOrg), статус - только просмотр без возможности менять. Воронка документов: под сводкой добавлена детальная таблица проблемных заказов (тот же формат, что "Обзор заказов → Проблемные заказы", problemOrderRowHtml_ вынесена в общую функцию для обеих страниц). Бэкенд: buildManagerView_ прикладывает problem_orders, отфильтрованный на менеджера. См. plans/2026-08-12-manager-debt-and-docs-reuse-core.md |
| 158 | 2026-08-12 | 5 задач на день от ИИ (GPT-5 через kie.ai) на личной странице менеджера - по просьбе Влада, аналог алгоритмических задач Васина, но сгенерированный моделью по личной ситуации менеджера (план/факт/прогноз, топ заказчиков, пропавшие клиенты, дебиторка). Новый лист ИИ_Задачи_Менеджеров - кэш на календарный день, не вызывает ИИ на каждое открытие страницы. action=generate_ai_tasks в doGet, ключ KIE_API_KEY только из Script Properties. ВАЖНО: живой вызов kie.ai не проверен в этой сессии (нет доступа к ключу/редактору Apps Script) - Владу нужно вручную добавить Script Property KIE_API_KEY и проверить первую генерацию, см. plans/2026-08-12-ai-daily-tasks-manager.md |
| 159 | 2026-08-12 | Доработка ИИ-задач по разбору первой живой генерации (Влад построчно разобрал 5 задач) - контекст обогащён конкретными документами по должникам (дата/юрлицо/номер/сумма), датой последнего заказа у пропавших клиентов, примерами проблемных заказов; промпт получил правильные причинно-следственные факты (путевые листы - зона логистов, непроведённые документы не "держат выручку", а снижают вероятность оплаты) и разделение целей "рост продаж" vs "деньги/документы" в совете по плану; новое поле category на задаче. UI: убран заголовок "Личная страница менеджера", компактный виджет прогноза плана в шапке, единый порог 3-зонной раскраски (0-50/50-80/80-100+) для всей полосы плана. См. plans/2026-08-12-ai-daily-tasks-manager.md |
| 160 | 2026-08-12 | Третий разбор Влада (скриншот Цегельникова): полный блок "Выполнение плана продаж" (шкала+Факт/Прогноз/План+таргет по дням) встроен целиком в шапку рядом с фамилией (было - только компактный %, не устроило); цвет заливки шкалы теперь по прогнозу, а не по факту (факт середины месяца почти всегда красный по определению календаря - вводило в заблуждение, при прогнозе 91% полоска теперь зелёная); в "Расчёт зарплаты" - рекомендация до следующей ступени оклада ("не хватает X оборота - оклад вырастет до Y"); промпт ИИ - запрет повторять проценты/суммы плана в тексте совета (кэшируется на день, а факт растёт в течение дня - расходилось с живой шкалой, 84% vs 91%). См. plans/2026-08-12-ai-daily-tasks-manager.md |
| 161 | 2026-08-12 | Четвёртый разбор Влада: шкала плана убрана из шапки обратно в отдельный полноширинный блок ("не нравится, как ФИО и полоска рядом - ФИО на самый верх, полоску опусти вниз, как было"), тоньше (16px->8px); пустая .page-header больше не оставляет линию-разделитель над шапкой личной страницы; "Расчёт зарплаты" визуально аккуратнее (строки в два уровня, проценты - цветные бейджи, как на вкладке "Зарплата"), новая строка "Итого по прогнозу" синим цветом (текущий темп через paceRatio и тот же calcMgr, подхватывает переход ступени оклада); правила эскалации ДЗ для ИИ-задач - долги 600+ дней и "чужие" отделы исключены из контекста целиком (было: "бесполезная рекомендация" на долг 827 дней), для остальных - 60-90 дней = составить претензию, 90-600 = подать в суд, пропуск уже эскалированных по статусу. См. plans/2026-08-12-ai-daily-tasks-manager.md |
| 162 | 2026-08-12 | Пятый разбор Влада (МИК-СЕРВИС+/ТСК-3): buildManagerAiContext_ теперь собирает ДЗ менеджера первой и помечает top_customers/lost_customers полем unpaid_balance при наличии непогашенного остатка - промпт запрещает рекомендовать наращивание объёма с должником без оговорки "сначала оплата"; промпт: "возобнови работу с..." вместо "верни клиента"; определение грамматического рода по отчеству менеджера (-вна/-чна = женский, -вич/-ич = мужской) - была системная ошибка, женщинам доставался мужской род по умолчанию в обращении на "ты"; явное требование разделителей тысяч в числах ("7 618 250", не "7618250"). См. plans/2026-08-12-ai-daily-tasks-manager.md |
| 163 | 2026-08-12 | Кнопка "🔄 Заново" в блоке "Задачи на сегодня (ИИ)" - сброс кэша на сегодня и генерация заново, после серии правок промпта старые кэшированные строки (например, у Савитка) не обновлялись сами по себе, Влад просил способ "обнулить" без ручного похода в Google Таблицу. deleteAiTasksCacheRow_ + параметр force в generateManagerAiTasksCached_/doGet action=generate_ai_tasks; retryAiTasks_ теперь всегда шлёт force:'1'. См. plans/2026-08-12-ai-daily-tasks-manager.md |
| 164 | 2026-08-12 | ИИ-задачи: даты в контексте (last_order_date, unpaid_docs[].date, documents.examples[].date) теперь форматируются на бэкенде в русский вид ("18 июля"/"7 мая 2024") до того, как попадут в промпт - модель раньше сама пыталась переформатировать голый ISO и перепутала на американский порядок ("07.17.2026"); заодно исправлен порядок операций - сортировка "самые старые документы первыми" теперь на сыром ISO до форматирования (иначе ломался хронологический порядок). documents.examples получил ту же кросс-ссылку с ДЗ, что top_customers/lost_customers (unpaid_balance) - промпт теперь различает "у клиента есть долг → провести документы ускоряет оплату" и "долга нет → просто документальный порядок закрытия сделки", раньше писал одинаковое обоснование для обоих случаев. См. plans/2026-08-12-ai-daily-tasks-manager.md |
| 165 | 2026-08-13 | Счётчик входов сотрудников (Влад: "раздаю доступы, хочу видеть кто сколько раз заходил") - новый лист Логи_входов (одна строка на email, счётчик + первый/последний вход, не полный лог каждого визита), logAccessVisit_ вызывается в doGet только когда action пуст (главная загрузка страницы, не каждый мелкий доп-запрос), getAccessLogSummary_ отдаётся только роли admin вместе с основным ответом. Фронтенд: карточка "👀 Активность сотрудников" на Панели управления. |
| 166 | 2026-08-13 | Новая вкладка "Поступления" - реальные деньги на р/с (тралы Ярд), отдельный отчёт 1С (Влад настроил рассылку 3 раза в день). importReceiptsReport()/parseReceiptsRawRows_() - импорт накопительного с начала месяца отчёта, полная перезапись текущего месяца (Поступления_данные) + архив по месяцам (Поступления_YYYY-MM), без отдельного листа истории (не нужен - каждая строка несёт свою дату навсегда). Менеджер = ответственный за последнюю сделку клиента (getClientManagerMap_, переиспользует клиентскую аналитику - Влад явно отверг гипотезу про пометки в скобках отчёта как признак менеджера), резолвится и сохраняется при импорте. getReceiptsData() - сумма за месяц/сегодня/среднее, по дням, по менеджерам, по клиентам, по месяцам. Парсер проверен на реальном образце файла (сумма строк точно = "Итого", 225 строк). Фронтенд: пункт меню сразу после "Глобальной статистики" - KPI, график по дням/месяцам, таблицы по менеджерам/клиентам. См. plans/2026-08-13-receipts-report-tab.md. ВАЖНО: importReceiptsReport() ещё не запускался вручную (нет доступа к почте из этой сессии) - лист Поступления_данные появится и заполнится только после ближайшего планового runAll() или ручного запуска в редакторе. |
| 167 | 2026-08-13 | Оглавление ручных функций в самом начале файла (Влад: "столько уже функций, что неудобно листать искать") - список актуальных для регулярной работы (runAll/import*/setupTrigger/setupAccessSheet/setupShtatkaAutoMigration) с описанием и подсказкой Ctrl+F, плюс пояснение, что остальные ~80 функций без "_" в конце - разовые диагностические/фиксовые скрипты под старые инциденты (Bitrix24, дедуп заказов и т.п.), почти все уже отработали своё. Без изменений в логике. |
| 168 | 2026-08-13 | Влад собирался разово выгрузить "Поступления" за пол года (с начала года) на ту же почту, спросил "сам ран алл спарсит или сюда лучше кинуть?" - при СТАРОЙ логике импорта это было бы опасно: importReceiptsReport() маршрутизировал по единому "месяцу периода отчёта" целиком, разовая выгрузка за 6+ месяцев схлопнула бы всё в один "текущий месяц" и испортила бы KPI/график. Фикс: маршрутизация теперь по месяцу КАЖДОЙ СТРОКИ (группировка), один алгоритм одинаково безопасно обрабатывает обычный ежедневный импорт, позднюю коррекцию прошлого месяца И разовую массовую историческую выгрузку - archiveMonths/toSheetRows, проверено на симуляции 4-месячного файла (суммы сошлись, ничего не потеряно/задвоено). Заодно реализовано сравнение "Поступления vs Выручка" по дням/месяцам (Влад: "сравнивал поступления с выручкой"): aggregateOrdersRows() получил summary.by_day (тот же источник/фильтрация, что total_amount), getReceiptsData(ss, ordersData) - revenue в daily (текущий месяц, из ordersData) и monthly (из кэша История_месяцев, без пересчёта архивов заказов на каждый запрос). Фронтенд: график "Поступления" - bar+line combo с "Выручкой", null = разрыв линии, не ложный ноль. |
| 169 | 2026-08-13 | Влад: график "Поступления/Выручка" не должен быть кривой линией - тоже столбики, "чтобы было чётко видно разницу". Плюс два новых требования: сальдо (поступления-выручка) с начала года, и учёт наличных поступлений (отчёт 1С по р/с видит только безнал, наличка видна в таблице заказов, колонка "Оплата нал"). Реализовано: график - "Поступления" сложенный столбик (безнал+нал, stack:'receipts') рядом со столбиком "Выручка" (stack:'revenue'), и по дням, и по месяцам. aggregateOrdersRows() получил summary.total_cash/by_day_cash. История_месяцев - новая колонка "Наличные" (строго в конец, после "Обновлено", не сдвигает индексы старых строк). backfillMonthSummaries() воссоздана (была удалена как одноразовая 2026-08-04) - для пересчёта колонки в уже существующих строках, добавлена в оглавление ручных функций. getReceiptsData() - summary.ytd (сальдо с начала года, с пометкой, если период выручки короче периода поступлений). Открытый вопрос (не решён в этой сессии): Влад считает, что уже загружал архив заказов за май - в коде дашборда свидетельств архива "Заказы_2026-05" нет (видимо, речь про отдельную offline-систему клиентской аналитики "мега база", не подключённую к дашборду) - ждёт проверки Влада, есть ли реально такой лист в таблице. |
| 170 | 2026-08-13 | Влад вспомнил: "мы вроде делали отдельную таблицу по той большой выгрузке за предыдущие периоды и года" - это подтвердилось: CLIENT_HISTORY_SHEET_ID ("Нормализованные_история_заказов"/"История_клиентов_агрегат"), уже подключена к дашборду для вкладки "Клиенты", покрывает выручку по дням вплоть до CLIENT_HISTORY_CUTOFF='2026-05-31'. receiptsMonthRevenueMap_() теперь донабирает из неё все месяцы старше того, что уже есть в История_месяцев (сейчас июнь-август) - для сравнения "Поступления vs Выручка" на вкладке "Поступления" глубже назад по году. НЕ пишет в саму История_месяцев (там нет остальных полей - ВП/затраты/план, строка выглядела бы обманчиво на "Глобальной статистике"). Проверено логической симуляцией слияния - уже известные месяцы не перезаписываются, историческая часть строго ограничена cutoff. Известная методологическая разница: offline-выгрузка клиентской истории исключает внутренние перевозки, живая "Выручка" дашборда - нет (могут быть небольшие расхождения на стыке методик, не ошибка). |
| 171 | 2026-08-13 | Влад посмотрел скриншот вкладки "Поступления" и попросил ряд правок: 1) "внутренних и не должно быть, мы всё равно не получаем по ним поступления... сравнение должно быть поступления / коммерческие заказы" - aggregateOrdersRows() получил summary.total_commercial/by_day_commercial (выручка БЕЗ внутригрупповых), История_месяцев - новая колонка "Выручка коммерческая" (в конец), используется вместо общей "Выручки" для сравнения на "Поступлениях"; заодно историческая часть (client-history агрегат, тоже без внутренних) стала методологически согласована с живой. 2) "наличку показывает ноль хотя в июле/июне было" - НЕ баг, нужен повторный ручной запуск backfillMonthSummaries() (новые колонки "Наличные"/"Выручка коммерческая" у старых строк июня/июля пустые, пока не пересчитаны). 3) "столбики вместо линии не нравятся, наличка контрастным цветом внутри безнала, Поступления зелёным" - цвета графика: безнал=зелёный (#1D9E75), нал=золотой (#EF9F27, стек внутри той же полосы), Выручка=синий, отдельным столбиком рядом. 4) "два этажа городить не нужно" - "Сегодня"+"Среднее в день" объединены в одну KPI-карточку (снова 4 в ряд), "По менеджерам"+"По клиентам" объединены в один блок с переключением вкладками (как в Заказах/Клиентах, tab-btn). 5) "сальдо по году, также сальдо по месяцу добавь" - summary.month_balance в getReceiptsData(), отображается в той же карточке, что и годовое сальдо. 6) "там где таблица по клиентам пусть будет столбик по нашим юрлицам куда поступили деньги" - новый горизонтальный bar-чарт по юрлицам на вкладке "По клиентам" (лениво рендерится при открытии вкладки). НУЖНО ОТ ВЛАДА: заново запустить backfillMonthSummaries() в редакторе Apps Script, чтобы наличка/коммерческая выручка подтянулись в уже существующие строки июня/июля. |
| 172 | 2026-08-13 | Влад посмотрел скрин с историей по месяцам: "не красиво, столбики наличка и выручка должны быть рядом" - сложенный столбик (безнал+нал) прятал наличку невидимой полоской (кратно меньше безнала). Убран stack - теперь три раздельных столбика рядом (безнал/нал/выручка). "Наличку визуально чуть гиперболизируй чтобы видно было" - если реальное значение >0, но меньше 5% от максимума на графике, столбик рисуется минимальной видимой высотой (dataset.realData хранит настоящее число для подсказки - визуальный акцент, не искажение данных). Второе: "это [график по юрлицам на вкладке Клиенты] не нужно, нужно просто колонка в таблице с нашим КА куда упали деньги" - убран отдельный график по юрлицам (не понравился), вместо него - колонка "Юрлицо" прямо в таблице "По клиентам" (byCustomer.orgs, через запятую, если клиент платил в несколько наших юрлиц). |
| 174 | 2026-08-13 | Два дела разом. 1) Влад: "осталось наличку с января по май найти и подгрузить, можем подтянуть из мега-базы?" - client_history_normalize.js (отдельный Apps Script проект "МегаБаза", scripts/client_history_normalize.js в этом репо = Код.js там) теперь опционально извлекает "Оплата нал" (если колонка есть в сыром листе "Лист_1" - НЕ гарантировано, лог явно покажет найдена/не найдена), добавлена колонка "Наличные" в Нормализованные_история_заказов и поле "c" в дневной JSON История_клиентов_агрегат (включая недельный fallback для переполненных клиентов). full_script_final.js: receiptsMonthRevenueMap_() читает daily[date].c вместо жёсткого cash:0 для месяцев до CLIENT_HISTORY_CUTOFF. Ждёт от Влада: normalizeClientHistory() + buildClientHistoryAggregate() в редакторе "мега база". 2) КООРДИНАЦИЯ: при подготовке к пушу обнаружено, что другая сессия задеплоила ПРЯМО В РЕДАКТОР фичу кэширования главного payload (buildHeavyMainPayload_/saveMainPayloadCache_/readMainPayloadCache_/getMainPayloadCacheOrLive_, лист "Кэш_главной_страницы", вызов в runAll()) - в git её не было вообще (ни кода, ни плана). Перенесено в репозиторий отдельным sync-коммитом (f53745d) с полным набором полей текущей ветки (Юрлицо в byCustomer + историческая наличка), затем задеплоено вместе. Также подтянут f74a82c с main (страница логиста - единый каркас с менеджером + ИИ-задачи) через git merge перед пушем. |
| 175 | 2026-08-13 | СТРАНИЦА ЛОГИСТА - единый каркас с менеджером (Влад: "структурно каркас должен быть единообразным") + ИИ-задачи для логистов (GPT-5, новый промпт - маржа найма/поставщики/документы, БЕЗ кросс-ссылки с ДЗ - долг клиента привязан к его менеджеру по продажам, не к логисту). Полоска "Маржа найма к премии >1М" вместо плана продаж (у логистов плана нет), расчёт зарплаты в стиле менеджера ("Итого на сегодня"+"Итого по прогнозу"), 3 отдельных блока-таблицы объединены в один card с вкладками. Общий каркас (planProgressBar/salRow/pctBadge_/scaleRowForForecast_/renderAiTasksBlockHtml_ и т.п.) вынесен на module scope - переиспользуется менеджером и логистом. generateAiTasksCached_ обобщён под обе роли. См. plans/2026-08-13-logist-page-unify-with-manager.md. ПЛЮС: при координации перед деплоем обнаружена и синхронизирована параллельная незакоммiченная работа (precompute-кэш главной страницы, Кэш_главной_страницы/buildHeavyMainPayload_/saveMainPayloadCache_) - была задеплоена мимо git с базы ДО колонки "Юрлицо" (67d7dfc), сделан аккуратный merge вместо overwrite, обе фичи сохранены. |
| 176 | 2026-08-13 | Влад посмотрел живые данные (после первого реального импорта): 1) "где 731 (наличка), должна быть сумма наличка+безнал одной цифрой, где нет налички - просто безнал" - подпись над столбиком графика теперь combined (bankValues[i]+cashValues[i]), не только наличка сама по себе. 2) "если создавал листов по каждому месяцу, сделай компактно на одном" - архив "Поступления_YYYY-MM" (по листу на месяц) заменён на ОДИН лист "Поступления_архив", writeReceiptsArchiveMonth_() заменяет данные конкретного месяца внутри общего листа. migrateReceiptsArchiveToSingleSheet() - разовая миграция старых листов (запустить вручную один раз, удаляет их после переноса). 3) "сделай третью вкладку Поступления сегодня, разбивку хочу видеть" + "ещё вкладку Поступлениям вчера" - getReceiptsData().today/yesterday, построчный список поступлений за день (клиент/менеджер/юрлицо/документ/сумма), два новых таба в объединённом блоке. ВАЖНО ДЛЯ ВЛАДА: 1) главный payload теперь кэшируется (см. v175) - эти изменения появятся на сайте только после runAll() (планового или ручного); 2) до запуска migrateReceiptsArchiveToSingleSheet() старые месяцы (июнь-июль и т.д.) пропадут из графика "по месяцам" - данные никуда не делись, просто в старых листах, миграция обязательна. |
| 177 | 2026-08-13 | Скриншот Влада: "Прус-Роскошный задвоился" в дропдауне личной страницы (был и обычной строкой, и "· логист" - часть его сделок в 1С помечена в поле "Менеджер по продажам" = он же, артефакт данных, хотя реальная роль - логист). Фикс: `dedupeMyPageEntries_()` - при коллизии имени в ростере дропдауна побеждает запись типа logist (реальная роль по фиксированному списку). Применено в обоих местах сборки `entries` (myPageCurrentEntry_/renderMyPage). Фронтенд-only, без бэкенд-деплоя. |
| 178 | 2026-08-13 | ТРИ РОЛИ ЛОГИСТОВ (Влад: "нужно логистов разделить на три роли - длинномеры/Васин уже отдельно, наёмная техника/Прус+Суркова - упор от маржи найма, свой парк тралов/Сильчев+Кан+Махура - упор от своего парка"). Формула ЗП (calcLogist) НЕ меняется ни для кого - меняется только показатель на полоске личной страницы. Own-tral логисты (Сильчев/Кан/Махура) получили полоску "ВП своего парка тралов" к прошлому месяцу (нет фиксированного плана в данных - Влад выбрал сравнение с прошлым месяцем через AskUserQuestion). Backend: `logistMap.own_profit_tral`/`own_profit_long` (та же логика, что `managerMap.own_profit`), `isOwnTralLogistName_`/`prevMonthKeyOf_`/`getOwnTralPrevMonthProfit_`, `buildLogistView_` прикладывает `own_profit_tral_prev_month` self-login логисту "бесплатно", новый `doGet action=own_tral_prev_month` - lazy-fetch для admin-предпросмотра. Frontend: `isOwnTralLogistSur_` (зеркало бэкенда), `planProgressBar` получил опциональный `targetLabel` ("План" по умолчанию, "Прошлый месяц"/"Прошлый месяц (загрузка…)" для own-tral, старые вызовы не тронуты), `ownTralPrevCache`/`loadOwnTralPrevIfNeeded_` (тот же lazy-паттерн, что `mgrLostCache`), `renderMyPageLogist` разветвлён по флейвору - каркас (KPI/зарплата/вкладки) общий, полоска разная. Проверено синтетикой в Browser preview: дедуп, own-tral admin-предпросмотр (состояния загрузка/отставание/превзошли/ошибка), own-tral self-login (без лишнего round-trip), регрессия наёмной техники (Прус) и менеджера - без изменений. См. plans/2026-08-13-logist-three-roles-split.md. ПЛЮС координация: `clasp pull` перед пушем обнаружил, что живой редактор уже содержал строку 176 выше (единый архивный лист "Поступления_архив" + вкладки "Поступления сегодня/вчера") - на момент проверки эта работа ещё не была подтянута в текущую ветку из git, перенесена отдельным sync-коммитом (af2cf4e, содержимое байт-в-байт совпало с уже существовавшим коммитом df74fed на main), затем задеплоена вместе со своими изменениями версией 177 (Apps Script). |
| 179 | 2026-08-13 | Влад показал скриншот: блок "Задачи на сегодня (ИИ)" у Сурковой (наёмная техника) выдавал чисто менеджерские задачи (реактивация "пропавших клиентов", category "выручка") - логистике вообще не по теме. КОРЕНЬ (не в промпте - он уже был написан нормально): кэш `ИИ_Задачи_Менеджеров` был ключован только на "Дата"+"Сотрудник", БЕЗ роли - у людей с двойной ролью (Суркова - менеджер И логист найма, исторически то же было у Пруса) первая за день генерация "побеждала" на весь день независимо от того, какая роль реально запросила задачи. Фикс: колонка "Роль" добавлена В КОНЕЦ листа (не вставлена - старые строки не сдвигаются, апгрейд листа "на месте" без миграции), `findAiTasksCacheRow_`/`saveAiTasksCache_`/`deleteAiTasksCacheRow_`/`generateAiTasksCached_` теперь принимают и фильтруют по роли - старые строки без роли просто не совпадают ни с одним новым запросом и тихо перегенерируются один раз (самоисцеление). ВТОРОЙ баг попутно найден и исправлен: `parseAiTasksResponse_` (общий парсер ответа ИИ) держал список валидных `category` только из менеджерского промпта (`выручка/дебиторка/документы`) - категория `поставщики` из логистского промпта молча стиралась в пустую строку, бейдж не показывался. Добавлена в общий список. ТРЕТЬЕ (по запросу Влада - контроль общей маржи, "если провалимся ниже 23%, никто не получит премии"): `buildLogistAiContext_` теперь передаёт модели `overall_margin_pct` (общая маржинальность найма этого логиста, раньше видела только прогресс к порогу 1М в рублях, не сам % маржи) - промпт требует ОБЯЗАТЕЛЬНУЮ задачу №1 про восстановление маржи, если `overall_margin_pct` ниже 23% или близко (23-27%), со ссылкой на конкретных поставщиков с margin_pct<23%. Проверено изолированными Node-тестами (мок Google Sheets): роль-изоляция кэша (менеджерская генерация не поражает логистскую и наоборот, force-удаление одной роли не трогает другую, легаси-строки без роли безопасно промахиваются), категория "поставщики" переживает парсинг. НЕ добавлена "прогрессия по ступеням оклада" (4-й пункт из просьбы Влада) - у логистов, в отличие от менеджеров, оклад ФИКСИРОВАННЫЙ (20 000 ₽, без ступеней в `calcLogist`) - выдумывать несуществующую механику в промпте не стали, ближайший реальный рычаг (премия 30 000 ₽ за маржу >1М) уже был в промпте и оставлен как есть. |
| 180 | 2026-08-13 | Влад построчно разобрал первый живой прогон промпта логиста наёмной техники (Суркова) - 4 конкретные правки. 1) "Дожми путевой и реализацию... документы" - логист НЕ закрывает документы (проведение/реализация делает бухгалтер) и НЕ обращается в бухгалтерию напрямую - промпт теперь требует: по not_posted/no_realiz логист звонит ОТВЕТСТВЕННОМУ МЕНЕДЖЕРУ клиента, просит поторопить бухгалтерию (не бухгалтерию напрямую). no_waybill остался зоной логиста напрямую с поставщиком (без изменений). 2) "У Сурковой сделки со 100% маржой - значит не внесена стоимость поставщика в 1С" - новое поле `cost_missing_deals` в `buildLogistAiContext_` (сканирует ВСЕ сделки месяца на cost<=0 при amount>0, не только последние 8), с конкретным заказчиком - промпт делает это ПРИОРИТЕТОМ №1 (выше даже общей маржи 23%, которую пропуск стоимости и так искажает). 3) "Срежь рейсы у АСВ-ГРУПП, замени на ВГС СПЕЦТЕХСТРОЙ" - некорректно: у поставщиков разная специализация по технике (не у всех есть и тралы, и длинномеры) - промпт больше не предлагает КОНКРЕТНУЮ замену поставщика, только пересмотр ставки с слабым или "поискать альтернативу с тем же типом техники" без имени. 4) "Возьми повторный рейс у СК ФЕНСМА через Рамазанова" - логист не ведёт клиентов (это менеджеры) - промпт переформулирован: логист звонит МЕНЕДЖЕРУ, напоминает про удачную связку заказчик+поставщик, предлагает менеджеру взять ещё заказ, логист снова поставит наём. Проверено изолированным Node-тестом на синтетике по мотивам реального скриншота (cost_missing_deals корректно ловит ЗИМАЛЕТТО/ОНТАЙМ с cost=0 и называет заказчика НГБР-1500/ТСК-3). |
| 181 (реальный) | 2026-08-14 | Влад загрузил лист "Январь_Май" в таблицу "мега база" в формате живого отчёта заказов (том же, что июнь-июль-август) - вместо продолжения возни с client_history_normalize.js (отдельный хрупкий пайплайн - за ночь нашли и починили несколько багов, а выгрузка всё равно оказалась устаревшей на ~9% против свежего 1С) новая `importHistoricalOrdersFromMegaBase()` раскладывает этот лист как ОБЫЧНЫЕ архивы Заказы_2026-01..05 в основной таблице дашборда - тем же кодом (parseOrdersRawRows/splitOrdersRawByMonth/writeArchiveSheet), что уже проверенно работает для июня-июля, включая уже исправленные сегодня TRAL_MANAGERS/INTERNAL_CLIENTS (5 бывших сотрудников, убранное "БАЗА"). Дальше - `backfillMonthSummaries()` пересчитает "История_месяцев" для этих месяцев тем же кодом, что для любого другого месяца - без отдельной логики "исторических" месяцев вообще. Ожидает проверки Влада (формат листа визуально похож на нужный - "Адрес объекта"/"Баланс по организации"/"Вариант расчета" совпадают с ожидаемыми именами колонок, в отличие от старого "Лист_1", но колонки в файле отсортированы по алфавиту не по порядку - неважно, код ищет по имени). |
| 182 (реальный) | 2026-08-14 | Первый запуск importHistoricalOrdersFromMegaBase() упал ("Service Spreadsheets timed out" - транзиентный сбой Google) внутри writeArchiveSheet, но формат листа "Январь_Май" распознался верно (Влад подтвердил: та же стандартная выгрузка 1С, что и всегда, тот же отчёт, что за июль/август). Каждый месяц теперь в своём try/catch - падение одного не обрывает остальные, повторный запуск безопасно доделывает недостающее. |
| 183 | 2026-08-14 | Партичный повторный запуск (после 182) показал: январь-март записались, апрель-июль упали тем же таймаутом - и тут выяснилось, что лист "Январь_Май" неожиданно содержит ещё и июнь-июль (стандартный период выгрузки 1С "с начала до сегодня", а не строго названные в листе месяцы). Это опасно: writeArchiveSheet СЛИВАЕТ данные с уже существующим архивом - если бы запись июня/июля всё же прошла, она откатила бы уже живые актуальные архивы дашборда к более старому снимку на момент этой выгрузки. Фикс: граница месяцев теперь жёстко захардкожена `IMPORT_MIN_MONTH_='2026-01'..IMPORT_MAX_MONTH_='2026-05'` (было - динамическая проверка "текущий/будущий месяц") - функция физически не может записать ничего вне января-мая, сколько раз её ни запускай. |
| 184 | 2026-08-15 | Все 5 месяцев (январь-май) успешно доимпортированы (retry дозаписал апрель-май после таймаутов), backfillMonthSummaries() пересчитал "История_месяцев" для всех 8 месяцев. Влад сверил "Глобальную статистику" со скриншотами живого 1С "План-фактный анализ продаж" - за январь дашборд 17,702,180 vs 1С 19,475,363,33, слишком большая разница, спросил "17-е это без внутригрупповых или нет?". По коду: computeSalesFaktPlan_() (formируeт "revenue" в "Глобальной статистике") строит сумму НЕ из summary.total_amount (честный итог по всем строкам месяца), а реконструирует её из by_manager (только TRAL_MANAGERS) + довесок по internal_amount - гипотеза, что часть суммы теряется в этой реконструкции для свежих январь-майских архивов, хотя для июня совпадает ТОЧНО. Добавлен разовый diagnoseSalesFaktGap_2026_08_15() - печатает все промежуточные числа по каждому месяцу, чтобы найти причину точно, не гадать. Ждёт запуска Владом. |
| 185 | 2026-08-15 | Первый диагностический прогон показал "суммы чистых логистов" (по колонке mgr_l) везде пустыми {} - гипотеза про logistMap неверна. Добавлен второй разовый diagnoseOrphanCommercialRows_2026_08_15() - проверяет напрямую по колонке mgr_s (та же, что строит managerMap): строки НЕ внутренние И mgr_s вне TRAL_MANAGERS. |
| 186 | 2026-08-15 | НАЙДЕНА И ИСПРАВЛЕНА настоящая причина разрыва "Глобальной статистики" с 1С. diagnoseOrphanCommercialRows_2026_08_15 точно назвал виновников по месяцам: январь -836000 (Каспарова+Фидан), февраль -1329500 (Каспарова+Васёв), апрель -864000 (Горбачев), июль -42250/август -100800 (Васин) - коммерческие (не внутренние) заказы с "Менеджером по продажам" = логист/бывший сотрудник вне TRAL_MANAGERS молча выпадали из computeSalesFaktPlan_().salesFakt. Март/май/июнь совпадали ТОЧНО - там просто не было заказов у таких людей, что и подтвердило причину. Фикс: computeMonthSummary_ теперь берёт os.total_amount (честная безусловная сумма ВСЕХ строк месяца) вместо sfp.salesFakt для поля revenue в "История_месяцев"/"Глобальной статистике". sfp.salesFakt НЕ тронут - используется отдельно для %"Выполнения плана продаж" на Панели/По менеджерам, там намеренно ограничен теми же именами, что и salesPlan (activePlanKeys) - другая семантика. Оба разовых диагностических скрипта удалены после использования (правило CLAUDE.md). ВАЖНО ДЛЯ ВЛАДА: нужно заново запустить backfillMonthSummaries(), чтобы пересчитать уже записанные строки "История_месяцев" новой формулой (иначе фикс виден только в коде, старые строки в таблице останутся со старыми числами). |
| 187 | 2026-08-15 | Влад сверил januarь после фикса v186 (18 538 180) со скриншотом 1С (19 475 363,33) - разница ещё осталась ("нужно дальше копать"). Найдена ВТОРАЯ, более глубокая причина: parseOrdersRawRows() имеет жёсткий фильтр isTralDept - строка НЕ ПОПАДАЕТ В АРХИВ ВООБЩЕ (не просто теряет выручку), если ни "Менеджер по продажам" не в TRAL_MANAGERS, ни "Менеджер по снабжению" не в TRAL_LOGISTS. 5 бывших сотрудников (Васёв/Каспарова/Фидан/Горбачев/Свешников) уже были добавлены в эти списки в client_history_normalize.js (мега-база) ещё 2026-08-14, но НЕ в full_script_final.js - тогдашний комментарий объяснял это тем, что "мега-база отдельный исторический источник, тут работает текущий состав". Это устарело с v181 (importHistoricalOrdersFromMegaBase переиспользует ИМЕННО этот файл для январь-майского импорта). Перенесено: Васёв/Каспарова/Фидан → TRAL_MANAGERS, Горбачев/Свешников → TRAL_LOGISTS. Безопасно для текущих месяцев (бывшие сотрудники, в живых данных не встречаются). ВАЖНО ДЛЯ ВЛАДА: нужно ЗАНОВО запустить importHistoricalOrdersFromMegaBase() (доберёт ранее отфильтрованные строки этих 5 людей за январь-май, безопасно перезапускать - writeArchiveSheet сливает по номеру заказа), затем backfillMonthSummaries() ещё раз. |
| 188-191 | 2026-08-15 | Январь после фикса v187 сблизился с 1С почти вплотную (368к из 19.48М, 1.9% - остаток похож на естественный дрейф корректировок 1С). Май остался с большим разрывом (9.5%). Диагностика по шагам: diagnoseMayGap (сырая сумма мая из "Январь_Май" 44.6М - близко к 1С 45.56М, значит проблема не в разбивке по месяцам) → часть 2 (нашла 98 строк/3.37М отсеянных isTralDept ЦЕЛИКОМ - но 91.6% из них оказались людьми из явного списка "Исключить из аналитики тралов" CLAUDE.md - буровые/краны, НЕ баг) → Влад подтвердил "не наши" по оставшимся 3 именам (Котляренко/Спорышев/Грачева) → Влад прислал ручную выгрузку мая напрямую из 1С (МАЙ.xlsx, 951 заказ, 45 514 157,70 - почти идеально сходится с живым 1С) и попросил сверить пономерно → dumpMayArchiveOrderNumbers (куски по 60, чтобы не обрезалось логом) → локальное сравнение (Python, вне Apps Script) нашло 133 заказа (4 264 041,67) из файла Влада, отсутствующих в архиве - ВСЕ на уже известных сотрудниках отдела, не проблема фильтрации, просто их не было в старой выгрузке "Январь_Май" мега-базы (1С дополнила май новыми документами задним числом после 14.08). |
| 192 | 2026-08-15 | importMayManualFix_2026_08_15() - Влад загрузил свежий майский файл в отдельную таблицу "Копия ДАШБОРД 2.0" (1jCPRXYDFcTpZIHdJfngZveOQFycu6qbcl-MoXBxtBRM), лист "МАЙ_ТЕСТ". Функция сливает его с "Заказы_2026-05" через writeArchiveSheet (дедуп по номеру заказа, безопасно перезапускать). ВАЖНО ДЛЯ ВЛАДА: запустить эту функцию, затем backfillMonthSummaries() - май должен подняться примерно до ~45.5М. |
| 193 | 2026-08-15 | Фикс importMayManualFix_2026_08_15: вставка листа МАЙ_ТЕСТ растянула getDataRange() до 105 колонок вместо 46 (форматирование/границы) - writeArchiveSheet падал при слиянии с архивом. Обрезка по реальной ширине заголовков. Первый (упавший) запуск успел archive.clear() ДО ошибки - архив мая оказался пуст, второй успешный запуск не слился, а записал начисто 951 заказ (45 514 157,70 ₽) - ничего не потеряно, все 818 старых номеров уже были подмножеством новых 951 (проверено сравнением). |
| 194 | 2026-08-15 | Влад отверг гипотезу "1С меняет закрытую базу задним числом" ("бухгалтера не меняют без служебки") - потребовал построчную проверку без предположений. diagnoseMissing133Rows_2026_08_15 проверил каждый из 133 номеров напрямую в листе "Январь_Май" мега-базы: 0 из 133 найдены - сама эта выгрузка физически неполная (не проблема "задним числом", а неполный экспорт как таковой). |
| 195 | 2026-08-15 | Влад вручную нашёл один из "пропавших" номеров (465409) в ДРУГОМ, более старом листе мега-базы - "Нормализованные_история_заказов" (построен из "Лист_1" ещё до перехода на "Январь_Май"). diagnoseJanAprMissingViaOldHistory_2026_08_15 - проверка той же неполноты для января-апреля через сравнение с этим старым листом (не успели прогнать - Влад параллельно сделал новую полную выгрузку). |
| 196 | 2026-08-15 | Влад сделал НОВУЮ, более полную выгрузку января-мая прямо из 1С - лист "ЯНВАРЬ_МАЙ_НОВЫЙ" в той же таблице "мега база" ("давай всё по нему проверяем"). importHistoricalOrdersFromMegaBase() переключена на этот лист (тот же формат, с обрезкой лишних колонок как в importMayManualFix). Оба промежуточных разовых скрипта (importMayManualFix_2026_08_15, diagnoseJanAprMissingViaOldHistory_2026_08_15) удалены - их подход (латать/сверять со старыми источниками) заменён полным переимпортом из нового полного листа. ВАЖНО ДЛЯ ВЛАДА: запустить importHistoricalOrdersFromMegaBase() (сольёт по номеру заказа со всеми существующими архивами января-мая, безопасно), затем backfillMonthSummaries(). |
| 197 | 2026-08-15 | importHistoricalOrdersFromMegaBase упала: "нет колонок Начало работ/Дата создания" на ЯНВАРЬ_МАЙ_НОВЫЙ. Сообщение об ошибке расширено - печатает реальные заголовки листа вместо гадания вслепую. |
| 198 | 2026-08-15 | Найдена точная причина: ручные выгрузки 1С называют колонку "Начало"/"Окончание" (без слова "работ"), не "Начало работ"/"Окончание работ" как ежедневный авто-импорт - другой вариант настроек экспорта, не ошибка Влада. Добавлен алиас на уровне карты колонок в splitOrdersRawByMonth И parseOrdersRawRows (раньше был только точечный фикс в первом) - чинит и уже загруженный "МАЙ_ТЕСТ" задним числом (дата "Начало работ" читалась пустой). Влад успешно перезапустил: 2026-01 (408 строк), 02 (494), 03 (668), 04 (1027), 05 (951) - "цифры стали очень похожи на действительность". |
| 199 | 2026-08-15 | Влад: "Васин - логист, такой же как Сильчев/Кан/Махура... его обороты это внутренние перевозки. Что опять придумано лишнее?" - отверг мой вывод, что 100 800 ₽ Васина за август коммерческие. Также потребовал: "все цифры по выручке должны быть везде одинаковые... не должно быть разницы во всём дашборде". Добавлена диагностика для проверки фактов по строкам Васина (не гипотез). |
| 200 | 2026-08-15 | Влад сам нашёл причину в 1С (та же красная подсветка, что у МЕГАКРАН/ОТДЕЛ БУРОВЫХ) - 2 строки Васина за август были на заказчика "Панасюк Роман Викторович" (100 800 ₽), подтвердил считать как внутренние перевозки. Добавлено в INTERNAL_CLIENTS в ОБОИХ файлах (основной дашборд + мега-база). Это автоматически чинит расхождение "Панель" (salesFakt) vs "Глобальная статистика" (total_amount) за август через уже существующий механизм довеска по internal_amount - без структурных правок. Разовая диагностика Васина удалена (задача решена). |
| 201 (не создана, лимит версий) | 2026-08-15 | **ИНЦИДЕНТ**: обнаружено, что отчёт 1С "Отчёт Таблица заказов" (авто-импорт дашборда) не включает заказы с типом техники "Машина прикрытия" - реальная платная услуга, найдено построчным сравнением с ручной выгрузкой (5 заказов/600 000 ₽ за август). Влад дополнительно выгрузил такие же заказы за июнь-июль (5 шт/123 000 ₽, "прикрывашки.xlsx"). Влад попросил взять загрузку файлов на себя - данные обоих локальных xlsx вшиты прямо в код функции importPrikrytieAndAugustFix_2026_08_15 (без загрузки в Google Таблицы). ПЕРВЫЙ запуск упал: "The data has 105 but the range has 46" - mergeRawOrderRows_ слепо конкатенировал строки разной ширины (живой импорт даёт ~105 колонок, ручная выгрузка ~46) - writeArchiveSheet делал archive.clear() ДО setValues(), поэтому "Заказы_2026-06" осталась пустой после сбоя. Версии Apps Script закончились (лимит 200) - редеплой /exec невозможен, но для этой функции он и не нужен (только ручной запуск в редакторе). Достигнут потолок clasp create-version - `clasp run-function` тоже не сработал (нужен API Executable деплой, не настроен). |
| 202 (не создана) | 2026-08-15 | Фикс mergeRawOrderRows_ (слияние по ИМЕНИ колонки, единый заголовок вместо слепой конкатенации разной ширины) задеплоен через clasp push (без версии - не нужна для ручного запуска). Повторный запуск importPrikrytieAndAugustFix_2026_08_15 - ЗАДЕЛ и "Заказы_2026-07" стала тоже почти пустой (точная причина второго сбоя не установлена - возможно, повторный запуск начался ДО применения фикса, или отдельная граничная ситуация). Влад: "Ты меня сегодня измотал... реши сам, верни как было" - решение принято и выполнено самостоятельно без дальнейших вопросов. |
| 203 (не создана) | 2026-08-15 | **ВОССТАНОВЛЕНИЕ**: написана recoverJuneJulyArchives_2026_08_15() - через Drive API v2 (revisions.list/exportLinks) находит последнюю ревизию файла ДО инцидента (граница 22:50 МСК), скачивает как временный xlsx, копирует "Заказы_2026-06"/"Заказы_2026-07" обратно. Защита: не трогает лист, если в найденной ревизии <500 строк. Запущено Владом - успешно восстановлено 1059 строк (июнь) + 1140 строк (июль) из ревизии 17:26:48 UTC. Параллельно исправлен корень проблемы в writeArchiveSheet - теперь ПИШЕТ новые данные ПЕРЕД очисткой старых (не наоборот), плюс try/catch на каждый месяц в importPrikrytieAndAugustFix (один сбойный месяц больше не обрывает остальные). Фикс проверен изолированно (Node.js, синтетические данные 105 vs 46 колонок) ДО повторного запуска на живых данных - прошёл без ошибок. Повторный (уже безопасный) запуск importPrikrytieAndAugustFix_2026_08_15 прошёл чисто: +3 (июнь), +2 (июль), +3 из 5 (август, 2 уже были подтянуты обычным импортом). backfillMonthSummaries() пересчитан для всех 8 месяцев. Оба разовых скрипта удалены после успешного использования. См. подробный разбор в retrospectives/2026-08-15. |
| 201 | 2026-08-16 | Пять правок страницы "Поступления" + кривая ДС на Глобальной статистике (Влад): 1) заголовок страницы (PAGE_TITLES отсутствовал); 2) блок "Поступления на этой неделе" с % к прошлой неделе (тот же день недели, честное сравнение); 3) итоговая строка (tfoot) в таблицах По менеджерам/По клиентам, по ВСЕМ клиентам, не только показанным; 4) клик по юрлицу в блоке "По юрлицам" фильтрует и сортирует "По клиентам" по сумме ИМЕННО в этом юрлице (byCustomer[].orgAmounts, не общая сумма клиента); 5) кривая "Поступления ДС" на Глобальной статистике, выключена по умолчанию (Chart.js hidden:true, включается кликом по легенде - встроенное поведение). Потолок версий Apps Script (200/200) снят - Влад удалил ~150 старых неиспользуемых версий через веб-интерфейс (bulk delete, недоступно через clasp/API, но есть в UI - официальная документация подтвердила, моя более ранняя оценка "версии нельзя удалить" была неверна).
| 202 | 2026-08-16 | Вкладка "Поступления" на личной странице менеджера (Влад: "менеджером по продажам добавь вкладку Поступление, внутри выбор сегодня/вчера/неделя/месяц, все поступления должны относиться к конкретному менеджеру"). buildManagerView_ отдаёт result.receipts (today/yesterday/week/this_month), приватно отфильтровано по фамилии менеджера СЕРВЕРНО (та же приватность, что ДЗ/problem_orders) - не только у админ-предпросмотра, но и у настоящего логина менеджера. getReceiptsData() получил новое поле this_month (построчная разбивка за весь текущий месяц). |
| 203 | 2026-08-16 | Вкладка "План задания" на дашборде (вариант А, только чтение) - интеграция с отдельной таблицей планировки (`Планировка_заказов*`, вне этого репозитория). `CONFIG.ORDER_PLAN_INDEX_ID` указывает на новую служебную таблицу-указатель "План задания - Индекс" (одна строка month/year/id/url, перезаписывается `duplicateForNextMonth()` при создании копии на новый месяц - правка внесена в `Планировка_заказов_Август_2026/planirovka_zakazov_script.js`, текущий активный проект). `getOrderPlanView_()` читает заказы конкретного человека (менеджер-продавец и/или логист-диспетчер) прямо из таблицы планировки, сопоставляя имена с учётом разницы форматов (лист "Доступ" хранит ФИО, колонка B планировки - "Фамилия Имя", плюс у Прус-Роскошного фамилия в планировке короче через дефис) - сверено на реальных данных диагностической функцией, запущенной один раз в таблице планировки и затем удалённой. Новая ветка `?action=order_plan` в `doGet`. Таблица планировки остаётся основным рабочим инструментом - дашборд только читает, ничего не пишет.<br>**Отдельная находка при деплое:** worktree-ветка, в которой делалась эта фича, оказалась создана от точки на 320 коммитов позади `origin/main` (значит `scripts/full_script_final.js`/`files/index.html` в её рабочей копии были сильно устаревшими - без "Поступления", клиентской аналитики и т.д., хотя `origin/main` и живой Apps Script были синхронны между собой). Исправлено сбросом ветки на `origin/main` и переносом правок заново на актуальную базу - в этот git-репозиторий ничего не потеряно, но стоит иметь в виду при создании новых worktree-веток из старых точек: перед началом работы стоит проверять `git rev-list --left-right --count HEAD...origin/main`.
| 204 | 2026-08-16 | **Фикс производительности "План задания"**: живой тест (`testOrderPlanView`, три реальных человека подряд) показал ~2.5 минуты на человека - последовательное чтение ~31 дневной вкладки таблицы планировки через `getDataRange().getValues()` (Sheets API медленный на много мелких вызовов подряд), неприемлемо для живой вкладки дашборда. Добавлен `readOrderPlanDayTab_()` - кэширует РАЗОБРАННЫЕ (не сырые) строки каждой дневной вкладки в `CacheService` на 5 минут, ключ включает id таблицы планировки и номер дня. Дорогим остаётся только первый запрос ЛЮБОГО человека после истечения кэша - все следующие (другой менеджер, тот же самый) в пределах 5 минут читают уже разобранные дни из кэша, без обращения к Sheets API. Отдельная находка того же теста: в файле `1jCPRXYDFcTpZIHdJfngZveOQFycu6qbcl-MoXBxtBRM` теперь есть роль `logist` (не только `manager`/`admin`) - `getOrderPlanView_` уже совместим (работает через общий `access.name`), правка не потребовалась.
| 205 | 2026-08-16 | **Фоновый прогрев кэша "План задания"** (Влад: "система должна закладывать мгновенный отклик как фундамент") - `warmOrderPlanCache()` добавлена в `runAll()` (уже выполняется 6 раз в день каждые 3 часа), TTL кэша поднят с 5 минут до 4 часов (`ORDER_PLAN_CACHE_TTL_SEC`, с запасом перекрывает интервал между прогонами). Практический эффект: холодные (~2.5 мин) 2.5-минутные чтения теперь оплачивает плановый прогон `runAll()`, а не менеджер, открывший вкладку - обычный пользователь почти никогда не видит задержку. Данные "План задания" при этом обновляются с той же частотой, что и весь остальной дашборд (не хуже и не лучше остальных вкладок). Настоящий realtime (Google Sheets → дашборд без опроса, через onEdit-триггер) и защита от конкурентной записи (потребуется, когда появится ввод заявок из дашборда - вариант Б) обсуждены с Владом как отдельная, более крупная задача на будущее, не блокирующая вариант А. Заодно удалена одноразовая `testOrderPlanView()` (своё отработала).
| 206-207 | 2026-08-16/17 | **Полная карточка заказа + заказчик** (Влад: "заявки максимально визуально приближены к таблице", "уже все там должно быть - адреса, телефоны, это будущая форма подачи заявок"). Карта колонок таблицы планировки подтверждена НЕ по одному примеру значений, а напрямую по формуле ячейки N3 ("ЗАЯВКА ДЛЯ ВОДИТЕЛЯ": `="*"&A3&"*  *Заказ на:* "&C3&" "&C4&"...` и т.д.) - две одноразовые диагностические функции (`dumpOrderPlanColumns`, `dumpOrderPlanFormula`) запущены в таблице планировки и удалены. `readOrderPlanDayTab_` теперь отдаёт заказчика (G3, "От кого грузимся"), груз/габарит (F3/F4), адреса и контакты погрузки/выгрузки (H/I), условия переработки/примечание (J3/J4), статус оплаты (K4), наш/наёмный транспорт (L/M), отметки логиста (P/Q/R), имена менеджера/логиста. Кэш переведён на новый ключ (`order_plan_day_v2_`) из-за смены формы данных. `getOrderPlanView_` сортирует заказы внутри дня по заказчику (стабильно) - заказы одного заказчика идут подряд, даже если в самой таблице разбросаны. Фронтенд: колонка "Роль" убрана (на её месте "Статус"), на месте бывшего "Статус" - "Заказчик"; клик по строке открывает карточку заказа (переиспользует `#vcard-panel`, как карточка машины/контрагента) со всеми полями; вкладки дней растянуты на всю ширину блока (`flex:1`), остаются в один ряд.
| 208 | 2026-08-17 | **Фикс: заказчик пропадал у части заказов + сырые true/false**. Влад прислал скриншот живого листа с видимыми буквами колонок - карта из v206/207 ошибочно брала заказчика из G3 ("От кого грузимся" по формуле N3) вместо D3 (человеческий заголовок строки 1 - буквально "Заказчик"), из-за чего у заказов без заполненного G3 (Цегельников) заказчик не показывался. Причина ошибки - диагностика 2026-08-16 читала только строку 2 (функциональные подзаголовки), не строку 1 (человеческие заголовки), где и написано явно "Заказчик" под D. Исправлено на D3 (заказчик) + D4 (контакт). Отдельно: P/Q/R - реальные чекбоксы (booleans) в Sheets, а не текст - старый хелпер `s()` с `value||''` молча превращал `false` в пустую строку и `true` - в текст "true" (Влад: "не true или false, а подтверждено/не подтверждено, зелёным/красным"). Переписано на строгое `=== true`, поля переименованы по смыслу (invoiceIssued/moneyReceived/requestSentToDriver/driverConfirmed/orderEnteredIn1C/logistClosed). Кэш - новый ключ `order_plan_day_v3_`. Фронтенд: новая колонка "Водитель" в таблице (после "Статус", как просил Влад) и цветные бейджи (зелёный/красный) везде, где раньше был текст true/false. Заодно переименована сама вкладка дашборда с "План задания" на "План задание" - по прямой просьбе Влада, чтобы совпадало с названием исходной таблицы ("🌼 План задание АВГУСТ 2026 🎾").
| 209 | 2026-08-17 | **Admin-предпросмотр "План задание" по любому менеджеру** (Влад: "нужно, чтобы сюда мог заходить Цегельников... и чтобы я под ним, и вообще под любым менеджером" - до этого он подменял свой личный email на "Цегельников" в листе "Доступ", чтобы вообще что-то увидеть, потому что admin не менеджер/логист в самой таблице планировки и `getOrderPlanView_(access.name)` для него всегда возвращал пусто). `doGet` для `action=order_plan`: manager/logist - всегда только себя (без изменений), admin - имя из `&manager=` (тот же паттерн, что уже давно работает для my-page/receipts - `mlcManager`/`gatManager`). Фронтенд: `orderPlanTargetKey_()`/`loadOrderPlanData()` берут выбор из ОБЩЕГО селектора "Личной страницы" (`myPageCurrentEntry_()`) - переключение того же дропдауна, что уже двигает my-page/receipts, теперь двигает и "План задание"; кэш инвалидируется при смене выбранного человека. Подпись "План задание — <имя> · переключить можно на «Личной странице»" - только для admin. Реальный вход конкретного менеджера своей почтой (например Цегельникова) - это просто добавление строки в лист "Доступ" (email/ФИО/manager), кода не требует - Влад делает сам.
| — (только фронтенд, без версии Apps Script) | 2026-08-17 | **Калькулятор стоимости перевозки как вкладка дашборда** - портирован из `ЯРД_Калькулятор_v1.3/yard-calc-v1.7.html` (карта+маршрут+расчёт+генерация КП), переоформлен под тёмную тему дашборда вместо навы/золота оригинала, логика расчёта не менялась. Доступен всем менеджерам/логистам и директору, история расчётов пока в `localStorage` (не Sheets). Методика описана в `.business/products/pricing.md` (был пустым шаблоном). Детали - `plans/2026-08-17-calculator-dashboard-integration.md`. Запушено `claude/calculator-dashboard-integration-ee9d8e` → `main` (fast-forward, `98a81e0..a14db9c`), задеплоено на GitHub Pages - проверено напрямую (`clc-map`/`data-page="calculator"` присутствуют в живом `files/index.html`, пункт меню открывается в реальном браузере без ошибок консоли).
| 228 | 2026-08-18 | **Калькулятор: авто-Faymonville + сохранение на сервер** (Влад прислал скриншот - при выборе буровой 110т категория Faymonville осталась 60-75 вместо 100-120, посчитало по заниженному тарифу; плюс "калькулятор должен сохранять на сервер расчёты именно менеджера кто сделал расчёт"). `faymonCatByMass()`/`syncFaymonCat()` на фронтенде подбирают категорию по весу при смене массы или типа техники (>120т - не трогает выбор, тост "согласуйте с руководителем", авторасчёта нет). Новый лист "Калькулятор_История" + `action=calc_save`/`action=calc_history` - автор строки берётся из `access.name` (проверенный id_token, лист "Доступ"), подделать нельзя; manager/logist видят только свои расчёты, admin - все (последние 300). **Перед этим деплоем обнаружена чужая недокоммiченная работа в живом коде** (сессионные токены 48ч + live-источник заказов с api.yardhub.ru, задеплоены тем же днём в обход git другой параллельной сессией) - сначала перенесена в репозиторий отдельным sync-коммитом (`a675df4`), потом поверх - калькулятор (`6271f49`). Редеплой живого `/exec` проверен напрямую curl'ом (`needLogin` без токена, не серверная ошибка).
| — (только фронтенд, без версии Apps Script) | 2026-08-18 | **КП переделан на фирменный бланк** (Влад: "форма отвратительная... на бланке фирмы как в приказах, аккуратное содержательное КП"). `genKP()` полностью переверстан: та же шапка (наименование/ОГРН/ИНН/КПП/адрес, разделительная черта), что и в `Приказы/*.md` этой же компании, Times New Roman, без золотых рамок/бейджей/эмодзи прежней версии. Строка подписи показывает реального менеджера (`D.managerName`/`D.logistName`), если известен. Логика расчёта КП не менялась.
| — (только фронтенд, без версии Apps Script) | 2026-08-17 | **Мобильное меню + доработки "План задание" по одному скриншоту с телефона**. Влад: "открываю с телефона, вижу страничку, но не могу переключаться по меню" - `.sidebar` на `@media(max-width:900px)` был полностью скрыт (`translateX(-100%)`) без единой кнопки, чтобы вернуть его - добавлена фиксированная кнопка-гамбургер (вне `.page-header`, у "Личной страницы" он скрыт целиком - иначе кнопка пропадала бы вместе с ним) + затемнение с закрытием по тапу + автозакрытие при выборе страницы. Для "План задание": (1) в колонке "Машина/водитель" теперь только Фамилия Имя (было - вся строка с телефоном), рядом кнопка-копирование полного текста с иконкой-подтверждением (галочка на 1.4 сек) - такая же кнопка добавлена в карточке заказа у "Назначенная машина"; (2) убраны 4 неиспользуемые отметки (Счёт выставлен/Деньги получены/Заявка внесена в 1С/Логист проставил отработку - у команды везде "Нет"), оставлены только "Заявка отправлена водителю"/"Подтверждение водителя"; (3) добавлена итоговая строка "Итого за день" под таблицей заказов - складывает только заказы с чистым числом в стоимости (`orderPlanCostNumber_`, вынесен из `orderPlanFormatCost_` общим хелпером), заказы с текстом вместо числа явно помечены как не вошедшие в сумму, а не додуманы.
| 210 | 2026-08-17 | Сессия на 1-2 суток вместо перелогина через Google каждый час (Влад: "постоянно раз в час требует перезахода... один раз залогинились - день-два никто не трогает тебя", план [plans/2026-08-17-session-token-persistent-login.md](../plans/2026-08-17-session-token-persistent-login.md)). Google `id_token` живёт ~1 час (срок жизни от Google, не наш) - после первой проверки `id_token` backend выдаёт свой подписанный токен (`issueSessionToken_`/`verifySessionToken_`, HMAC-SHA256, секрет `SESSION_SECRET` в Script Properties генерируется сам при первом обращении) на 48ч со скользящим окном - продлевается при каждой полной загрузке страницы. Права доступа по листу "Доступ" по-прежнему проверяются на каждый запрос, отзыв доступа работает мгновенно и с длинной сессией. Фронтенд (`files/index.html`): `apiUrl()` шлёт оба токена, `loadData()`/`apiFetchJson()` сохраняют свежий `session_token`, `initGoogleSignIn()` грузит данные сразу при наличии любого валидного токена. |
| 211-212 | 2026-08-17 | Начало миграции на свой сервер (VPS Beget, `api.yardhub.ru`) - первые два куска дашборда переведены с Google Таблиц на MySQL+Node.js: вкладка "Клиенты" (мегабаза, 112 065 строк, 2020-2026, вместо урезанного листа на 2781 строку - обнаружили и починили по дороге) и заказы за закрытый месяц 2026-06 (снимок `action=orders_period`, не пересчитываем бизнес-логику заново на сервере - берём готовый проверенный расчёт и просто раздаём быстрее). v211 - временный секретный снимок-экспорт для переноса июня (`export_period_snapshot`), v212 - снимок использован, экспорт убран обратно. Подробности архитектуры (Node.js/systemd/nginx/certbot, MySQL-схема, API-ключ вместо полноценной сессии - осознанное временное упрощение) - в `README.md` на самом сервере (`/root/yard-dashboard/README.md`), не в этом репозитории. Фронтенд: `files/index.html` — по дороге случайно затёр фичу "калькулятор" другой параллельной сессии одним пушем, сразу же восстановлено (см. историю коммитов `d6c93a0`→`0aace05` в git). |
| 213-217 | 2026-08-17..18 | Продолжение миграции на сервер (`api.yardhub.ru`) - "Поступления" (архив + текущий, отдельно от заказов) и "История_месяцев" перенесены в MySQL: `receipts_raw`/`receipts_archive` (2813 строк, янв-июль) наполняются постоянным потоком с 1С по FTP (`uploads/`, cron раз в час, минимальный шаг расписания в 1С), `month_summary` (8 месяцев, наличка+коммерческая выручка) - через временные секретные экспорты `export_receipts_archive`/`export_month_summary` (v213-217, использованы и убраны обратно, v216 - фикс: было хардкожено 15 колонок вместо `MONTH_SUMMARY_HEADERS.length`=16, обрезало "Выручка коммерческая"). Сайт пока продолжает читать эти данные из Google Таблиц - только перенос данных, без переключения фронтенда. |
| 218-222 | 2026-08-18 | Заказы за июль 2026 (закрытый месяц) перенесены на сервер тем же безопасным способом, что июнь - НЕ переизобретаем расчёт зарплат/планов/ВП, берём готовый JSON от уже существующего `action=orders_period` (вызывает `getGrossProfitForPeriod`/`getOrdersDataForPeriod`/`computeSalesFaktPlan_` напрямую) через временный секретный `export_period_snapshot` (v218, использован и убран, v219). Снимок сохранён в `period_snapshots` на сервере - фронтенд (`fetchOrdersPeriodFromServer_`) уже был написан универсально под любой период, никаких правок `files/index.html` не понадобилось, июль заработал с сервера сам. Порт "Поступлений" (`getReceiptsData()`) на сервер (`/api/receipts` в `server.js`) - сверен с живым расчётом Apps Script через временный секретный `export_receipts_check` (v221, убран v222) - совпало один в один (разница по августу ~30 тыс ₽/0.01% - ожидаемое отставание, `orders_normalized` обновляется раз в час, а не мгновенно). Фронтенд: `files/index.html` - после основной загрузки страницы в фоне подменяет `D.receipts` на свежий серверный расчёт (`fetchReceiptsFromServer_`), при любой ошибке тихо остаётся на данных из основного payload, ничего не ломается. Отдельно (сосед-проект МегаБаза_Apps_Script, вне этого репозитория): лист "ЯНВАРЬ_МАЙ_НОВЫЙ" залит в новую постоянную таблицу `orders_archive` (3545 строк) - выручка сверена и верна, но колонка "Прибыль" в этом сыром листе ненадёжна (маржа 78-88%, в отличие от 43-58% на уже посчитанных месяцах) - НЕ выводить как готовую цифру без подтверждения Влада. Август (текущий месяц) и январь-май пока не подключены к фронтенду "Заказы" - август ещё не закрыт (снимок рано снимать), январь-май требует либо доверенного источника прибыли, либо переноса полной зарплатной логики (сознательно отложено). |
| 223-226 | 2026-08-18 | **Текущий (открытый) месяц "Заказы" переключён на сервер** (Влад: "файлы приходят каждый час, сайт должен обращаться именно к ним, не с таблицы") - см. `plans/2026-08-18-live-orders-current-month-server-source.md`. Ключевое решение: НЕ переносим `aggregateOrdersRows`/`joinManagerPlans_`/зарплатную-плановую логику на сервер (риск), а меняем ТОЛЬКО источник сырых строк на входе - `getOrdersData(ss)` теперь сначала пробует `fetchOrdersRawFromServer_()` (новый эндпоинт `/api/orders_raw` в `server.js`, читает `orders_normalized`, обновляется по FTP раз в час), при любой ошибке/пустом ответе тихо падает обратно на `Заказы_данные.getRange().getValues()` - расчётный код не тронут ни строкой. Перед включением сверены (временные секретные `diag_orders_norm_sheet`/`diag_orders_compare`/`diag_set_yard_api_key`, v223-224 добавлены, v226 убраны): 1) построчно номера заказов Таблицы vs сервера за август - разошлись всего на 5 совсем свежих заказов (829 000 ₽, уже на сервере, ещё не долетели по email-каналу) и 4 копеечных в обратную сторону - сервер оказался ТОЧНЕЕ; 2) полный агрегат `getOrdersData()` до/после включения - та же структура ключей, то же число менеджеров/логистов (9), пороговые флаги вроде `hired_margin_qualifies` не изменились, суммы выросли (сервер поймал заказы, до которых Таблица ещё не дошла). API-ключ сервера положен в Script Properties (`YARD_API_KEY`) через временную ручку, не хардкожен в коде. |
| 228 | 2026-08-18 | (параллельная сессия, синхронизировано в git отдельным коммитом) История расчётов калькулятора - новый лист `Калькулятор_История`, `action=calc_save`/`calc_history`, автор строки - `access.name` (не то, что прислал клиент). |
| 227, 229-230 | 2026-08-18 | **Замер тайминга смены периода** (Влад: "поттормаживала переключение по месяцам... планы нужно перенести на сервер") - временный `diag_timing_period` (v229, убран v230) засёк реальные расходы времени на июль: `getStaffData` (лист "Штатка") 1065мс, `aggregateFinHistoryForRange` (лист "История_финансов") 4646мс, весь `getGrossProfitForPeriod` 9902мс, весь `getOrdersDataForPeriod` 16325мс (из них сам `fetchOrdersRawFromServer_` - только 604мс, остальное - `aggregateOrdersRows`/`computeLostCustomers_`, CPU внутри Apps Script), `getManagerPlans_` (лист "Планы_менеджеров") - всего 796мс. Вывод: гипотеза Влада не подтвердилась - "Планы_менеджеров" ничтожная доля (796мс из ~26 сек суммарно), реальные тяжеловесы - "Штатка"/"История_финансов" (~10 сек, самая денежная зона, трогать осторожно) и сама агрегация заказов в Apps Script (~15 сек, CPU, не связана с источником данных - переезд на сервер её не ускорит без переноса самой расчётной логики). Планы_менеджеров переносить не стали - не имеет смысла при таком соотношении. | (1) Влад: "поскольку в начале нового месяца есть изменения по старому, отчёт например в августе летит сразу с июлем" - недавно закрытый месяц у 1С ещё донасчитывается, "закрытый" не значит "навсегда застывший". `getOrdersDataForPeriod(ss, period)` теперь ТОЖЕ сначала пробует `fetchOrdersRawFromServer_(period)` (тот же `/api/orders_raw`, который сам решает - `orders_normalized` (живое окно, обычно текущий + недавно закрытый месяц) или `orders_archive` (январь-май, разовый перенос мегабазы) - самообновляется, пока 1С продолжает досылать период, без ручных переснимков; фолбэк на архивный лист Google Таблицы, если сервер за период ничего не отдал. Заодно снят с `period_snapshots` статус единственного источника для июля - он там всё ещё остался про запас (июнь), но фронтенд для июля теперь идёт через живой путь. (2) Влад: "Пропавшие клиенты" на личной странице менеджера долго грузятся - причина найдена: `readOrdersRowsForMonth_` (используется `computeLostCustomersForManager_`, 3 месяца на каждое открытие вкладки) читал Google Sheets напрямую, МИМО всего серверного переезда - 3 медленных запроса к Sheets API при каждом клике. Теперь тоже сначала пробует сервер (мгновенно), фолбэк на лист - тот же приём, что и everywhere в этом переезде. Добавлен `noteServerDataQuality_()` - если `/api/orders_raw` отвечает `source:"archive"` (январь-май, урезанный формат исходного листа - нет "Проведён"/"Реализация"/полной "Прибыли"), в ответ кладётся `data_quality_warning` с явным текстом "не использовать для зарплат/премий за этот период" - долетает до фронтенда как есть, не показываем красивые, но неверные цифры молча. `getAvailablePeriods()` - январь-май добавлены в список периодов явно (в этой таблице нет листов "Заказы_2026-0X" для них, только на сервере). |
261-263 | 2026-08-19 | **ДЗ - ВКЛЮЧЕНО целиком** (Влад: "Все переноси на сервер и включай"). История (`debt_history` 43 дня, `debt_history_by_customer` 6887 строк - разово, `import/import-debt-history-once.js`) + ежедневный снимок (`import/snapshot-debt-history.js`, идемпотентно, cron раз в 2 часа) - все 42 дня истории (кроме сегодня) совпали ТОЧНО с живым, drill-down "что изменилось" тоже сверен (тот же compare_date, тот же состав контрагентов). Новый `/api/debt_computed` на сервере + `/api/debt/set_status`/`set_comment` (запись статуса - вызываются ИЗ Apps Script СРАЗУ после записи в лист "ДЗ_Статусы", двойная запись держит сервер в реальном времени). Apps Script: `getDebtData()` пробует сервер первым (`fetchDebtComputedFromServer_`, `debtCalcLooksSane_`), флаг `USE_SERVER_DEBT_CALC` - **ВКЛЮЧЁН**, `verifyServerDebtCalc()`/`enable`/`disableServerDebtCalc()` - тот же принцип, что у заказов. Включение подтверждено на реальном пути (временный `diag_enable_debt_calc`, использован и убран): `getDebtData(ss)` реально отдаёт серверные цифры (40 560 045 ₽/164 должника). Фронтенд НЕ трогали - `action=...` в Apps Script не изменился, смена источника прозрачна для сайта. |
(вне Apps Script, сервер) | 2026-08-19 | **ДЗ - продолжение: статусы перенесены, getDebtData() портирован и сверен.** `debt_status` (MySQL, 51 запись, 1 дубль в источнике склеен) - `import/import-debt-status.js`, cron `*/10 * * * *` (запись статусов пока остаётся в Таблице, не переключена). `api/lib/debt-calc.js`: `aggregateDebtData()` - порт `getDebtData()` (summary/by_customer/by_manager/by_status/guarantees/корзины просрочки/банкроты/автостатус "В графике"), БЕЗ history-блоков (История_ДЗ ещё не на сервере - `not_computed`). Сверка (`debt-parity-check.js`, временный `diag_debt_data` в doGet - использован и убран) против живого `getDebtData()`: источники разного времени (FTP-тест vs email-импорт), поэтому сравнивали ТОЛЬКО контрагентов с неизменным balance между снимками (158 из 161 общих) - **0 расхождений из 632 проверок** (менеджер/дни просрочки/статус/дата долга), `overdue_dead` (600+ дней) совпал точно (12 360 902 ₽), 4 из 5 юрлиц в `by_org` совпали точно. Остаток расхождений объясняется временем снимков, не багом порта. Готово: сырые данные + статусы + агрегация. Не готово: История (тренды/drill-down), запись статусов на сервер, переключение фронтенда. |
(вне Apps Script, сервер) | 2026-08-19 | **ДЗ (дебиторская задолженность) - начат перенос на сервер** (Влад: "блок ДЗ и всё что с ней связано переводим на сервер... начнём с того что отчёты теперь пойдут в сервер, а не на почту"). Отчёт 1С теперь идёт по FTP (`uploads_debt/`, тот же приёмник `receipts_upload`) вместо email, раз в час 10:00-19:00. `api/lib/debt-calc.js` - порт `parseDebtRawRows_` (FIFO-покрытие, гарантии, взаимозачёт, master-строки - вся логика 1-в-1, см. `plans/2026-07-08-debt-receivables-tab.md` в основном репо за историей решений). **НАЙДЕН БАГ в живом Apps Script-коде** (не связан с переносом, существовал с 2026-07-08): разбор начинается с жёстко зашитого индекса строки, из-за чего ПЕРВЫЙ блок юрлица в каждом отчёте (сотни контрагентов) не получал разбивку по юрлицам и с балансом 0 пропадал из "Должников" на живом дашборде. Влад: "не чинить там, сразу делать правильно на сервере" - `debt-calc.js` ищет начало данных динамически, по строке-заголовку. Проверено на реальном FTP-файле: 1525 контрагентов, 164 реальных должника, 40 560 045 ₽ (раньше 431 из 2310 контрагентов молча теряли разбивку по юрлицу). `import/import-debt.js` - парсит последний xlsx, пишет в MySQL `debt_customers` (TRUNCATE+INSERT, пропуск если файл не менялся), cron `*/5 * * * *`. **Осталось**: `ДЗ_Статусы` (ручные статусы/комментарии - не из 1С, нужен разовый перенос + новый API на запись), `История_ДЗ`/`История_ДЗ_по_контрагентам` (дневные снимки для графика/трендов), `getDebtData()`-эквивалент (агрегация для дашборда), переключение фронтенда - НЕ начаты. Подробности - `README.md` на сервере. |
| 248-250 + фронтенд без версии | 2026-08-19 | **АВАРИЯ (обновлённый разбор - первая гипотеза про "мёртвый deployment ID" оказалась НЕВЕРНОЙ, см. ниже): `/exec` периодически отдаёт HTML вместо JSON** - сайт не грузил данные (Влад увидел "Unexpected token '<'..." на "Панели", перезагружал дважды). Первая диагностика ошибочно решила, что deployment ID `AKfycbxWi4wJWsSeHdgwrsC-...` "мёртв" (стабильно 404 несколько попыток подряд) - создан НОВЫЙ deployment ID, `API_URL_DEFAULT` переключён на него, запушено в аварийном порядке. Дальнейшая проверка ОПРОВЕРГЛА эту гипотезу: через несколько минут старый ID стал отвечать 200 (хоть и с другой ошибкой - "Sorry, unable to open the file"), а НОВЫЙ ID начал давать ТУ ЖЕ картину (3 попытки подряд к нему: пусто/HTML-ошибка/корректный JSON) - значит проблема НЕ в конкретном deployment ID, а в кратковременной нестабильности самого Google (Apps Script `/exec` в целом, оба ID показали одинаковый паттерн ~1 успех из 3). **Реальный фикс**: `API_URL_DEFAULT` возвращён на исходный ID (плодить лишние deployment'ы бессмысленно раз проблема не в них), вместо этого добавлена retry-логика на фронтенде - `fetchJsonWithRetry_()` в `files/index.html`, до 3 попыток с паузой 700-1700мс, используется и в `loadData()` (главная загрузка), и в `apiFetchJson()` (все остальные запросы) - тихо повторяет при сбое парсинга JSON, показывает ошибку только если не повезло 3 раза подряд. Причина самой нестабильности на стороне Google НЕ установлена (возможно связана с необычно высокой частотой `clasp deploy` в этот скрипт за день - и моих, и параллельной сессии - но это не подтверждено, просто совпадение по времени). CLAUDE.md обновлён. Заодно (4-й раз за день) синхронизирована параллельная работа "Создание заявок План задание" (вариант Б, протестировано) - Фаза 1d уже была на месте при проверке, конфликтов не было. |
| 243-247 | 2026-08-18 | **Серверный расчёт заказов ВКЛЮЧЁН** (Влад: "Можешь сам её включить?"). `USE_SERVER_ORDERS_CALC='on'` в Script Properties - дашборд теперь считает "Обзор заказов"/"По менеджерам"/"По логистам"/"Зарплата" через `api.yardhub.ru` (4.6-8.5x быстрее), с тихим фолбэком на Apps Script при аномалии в ответе. **Инцидент по дороге**: параллельная сессия (доработка "Создание заявок План задание") трижды подряд пушила код мимо git с локальной копии, не включавшей мой более ранний пуш (v240-242) - каждый раз молча стирало функции переключателя (`fetchOrdersComputedFromServer_`/`verifyServerOrdersCalc`/`enable/disableServerOrdersCalc` и т.д., САМ расчёт `aggregateOrdersRows` не задет). Замечено сразу обязательным `clasp pull`+diff перед каждым push - три раза подряд восстановлено вручную поверх их свежей работы, проверено grep+`node -c`, передеплоено. Продакшн `/exec` ни разу не отдавал сломанный код реальным пользователям (проверено `clasp deployments` в процессе) - битым был только черновик в редакторе между push и следующим deploy. Финал подтверждён (`diag_final_check`, v246→247): флаг on, реальный `getOrdersData()` = 464 заказа/33 343 680 ₽/порог 23%=true, `verifyServerOrdersCalc()` = 0 расхождений. Подробности инцидента - `plans/2026-08-18-payroll-logic-server-port.md`, раздел "Включение (Фаза 1d)". |
| 240-242 | 2026-08-18 | **Перенос зарплатной логики - Фазы 1b и 1d готовы, включение НЕ сделано (ждёт решения Влада).** Новый эндпоинт `/api/orders_computed?period=YYYY-MM` на сервере - отдаёт ПОСЧИТАННЫЙ результат (~0.27 сек), явным полем `not_computed` перечисляет то, чего НЕ считает (`profit`/`profit_tral`/`profit_long`/`special_trals_profit` - это Штатка+История_финансов, не переносились). Заодно убрано дублирование правила выбора источника между `/api/orders_raw` и новым эндпоинтом (общая `readOrdersRawRows_`). Apps Script: `fetchOrdersComputedFromServer_()` + флаг `USE_SERVER_ORDERS_CALC` (**выключен по умолчанию**), управление функциями `enableServerOrdersCalc()`/`disableServerOrdersCalc()`/`serverOrdersCalcStatus()` из редактора; `serverCalcLooksSane_()` проверяет ответ сервера перед доверием (период, total_orders>0, `hired_margin_qualifies` именно boolean, массивы by_manager/by_logist) - иначе тихий фолбэк на локальный расчёт. Новый ПОСТОЯННЫЙ инструмент `verifyServerOrdersCalc(period)` - сверяет обе копии логики на одних сырых данных, поимённо по каждому менеджеру/логисту (запускать после любой правки списков/порогов). Приёмка (временный `diag_phase1d`, v241, убран v242, флаг включался на один запрос и выключался через `finally`): август 5236мс→1141мс (4.6x), июль 4544мс→531мс (8.5x), цифры ИДЕНТИЧНЫ, 0 расхождений, флаг остался выключенным. Сверка эндпоинта против снимков живого расчёта: 6 месяцев 0 расхождений, июнь - только порядок внутри равных `days_since` в `lost_customers` (значения и состав идентичны, свойство оригинала - не чинил молча, записан кандидат на улучшение), август - известный ТРЕЙДСЕРВИС. Подробности - `plans/2026-08-18-payroll-logic-server-port.md`, чекпоинт 5. |
| 237-239 | 2026-08-18 | **Перенос зарплатной логики - Фаза 1a полностью завершена.** `computeLostCustomers` (список пропавших клиентов) портирован в `orders-calc.js` - 7/8 месяцев полное совпадение, август разошёлся на 1 заказе (№469223, 21 000 ₽) - разобрано до конца: заказ есть в Google Таблице, но отсутствует в FTP-выгрузке 1С (та же природа, что известный инцидент с "Машина прикрытия", НЕ баг порта - см. подробности в plans/2026-08-18-payroll-logic-server-port.md, чекпоинт 4). Подтверждено отдельно: денежный агрегат по июлю (total_orders/total_amount) сходится с Таблицей идеально прямо сейчас - расхождение изолировано только в этой не-денежной функции. Вся расчётная логика заказов (aggregateOrdersRows/joinManagerPlans/computeSalesFaktPlan/computeLostCustomers) теперь портирована и сверена; денежная часть - 8/8 месяцев без единого расхождения. Временные диагностики (v237-239) использованы и убраны. Ничего не переключено - готово к Фазе 1d. |
| 231-236 | 2026-08-18 | **Перенос зарплатной логики на сервер (Фаза 1a-1c ЗАВЕРШЕНЫ)** - Влад одобрил перенос самого расчёта (не только источника сырых данных), см. `plans/2026-08-18-payroll-logic-server-port.md` и стратегию в CLAUDE.md. `aggregateOrdersRows`/`joinManagerPlans_`/`computeSalesFaktPlan_` портированы 1-в-1 в Node.js (`/root/yard-dashboard/api/lib/orders-calc.js`) - НЕ подключены ни к одному живому эндпоинту, только лежат для сверки. `manager_plans` в MySQL (47 строк, `import/import-manager-plans.js`, через временный `export_manager_plans`). Закрыта дыра июня - лист "Заказы_2026-06" (живой формат, 105-122 колонки) залит в `orders_archive` (`import/import-orders-june.js`, `DELETE WHERE month_key='2026-06'` + `INSERT`, НЕ `TRUNCATE` - январь-май не тронуты: 3545→4492 строки, +947 июня, 62 279 274 ₽ - совпадает день-в-день с уже проверенным `month_summary`). **Построчная сверка (`parity-check.js`) по ВСЕМ 8 месяцам - 0 расхождений везде** (через временный `diag_full_orders`, v231-236, использован и убран) - каждый менеджер/логист/поставщик найма, порог 23% маржи найма, воронка документов по декадам, все поля `by_manager`/`by_logist`/`internal`/`long_funnel`. Node считает то же самое за 15мс против секунд в Apps Script. НИЧЕГО не переключено - портированный код нигде не используется в бою, риск для реальных зарплат нулевой. Осталось: `computeLostCustomers_` (не денежный) + Фаза 1d (реальное включение с параллельным прогоном перед доверием единолично). |
| 244-253 | 2026-08-18 | **"План задание" - UX-доводка формы создания + редактирование существующей заявки.** По живой обратной связи Влада после первого успешного создания заявки: (1) время подачи - `type="time"` вместо текста (было "неудобно вводить знак ':'"); (2) заказчик - datalist-автодополнение по уже загруженным заказам, свободный ввод для нового заказчика остаётся; (3) контактное лицо запоминается ПО заказчику (свой datalist, перестраивается при смене заказчика) - лёгкая версия "карточки фирмы", полноценный СРМ с телефонами по каждому контакту - отдельная задача на будущее; (4) стоимость - `min=0`+проверка регулярным выражением до отправки (защита от текста); (5) галочка "Наличные" - т.к. отдельной колонки под это в таблице нет, уходит в примечание строкой "нал"; (6) **главный фикс производительности** - Влад: "жуткие тормоза, долго грузит, потом перегружает страницу... никто не будет работать с такой системой" - полная перезагрузка вкладки после каждой записи была лишней, теперь заявка добавляется/обновляется в уже загруженных данных на месте и перерисовывается мгновенно, без похода на сервер (серверный кэш дня всё равно сброшен бэкендом при записи - при следующем обычном обновлении подтянутся поля, которые заполнит логист). Новое: `updateOrderPlanEntry_`/`?action=order_plan_update` - редактирование уже созданной заявки (находит строку по day+orderNumber, не ищет свободную), включая смену статуса (пишет в ту же ячейку, что отслеживает `masterEditRouter` - покраска/Telegram сработают сами, тот же installable-триггер); проверяет, что редактирует именно автор заявки; НЕ трогает колонки L/M/O/P/Q/R (транспорт, отметки логиста - территория логиста). Таблица "План задание": колонка "Действия" с иконками ↻ (повторить) и ✎ (редактировать), видна только для СВОИХ заявок (role==='manager' на конкретном заказе). При деплое обнаружена и синхронизирована в git параллельная (недокоммiченная) работа другой сессии - серверный расчёт "Поступлений" для личной страницы менеджера (see commit 7cc7ebe).
| 254 | 2026-08-18 | **"План задание" - быстрая смена статуса из таблицы + 1С-заказчики + фильтр дат.** По живой обратной связи после первого редактирования: (1) Влад: "не увидел возможность менять статус... статус должен меняться прямо из таблицы" - колонка "Статус" стала `<select>` для своих заявок, `?action=order_plan_set_status`/`updateOrderPlanStatus_` - облегчённая запись, трогает ТОЛЬКО одну ячейку (статус, нижняя строка колонки A), никакого риска затереть остальные поля устаревшими клиентскими данными; пишет в ту же ячейку, что отслеживает `masterEditRouter` - покраска/Telegram сработают сами; тот же авторский контроль, что и у полного редактирования. (2) Влад: "список компаний должен быть из 1С... старые корявые уйдут со временем" - datalist заказчика теперь смешивает `D.debt.by_customer` (реальные контрагенты 1С, уже загружены с основным payload, без похода на сервер) с тем, что уже использовалось в самой таблице планировки - плавный переход, не резкий, свободный ввод для нового заказчика остаётся. (3) Влад: "предлагаемые даты должны быть сегодня и дальше" - день подачи в форме создания/повтора теперь начинается с сегодняшнего числа (редактирование не ограничено - заявка может требовать правки задним числом).
| — (только фронтенд) | 2026-08-18 | **Автодополнение заказчика - строгое "начинается с".** Влад: "когда ввёл 'пр' уже все должны быть с названием 'пр' в начале" - нативный `<datalist>` в Chrome фильтрует по совпадению ГДЕ УГОДНО в строке (не настраивается), из-за чего в подсказках попадались компании вроде "ДЕЛЬТА-М ООО Просецкий" при вводе "пр". Заменён на свой лёгкий автокомплит (`orderPlanCustomerInput_`/`orderPlanPickCustomer_`) - абсолютно спозиционированный список под полем, фильтрация строго `name.toLowerCase().indexOf(val) === 0`. Выбор варианта - через `onmousedown` (не `onclick`), чтобы не терять клик из-за `onblur`, который прячет список. |
| 264 | 2026-08-19 | **Фикс: "Не сданные путевые листы" считал служебные строки.** Влад заметил на дашборде (вкладка "Не сданные путевые листы", Обзор заказов -> июль 2026): у водителя Войткун показано 8, хотя в 1С по фильтру "Путёвка: Нет" из 8 строк только 4 реально безналичные (подлежат учёту), остальные 4 - "Прочее" (служебные, не участвуют в документообороте по определению, тот же принцип, что уже применялся в основной воронке "Статус документов"). Причина: `paymentVariant`/`isServiceRow` вычислялись только внутри блока "Статус документов", который в цикле идёт ПОСЛЕ блоков "По поставщикам найма" и "По водителям" - те физически не могли сослаться на переменные, объявленные ниже, и считали `no_waybill` по всем строкам без исключения. Фикс: переменные подняты в начало цикла `aggregateOrdersRows`, добавлено `&& !isServiceRow` в 4 местах (`supplierMap[...].no_waybill`, `lds.no_waybill`, `driverMap[...].no_waybill`, `driverMap[...].no_waybill_long`); `longFunnel.no_waybill` изменений не потребовал - уже был внутри правильно исключающего блока. Исправлено в ОБЕИХ копиях: `scripts/full_script_final.js` (Apps Script, v264, redeploy на живой `/exec`) и `api/lib/orders-calc.js` (сервер, scp + `systemctl restart yard-api`). Проверено на живых данных июля через `/api/orders_computed?period=2026-07` на самом сервере: Войткун 4/17 (было 8/17) - совпадает с ожиданием Влада. |
| 267-269 | 2026-08-19 | **Этап 0 плана устранения аудита ВЫПОЛНЕН + дешёвая победа Этапа 1.** (0.1) Автобэкап БД: `import/backup-db.js`, cron 01:00 UTC, хранение 30 дней, проверка целостности архива, отказ сохранять подозрительно малый дамп. **Восстановление проверено по-настоящему** - развернул последний архив в отдельную базу, все таблицы на месте (1526 ДЗ / 1510 заказов / 112065 клиентов), тестовая база удалена. (0.2) Журнал состояния `import_runs` + помощник `import/import-log.js`, обёрнуты все 5 cron-скриптов; различает `ok` / `skipped` (файл не менялся - ЗДОРОВОЕ состояние, мешать с ошибкой нельзя) / `error`; сам чистит записи старше 30 дней; никогда не роняет импорт, если запись в журнал не удалась. (0.3) `/api/import_status` + плашка в сайдбаре дашборда (`nav-stale`), когда импорт молчит или падает; молчание САМОГО сервера намеренно не показываем - там уже работает тихий фолбэк и цифры честные. (0.4) `watchdogCheckDataFreshness()` в Apps Script - сторож вынесен НАРУЖУ намеренно: сторож внутри того, за чем следит, не сообщит ни про мёртвый cron, ни про лежащий сервер; защита от спама (та же проблема не чаще раза в 6 ч, восстановление - один раз); токен бота остаётся в Script Properties, копии на VPS не появилось. (0.5) logrotate на логи импортов (7 дней, сжатие). **Этап 1.0:** запись статусов ДЗ переведена на отдельный `WRITE_KEY`, которого нет в браузере - публичный ключ теперь получает 403 на запись и 200 на чтение (проверено). Ключ сгенерирован на сервере и передан в Script Properties прямо с VPS через временный `set_write_key` (v268, удалён в v269) - значение не проходило через переписку. Плюс `enableKeepAlive` на пул MySQL после двух обрывов соединения за день (корень НЕ подтверждён, `wait_timeout` 8 ч - это страховка, не лечение). **Ручной шаг за Владом: выполнить `setupWatchdogTrigger` в редакторе Apps Script**, иначе уведомления не включатся. |
| — (только сервер) | 2026-08-19 | **Технический аудит + устранена тихая авария импорта заказов.** `import-orders-raw.js` падал 182 раза подряд, данные не обновлялись 8 часов, дашборд молчал (а на этих данных считается зарплата, `USE_SERVER_ORDERS_CALC=on`). Корень: `fs.readdirSync` без `encoding` отдаёт имя, декодированное как UTF-8; файл с именем в CP1251 (1С так называет выгрузки, если в рассылке снята транслитерация) даёт "мохибейк", и `statSync` по нему падает с ENOENT - роняя ВЕСЬ импорт, а не пропуская один файл. Триггер - отчёт ДЗ, случайно залитый в `uploads_orders`. Тот же баг был скопирован в ТРИ импорта (заказы/поступления/ДЗ) - вынесен в общий `import/find-xlsx.js` (имена читаются Buffer'ом, `try/catch` на каждый файл) и исправлен во всех сразу, не заплаткой в одном месте. Плюс `import-orders-raw.js`: раньше брал строго новейший .xlsx - чужой файл, оказавшийся новее, молча замораживал импорт навсегда (таблица не портилась, срабатывала проверка "0 строк - не пишу", но и не обновлялась); теперь перебирает от свежего к старому до первого разбираемого и падает с внятной ошибкой, если подходящего нет. Проверено прогоном всех трёх импортов, включая намеренный возврат сломанного файла в папку. Снят первый бэкап БД (`/root/backups/`, 2.7 МБ, целостность проверена) - до этого бэкапов не было ВООБЩЕ. Полный отчёт аудита с 10 рекомендациями по приоритету - отдельный артефакт (главное невыполненное: публичный `YARD_API_KEY` в `files/index.html` даёт полный доступ к серверу любому, кто откроет исходник страницы - ждёт решения Влада). |
| 265-266 | 2026-08-19 | **Кэш дропдауна "Период" на сервере - все 7 закрытых месяцев.** Влад заметил задержку ~8 сек при смене периода на вкладках "Заказы"/"По менеджерам"/"По логистам"/"Зарплата" и спросил, зачем вообще обращение к Apps Script, если данные уже на сервере. Причина: `period_snapshots` (серверный кэш для дропдауна) содержал только июнь - для остальных 6 закрытых месяцев (2026-01..05, 2026-07) фронтенд получал 404 от сервера и тихо откатывался на медленный путь через Apps Script (чтение листа + пересчёт). Единственное, что физически нельзя посчитать на Node - Валовая прибыль (`getGrossProfitForPeriod`, лист "История_финансов", не портирован) - но для ЗАКРЫТОГО месяца эти числа не меняются, поэтому решение: разовый временный `export_period_snapshot` (secret-gated, v265) - выгрузил снимки по всем 7 месяцам через curl-цикл с VPS (уже с исправленным багом путевых листов), залил в `period_snapshots` (`import/load-period-snapshot.js`), временный action убран (v266). `/api/orders_period?period=2026-07` теперь 0.27 сек (было 8 сек). Январь-май - `profit=0` в кэше не баг, то же самое, что и сегодня возвращает Apps Script для этих месяцев (История_финансов не имеет данных так далеко назад из-за миграции мегабазы) - предупреждение `data_quality_warning` едет вместе с данными, ничего не скрыто. На будущее: закрытие каждого нового месяца требует повторить этот разовый export+load, пока Валовая прибыль не переедет на сервер. |
| 271 | 2026-08-21 | **Новый менеджер Ратников Максим - личная страница + плашка, без ЗП.** Влад: "новый менеджер, не к Лиане, не к Айнур, сам по себе... всё как у обычного менеджера, только он без старшего. Зарплату ему считать пока не нужно, остальное всё как у всех продавцов." Backend: `'Ратников'` добавлен в `TRAL_MANAGERS` (иначе его заказы молча выпадали бы из `managerMap`/`by_manager` - тот же класс регрессии, что уже дважды ловили с Сурковой в `TRAL_LOGISTS`) и в `activePlanKeys` (чтобы будущий план для него корректно попадал в общий план компании). Frontend: `DEPT_CFG` получил запись `solo:true` (`head:null`, `members:['Ратников']`) - отдельная "плашка" на вкладке "Менеджеры" и в компактных карточках "Обзора", без "руководитель группы"/дублирующего списка участников. Личная страница - без изменений в структуре, кроме блока "Расчёт зарплаты": `isSalaryNotConfigured_(sur)` подменяет его плейсхолдером ("формула ещё не согласована") - ставка для него не решена, показывать `calcMgr()` с дефолтной ставкой было бы враньём сотруднику. Проверено изолированным Node-тестом (`renderSalesMgr` с синтетикой). **КООРДИНАЦИЯ:** первый пуш в git не дошёл до GitHub - сеть в контейнере оборвалась в момент `git push`, а параллельная сессия в это время независимо запушила несвязанный коммит (разбор Telegram-уведомлений) с той же базы - НЕ конфликт/откат, просто гонка публикаций. Обнаружено по `git merge-base --is-ancestor` (мой коммит не был предком origin/main), смёржено и запушено повторно, подтверждено независимым `git fetch` + прямым запросом к живому `yardhub.ru` (три вхождения "Ратников Максим" в отданном HTML). |
| 272 (фронтенд) + VPS | 2026-08-21 | **Предпросмотр "Личной страницы" для Ратникова без заказов + синхронизация серверной копии зарплатной логики.** Влад после первого деплоя: "могу зайти под его страницу, как я это делаю под другими? Почему нет выбора?" - дропдаун "Личной страницы"/"Смотреть как сотрудник" строится из `by_manager`/`by_logist` (кто угодно с хоть одним заказом), а у совсем нового сотрудника заказов ещё нет - выбрать физически нельзя. Добавлен `ALWAYS_PREVIEW_ROSTER_`/`buildMyPageRoster_()` - синтетическая заглушка-запись подмешивается в ростер, ТОЛЬКО если реальной записи с той же фамилией ещё нет (реальные заказы сами вытеснят заглушку, ничего убирать вручную не надо); заодно устранено тройное дублирование сборки ростера (`myPageCurrentEntry_`/`renderMyPage`/`viewAsRoster_` независимо повторяли один и тот же concat+dedupe - тот же класс риска, что дедуп Пруса). **ОТДЕЛЬНО обнаружено:** `USE_SERVER_ORDERS_CALC=on` - серверный расчёт (`api/lib/orders-calc.js` на VPS) имеет СВОЮ копию `TRAL_MANAGERS`, не синхронизированную автоматически с `scripts/full_script_final.js` (та же ловушка, что дважды роняла зарплату Сурковой, см. README сервера). Добавлено `'Ратников'` и туда, бэкап снят (`orders-calc.js.bak-20260821-221215`), Node-процесс перезапущен (require() кэширует модуль - правка файла без рестарта ни на что не влияет), проверено curl'ом с сервера. Заодно замечен (но НЕ тронут - вне рамок правки) более старый дрейф `INTERNAL_CLIENTS` между копиями ('БАЗА' есть только в Apps Script, 'Панасюк Роман Викторович' - только на сервере) - см. запись на VPS README, стоит отдельной сверки через `verifyServerOrdersCalc()`. |
| 273-280 (врем.) | 2026-08-21 | **Этап 1.1 плана устранения аудита - закрыт публичный read-ключ (пункт 01, главная находка).** Влад: "Ключ доступа к серверу меня беспокоит давай исправим". `YARD_API_KEY` лежал открыто в `files/index.html` - анонимный `curl` без единого входа получал 200 и полную базу должников. Разобрана точная карта вызовов (браузер напрямую vs Apps Script server-to-server) для всех 8 read/write-эндпоинтов, чтобы не сломать ни личную страницу менеджера, ни сторожа. `api/server.js`: новые `checkSession` (HMAC session_token, ТОТ ЖЕ секрет и формат, что `issueSessionToken_`/`verifySessionToken_` в Apps Script - сверено байт-в-байт на реальном токене, оба независимо посчитали одинаковую подпись) и `checkServerKey` (новый секрет `SERVER_KEY` только для Apps Script, никогда не в браузере) + `checkSessionOrServerKey` для двух эндпоинтов с обоими вызывающими (`receipts`, `import_status`). `YARD_API_KEY` в Script Properties Apps Script провёрнут на `SERVER_KEY` (код Apps Script не менялся - только значение переменной, которую он уже читал). `files/index.html`: `YARD_API_KEY`-константа убрана из файла ПОЛНОСТЬЮ, `fetchFromYardApi_` переиспользует уже существующий `SESSION_TOKEN`. Проверка в 4 слоя: HMAC байт-в-байт, полная матрица авторизации curl'ом (8 сценариев), настоящий сквозной вызов ЧЕРЕЗ САМ Apps Script новым ключом (`fetchDebtComputedFromServer_`/`fetchReceiptsComputedFromServer_`), живой браузер с CORS на yardhub.ru. Все временные диагностики (v273-280: `export_session_secret`, `issue_test_token`, `rotate_server_key`, `diag_check_server_key`) использованы и убраны. По пути синхронизирована незакоммiченная параллельная работа другой сессии (калькулятор - история расчётов/нумерация КП переехали на сервер, тот же паттерн `WRITE_KEY`) - конфликтов при слиянии не было. Известное ограничение: `session_token` не несёт роль - см. README на сервере и план. |
| 288 + VPS | 2026-08-25 | **ДЗ - добавлено 6-е юрлицо "УМИАТ ЯРД" (клиент/менеджер программно пропадал с дашборда).** Влад: "Ратников Максим он единственный кто будет работать от компании УМИАТ ЯРД, я добавил его вчера в отчеты ДЗ. Но пока на дашборде я его не вижу." Диагностика по старой памяти пошла не туда (временная функция под Gmail-канал ДЗ - "Письмо не найдено", т.к. отчёт ДЗ давно идёт по FTP на сервер, а не на почту; Влад поправил - см. новую память `feedback_debug_server_first`). Корень: `DEBT_ORG_KEYS`/`DEBT_ORG_SHORT_NAMES` жёстко знали только 5 юрлиц группы - `byOrgBalance`/`balance` в `parseDebtRawRows_` считались ТОЛЬКО по этим ключам, долг под любым другим юрлицом обнулялся, клиент выпадал из `customers` целиком (фильтр `balance>0`) - тот же класс бага, что уже дважды ловили (колонка "Менеджер", период отчёта 1С). Точная строка юрлица (`"02. УМИАТ ЯРД ООО"`) и написание менеджера подтверждены прямым дампом сырого xlsx с сервера (`/home/receipts_upload/uploads_debt/`, временный node-скрипт, использован и удалён) - Ратников ведёт и тралы, и краны/экскаваторы под этим юрлицом. Исправлено в ЧЕТЫРЁХ копиях: `scripts/full_script_final.js` (Apps Script, v288, redeploy на живой `/exec`), `api/lib/debt-calc.js`, `import/snapshot-debt-history.js`, `import/import-debt-history-once.js` (разовый, не на cron, синхронизирован для консистентности) - все три серверные копии залиты через scp с бэкапом (`*.bak-20260825-umiat`), `systemctl restart yard-api` (require() кэширует модуль). Плюс цвет юрлица на графике "Динамика ДЗ" (`files/index.html`, `debtOrgColors`). Проверено вживую: принудительный `import-debt.js` (1545 контрагентов, 177 должников, 44 227 918 ₽) - Ратников с реальными балансами по УМИАТ ЯРД в `debt_customers` (МО-4 2 796 000 ₽ и др.); `snapshot-debt-history.js` прогнан вручную, сегодняшний снимок истории уже включает новое юрлицо. |
| 290 + VPS | 2026-08-25 | **ДЗ - Ратников ведёт несколько отделов (тралы + краны/экскаваторы) под одним именем в 1С, его "чужой" долг исключён из тральной ДЗ на уровне ДОКУМЕНТА, не контрагента.** Влад после фикса v288 сверил цифры вручную: баланс Ратникова (5 752 065 ₽) не совпадал с его выручкой по тралам в "По менеджерам" (4 541 690 ₽) - разница (1 210 375 ₽) оказалась долгом контрагента "СУ 910 ООО АВТОБАН", у которого документ 473324 в сыром отчёте заказов помечен `Отдел: "2. Экскаваторный отдел"` (не тралы). Первая идея - статус контрагента "Долг отдела экскаваторов" (уже был готовый механизм, `DEBT_STATUS_EXCLUDE_FROM_TOTAL`) - Влад отклонил: "если помечу как отдел экскаваторов, то долги моего направления не будут читаться" - у ОДНОГО контрагента (пример "Мостоотряд") может быть долг из РАЗНЫХ отделов одновременно, статус на уровне контрагента прячет оба сразу. Решение (точечный фикс, без плана): `DEBT_CROSS_DEPT_MANAGERS = ['Ратников']` в `parseDebtRawRows_`/`parseDebtRawRows` - для документов этих менеджеров с описанием "Таблица заказов NNNNNN" сверяем номер с набором номеров из `orders_normalized`/"Заказы_данные" (`knownOrderNumbers`, передаётся импортом); не нашёлся - сумма документа вычитается из `byOrgBalance`/`balance` ДО суммирования (не по статусу, точно до документа), но сам документ остаётся в `unpaidDocs` с флагом `nonTralCrossDept` для прозрачности. Исправлено в ДВУХ копиях: `scripts/full_script_final.js` (+ новая `getKnownOrderNumbers_()` читает "Заказы_данные", Apps Script v290, redeploy на живой `/exec`) и `api/lib/debt-calc.js` + `import/import-debt.js` (запрашивает `orders_normalized.order_number` перед разбором, передаёт в `parseDebtRawRows`) на VPS - бэкапы сняты, `systemctl restart yard-api`. `files/index.html`: `buildDebtDocsHtml_` показывает исключённые документы отдельной строкой (приглушённые, с бейджем "не наш отдел - не в балансе") и отдельным пунктом в сверке "Итого по документам", не смешивая с покрытием авансом. Проверено: Node-юнит-тест на смешанном контрагенте (1 тральный + 1 кран-документ - в баланс попал только тральный, оба видны в списке), принудительный `import-debt.js` на реальных данных - `debt_customers` по Ратникову: АВТОБАН обнулился, СУММА(баланс) = 4 541 690 ₽ ровно; живой `/api/debt_computed` через curl подтвердил тот же `by_manager` итог. |
| 281-286 (283/286 живые) | 2026-08-24 | **Простой хостинга Beget (неоплата) + выручка/ДЗ/поступления вырваны из часового кэша.** Хостинг приостановлен из-за неоплаты - остановлен 08:33 UTC (11:33 МСК, совпало с наблюдением Влада "выручка в 11 уже такая же"), Влад оплатил, перезагрузка 12:18 UTC (15:18 МСК), сеть до VPS ещё долго флаппала короткими окнами при стабилизации на стороне Beget (сервисы были живы и здоровы в каждом успешном окне - подкачка не тронута, гипотеза про своп-трэшинг НЕ подтвердилась). Отчёты 1С отправлены вручную в 15:44 МСК - подхвачены короткими SSH-командами (каждая своим подключением, чтобы пережить обрыв), заказы/поступления/ДЗ/статусы ДЗ обновлены вручную, не дожидаясь cron. **Второй, более глубокий баг найден по ходу:** даже когда VPS уже отдавал свежие цифры, дашборд ещё час показывал застывшую выручку (39.3М вместо 47.7М) - главная страница читает ЧАСОВОЙ СНИМОК (`saveMainPayloadCache_`), обновляемый только внутри `runAll()`. Влад: "мы уже перешли на сервер по выручке ДЗ поступлениям, вся логика должна быть серверная, а не гугловская". Исправлено: `getMainPayloadCacheOrLive_()` теперь ВСЕГДА пересчитывает orders/debt/receipts/summary заново на каждый заход (уже пробуют сервер первым - миллисекунды), в часовом кэше остаются только vehicles/drivers/history/periods/driverOrderCounts (читают Штатку/Историю_финансов напрямую из Sheets, Валовая прибыль своего парка ещё не портирована). Побочно найдено (не баг): `getSummaryData()` возвращает `summary.revenue` (Sheets, часть Валовой прибыли) и `summary.salesFakt` (сервер, ordersData) - фронтенд читает именно `salesFakt`, давно осознанная разница определений. Проверено: `force_refresh_cache` поднял 39.3М->47.7М мгновенно; после фикса `summary.salesFakt === orders.summary.total_amount === 47718418` через прямой вызов (временная диагностика, убрана). Все временные диагностики (v281 `force_refresh_cache`, v284-285 `diag_main_payload`) использованы и убраны, живой `/exec` v286 проверен на чистый ответ. |
| 289 | 2026-08-25 | **Личная страница Рыщанова Рустема - руководитель отдела логистики.** Влад: "ИИ задачи по сотрудникам логистам, это путевые в основном по своему и наемному парку... табличка техника со всеми едeницами и расходами... табличка водители... по наемникам, не сданные путевые, топ поставщиков не сдавшие путевые... расчет зарплаты и план по валовой прибыли, план 35 млн на август... вкладки текущий и прошлый месяц... похожа на страницу Пруса". Рыщанов был в `TRAL_LOGISTS` и получал неверную (наёмную) страницу - он руководитель ВСЕГО отдела, не брокер найма лично. Backend: `isRyschanowLogistName_`, `buildRyschanowSummaryBundle_` (ВП своего парка/ВП спецтралов/маржа найма всех логистов/путевые компании целиком/техника/водители - переиспользует существующие `getSummaryData`/`getGrossProfitForPeriod`/`aggregateFinHistoryForRange`), новый ИИ-промпт (`buildRyschanowAiContext_`/`buildRyschanowAiTasksPrompt_`, категории документы/поставщики - уже валидны в общем парсере), `buildLogistView_` прикладывает `ryschanow_summary` только ему, `doGet` резолвит роль `'ryschanow'` по фамилии (в листе "Доступ" у него всё ещё `logist`). Frontend: `renderMyPageRyschanow` (отдельная функция, как у Васина, не флейвор наёмного логиста), `calcRyschanow` вынесена с уровня `renderSalesSalary()` на module scope (была дублирована локально почти 2 недели), `setMyPagePeriod`/`renderMyPage()` расширены на `D.drivers`/`D.summary.profit`/`D.summary.special_trals_profit` при переключении периода (раньше подменялись только для Васина). План 35 млн - НЕ хардкод, новый ключ `рыщанов_вп` в листе "Планы_менеджеров" (ждёт строки от Влада, ключ намеренно не `рыщанов` - не смешивать с возможным планом выручки). Проверено изолированными Node-тестами (backend: bundle/AI-context на синтетике; frontend: 7 сценариев рендера - все 5 вкладок admin-предпросмотра, self-login через `ryschanow_summary`, пустые данные без плана) - цифры зарплаты сверены вручную, сходятся с формулой один в один, ни одного NaN/undefined. Живой вызов kie.ai не проверен (как и с любым новым промптом) - нужна обратная связь Влада по первой генерации. См. `plans/2026-08-25-ryschanow-personal-page.md`. |
| 291 | 2026-08-25 | **Личная страница Рыщанова, раунд 2 - убрана зарплата, добавлена воронка документов, Техника/Водители/Наёмная техника доведены до полной идентичности одноимённых страниц.** Влад сравнил v289 с существующими страницами (5 скриншотов) и дал два вида правок. Пивот по зарплате: "что касается расчета зарплаты, который я писал, то убери его с этой страницы и перенеси его туда, где у меня есть отдельная вкладка «Зарплаты». И туда ту же самую структуру, табличку, как и у меня в зарплатах, только по его отделу, включая его самого" - блок "Расчёт зарплаты" убран с личной страницы ЦЕЛИКОМ (не сокращён), т.к. вкладка "Зарплата" -> "Отдел Рыщанова" уже считает весь отдел включая его самого, доп. код не понадобился; заодно убран мёртвый код, служивший только зарплате (`byLogistAll`/`qualMarginAllLogists`/`specialTralsProfit`/`useNewRules`). Доводка до полноты: "под зарплатой добавь воронку документов" - новый блок `📋 Воронка документов` (тот же 2-строчный формат, что "Обзор заказов", источник `orders.doc_by_decade`/новое поле бэкенд-бандла `ryschanow_summary.doc_by_decade`); "Техника"/"Водители" были урезанными версиями - переписаны как точные копии логики одноимённых страниц "Парк"/"Водители" (12 колонок, клик-сортировка `setMyPageRyschVehSort` с ▲▼↕, фильтры по типу/статусу, `driverForecastStatus_`, новое поле бэкенда `driver_order_counts` через `getDriverOrderCounts_`); "Наёмная техника" была упрощённой - добавлена колонка "Затраты" + визуальная "Шкала маржи" + итоговая строка, как у Пруса. Своя параллельная реализация вкладок (общие DOM id "Техника"/"Водители" заняты статикой настоящих страниц), переиспользует общие хелперы (`vehicleTypeMatches_`/`driverForecastStatus_`/`fmtK2`). **Координация:** в процессе деплоя обнаружена параллельная сессия, независимо внёсшая в тот же день фикс кросс-отдельной ДЗ Ратникова (`DEBT_CROSS_DEPT_MANAGERS`, см. запись v290 выше) и напрямую закоммитившая его в `main` (`f354dae`) - синхронизировано в git отдельным коммитом ДО своих правок (протокол координации, CLAUDE.md), затем `git merge origin/main` (идентичный текст с обеих сторон backend-части - слияние прошло чисто, без конфликтов; фронтенд-часть фикса `buildDebtDocsHtml_`, которой не было в этой сессии, подтянулась слиянием). Проверено изолированными Node-тестами (те же 7 сценариев + сортировка/фильтры по госномеру и типу "Трал", self-login с новыми полями бандла, отсутствие текста "Расчёт зарплаты" во всех сценариях), `node --check` на бэкенде и синтаксическая проверка всех 8 `<script>`-блоков фронтенда. См. `plans/2026-08-25-ryschanow-personal-page.md` (раздел "Раунд 2"). |
| 292 | 2026-08-26 | **Личная страница Рыщанова, раунд 3 - откат "Техники", "Водители" -> статичная "Загрузка парка", найден и исправлен баг с планом ВП.** Влад: "Раздел техника убрать. Раздел водители переименовать в раздел Загрузка парка и статично без фильтров - только машины со статусом Загрузить срочно/Добавить заказов/Простой, приоритетом первым Загрузить срочно, потом Добавить заказов, простой - в самом низу." Вкладка "Техника" удалена целиком вместе со своим состоянием сортировки/фильтров (`myPageRyschVehSort`/`myPageRyschVehFilter*`). "Водители" -> "Загрузка парка": машины классифицируются `driverForecastStatus_` (тот же, что страница "Водители"/Васин), "Идёт по плану" и "Нет плана" не показываем, порядок групп - по приоритету действия (не по тяжести статуса). Заодно найден отдельный баг ("не появилась менюшка с зарплатами") - `buildLogistView_` не прокидывал `managerPlans` в `ordersOut` при самостоятельном входе (admin-предпросмотр видел `D.orders.managerPlans` напрямую, поэтому план был виден только ему) - добавлено `gp_plan` в `buildRyschanowSummaryBundle_`. Проверено изолированными Node-тестами (группировка/порядок/исключения, план виден в обоих сценариях входа). |
| 293 | 2026-08-26 | **Личная страница Рыщанова, раунд 4 - НАЙДЕН КОРЕНЬ БАГА "план не виден" (Влад повторил претензию в третий раз с новыми скриншотами).** Фикс v292 (gp_plan) оказался недостаточен - план по-прежнему показывал 0 ₽ на живом сайте. Диагностика: `getOrdersData()`/`getOrdersDataForPeriod()` при `USE_SERVER_ORDERS_CALC=on` возвращают ответ VPS-сервера НАПРЯМУЮ (`if (computed) return computed;`), минуя `joinManagerPlans_` (сверку с листом "Планы_менеджеров") ЦЕЛИКОМ. У сервера есть своя копия этой логики (`api/lib/orders-calc.js`, источник - MySQL `manager_plans`), синхронизируемая HTTP-мостиком через `action=export_manager_plans` в `doGet` + `import/import-manager-plans.js` на VPS (cron не настроен, запуск вручную). Проверка на сервере (`SELECT * FROM manager_plans WHERE month_key='2026-08'`) показала "Рыщанов" (0.00) БЕЗ "рыщанов_вп" - таблица не обновлялась с момента, когда `export_manager_plans` был убран из `doGet` (штатная уборка "временного" кода - комментарий в самом import-скрипте прямо это признаёт: "убрать после переноса") - `import-manager-plans.js` с тех пор молча падал на "Не авторизован" при каждом запуске (воспроизведено вручную по SSH), задело НЕ ТОЛЬКО Рыщанова, а любую правку плана ЛЮБОГО менеджера в течение месяца, если сервер-расчёт был активен. Исправлено навсегда, не патчем: лист "Планы_менеджеров" остаётся ЕДИНСТВЕННЫМ источником (Влад вводит вручную), сервер больше не должен вести свою копию - `getOrdersData`/`getOrdersDataForPeriod` теперь ВСЕГДА накладывают `joinManagerPlans_` поверх серверного ответа. HTTP-мостик и VPS-скрипт синхронизации оставлены как мёртвый код (вне рамок текущей правки, не трогал). Заодно по новым скриншотам: правило ">5%" для "Простой — разобраться" (Влад: "машина либо в глубоком ремонте, либо без водителя - не то, что решается заказами") - прогноз <=5% (ноль/минус) исключается из списка целиком; убраны 4 маленькие KPI-карточки ("вся компания") - дублировали итоговую строку "Наёмная техника"; новая вкладка "Топ логистов с несданными путевыми" - новая агрегация `by_logist_no_waybill` в `aggregateOrdersRows` (по `mgr_l` заказа, свой парк + наём в одну сумму на логиста). Проверено изолированными Node-тестами (правило >5%, отсутствие KPI-карточек, новая вкладка в обоих сценариях входа, регрессия по остальным вкладкам не задета). |
| VPS-only | 2026-08-26 | **"Топ логистов с несданными путевыми" показывал пустой список на живом сайте - `by_logist_no_waybill` не был портирован на сервер.** Влад: "Это ну ни как пустым быть не может. Нужно показать логистов на чьих заказах нет путевых" - вкладка (добавлена в записи v293 выше) показывала "Все путевые листы сданы ✅" вместо реальных данных. Корень - ТА ЖЕ ловушка, что уже дважды ловили (TRAL_MANAGERS/manager_plans): `logistNoWaybillMap`/`by_logist_no_waybill` были добавлены только в `scripts/full_script_final.js` (`aggregateOrdersRows`), а `USE_SERVER_ORDERS_CALC=on` значит Apps Script отдаёт `orders` СЕРВЕРА напрямую (`fetchOrdersComputedFromServer_`) - у поля не было аналога в `api/lib/orders-calc.js`, `orders.by_logist_no_waybill` был `undefined` -> `[]` на фронтенде. Портировано 1-в-1 на VPS (тот же `logistNoWaybillMap`, те же две точки инкремента - водители/поставщики найма по `mgr_l`), бэкап снят (`orders-calc.js.bak-20260826-141557`), `systemctl restart yard-api`. Проверено прямым curl'ом на `/api/orders_computed?period=2026-08` - 6 логистов с реальными числами (Васин 55/Прус-Роскошный 41/Сильчев 38/Махура 33/Суркова 30/Кан 28). Apps Script/git НЕ трогались - баг был только в серверной копии, исходный код `scripts/full_script_final.js` был верен с самого начала. |
| 295 + VPS | 2026-08-26 | **Топ логистов без путевых - добавлен %, топ пересортирован.** Влад: "так отлично, только давай выведем количество заказов сравним с поступлением путевых ведём % и расставим топ согласно % не сданных путевых". `logistNoWaybillMap` дополнен `orders_own`/`orders_hired` - та же популяция заказов, что числитель (`!isInternalOrder && !isServiceRow`), просто без условия `!hw` - считает ВСЕ заказы, где путёвка в принципе требуется. `by_logist_no_waybill` теперь отдаёт `orders_total`/`no_waybill_pct`, сортировка по проценту (не по абсолютному счётчику) с количеством как тай-брейкером. Фронтенд - новые колонки "Заказов"/"%", та же пороговая раскраска (≥50% красный/≥25% янтарный), что у соседних таблиц воронки документов. Портировано на VPS В ТОМ ЖЕ заходе, не откладывая (см. память `project_orders_calc_vps_sync_gotcha` - тот же класс бага уже трижды ловили отдельно). Проверено изолированным Node-тестом (колонки/раскраска/порядок строк) и прямым curl'ом на живом сервере - топ реально сместился: Прус-Роскошный (53%, 41 из 77) впереди Васина (30%, хоть у него абсолютных больше - 55 из 186), именно то расхождение, ради которого Влад просил %. |
| 296 + VPS | 2026-08-26 | **ИИ-задачи Рыщанова - делегировать логисту, не звонить/требовать самому.** Влад дал детальный разбор 4 сгенерированных задач: "Лучше если ему будет дана команда поставить кому-то из логистов задачу собрать путевые с водителей, допустим что на рейсах Крысало больше всего был Сильчев логист к примеру" / "тут лучше если Рустем ставит задачу логисту по наемной технике" / "тут вообще не надо рекомендовать Рустему работать с логистами, которые ведут наш парк, по поводу найма. Важно чтобы рекомендации руководителю были с позиции руководить, а не делать самому". Корень - у промпта не было данных, ЧТОБЫ рекомендовать делегирование: не было связи водитель/поставщик -> ответственный логист. Добавлено: `driverMap`/`supplierMap` дополнены `logCounts` (логист -> число заказов с этим водителем/поставщиком), `argmaxLogist_()` выбирает того, у кого больше всего - `primary_logist` на `by_driver_no_waybill`/`by_supplier_no_waybill`. `logists[]` в контексте ИИ дополнен `own_fleet_focus` (`isOwnTralLogistName_` - Сильчев/Кан/Махура заточены под собственный парк, не найм) - промпту явно запрещено строить задачи "обсудить ставки найма" вокруг них. Промпт переписан: ГЛАВНОЕ ПРАВИЛО - никогда "позвони/потребуй/свяжись лично", только "поставь задачу [primary_logist]..."/"спроси с [логиста]..." - с явным примером НЕПРАВИЛЬНО/ПРАВИЛЬНО на реальном кейсе Крысало/Сильчев. Портировано на VPS (`api/lib/orders-calc.js`) в ТОМ ЖЕ заходе (`logCounts`/`argmaxLogist_`/`primary_logist` 1-в-1), бэкап `orders-calc.js.bak-20260826-150225`, `systemctl restart yard-api`. Проверено изолированным тестом чистых функций и прямым curl'ом на живом сервере - Крысало Алексей -> Сильчев Максим, Рамазанов Александр -> Суркова Анастасия, ровно то, что Влад предугадал сам в своём примере. Живой вызов kie.ai не проверен - нужна обратная связь Влада по первой генерации с новым промптом. |
| 297 | 2026-08-27 | **Личные страницы Ахтамовой/Гусейновой - руководители коммерческих групп.** См. `plans/2026-08-26-ahtamova-guseinova-personal-pages.md`. Влад: "они должны отражать и задачи, план выполнения отдела, точно такая же табличка [группы менеджеров], блок с вкладками поступления (по менеджерам/вчера/сегодня/эта неделя)" + уточнение того же дня: ДЗ/Зарплата - отдельными пунктами меню, не вкладками (тот же принцип "Отдельная и единственная", что у Рыщанова). Бэкенд: `commercialHeadTeam_` (фамильная проверка), `buildManagerView_` расширен командным срезом (`team_by_manager`, ДЗ/Поступления фильтруются на всю команду вместо одной фамилии - регрессия для обычных менеджеров исключена), `buildCommercialHeadAiContext_`/`buildCommercialHeadAiTasksPrompt_` (командный ИИ-промпт с тем же принципом "с позиции руководителя", что закреплён для Рыщанова), `doGet` резолвит роль `commercial_head` по фамилии. Все новые поля - из уже существующих полей `aggregateOrdersRows` (`amount_thru_yesterday`/`today_new_*`, уже на VPS) - VPS деплой не требовался. Фронтенд: `DEPT_CFG`/`HEAD_FULL` вынесены в module scope из `renderSalesMgr` (поведение не изменилось, подтверждено байт-в-байт регрессионным тестом); `renderDeptGroupTableHtml_` - параллельная переиспользуемая версия `deptBlock`/`mgrRow`/`planBar` со страницы "По менеджерам", включая уже готовую дельту "▲+N к сегодня" и click-to-expand (`today_new_list`) - ничего нового в бэкенде не понадобилось, только скопирован рендер; `renderMyPageCommercialHead_`/`renderCommercialHeadReceiptsBlock_` - личная страница; `renderCommercialDeptCardHtml_`/`hiredBreakdownRow` вынесены в module scope из `renderSalesSalary` (тот же приём, что уже сделан для Рыщанова) - company-wide вкладка "Зарплата" переключена на них БЕЗ регрессии (идентичный HTML-вывод в изолированном тесте); два новых пункта меню (Зарплата/ДЗ), видны только самим руководителям/admin-предпросмотру "как они". Найден и исправлен ДО деплоя баг: `renderCommercialDeptCardHtml_` и `renderDeptGroupTableHtml_` ждут РАЗНЫЕ формы конфига отдела (`COMMERCIAL_DEPTS` vs `DEPT_CFG`) - изолированный тест поймал `undefined` вместо названия отдела на странице "Зарплата" до того, как это попало на прод. Проверено изолированными Node-тестами на каждом шаге (backend team-scoping, регрессия `renderSalesMgr`/`renderSalesSalary`, дельта+click-to-expand, personal page admin/self-login, standalone Зарплата/ДЗ страницы). Живой вызов kie.ai не проверен. |
| 298 | 2026-08-27 | **Фикс: `cleanName`/`cleanManagerName_` обрезали 4-словные отчества (Гусейнова "...кзы").** Влад тестировал личную страницу Гусейновой ("Смотреть как сотрудник") - в заголовке/сайдбаре имя полное "Гусейнова Айнур Рафик кзы" (4 слова), а в табличке отдела (`renderDeptGroupTableHtml_` -> `cleanName(m.name)` для строки руководителя) обрезалось до "Гусейнова Айнур Рафик" (3 слова, без "кзы"). Влад: "ну хрен знает где с кзы где без кзы. Ты сам разберись с этим что бы всё работало". Причина - жёсткий `.slice(0,3)` из правила 2026-07-08 ("фамилия имя отчество, и всё, больше ничего не надо"), не учитывавшего среднеазиатские отчества из ДВУХ слов ("кзы"/"кызы" = "дочь", реальное легальное ФИО из 4 слов). Фикс в обеих копиях (`cleanName` frontend `files/index.html:5556`, `cleanManagerName_` backend `scripts/full_script_final.js:1768`): 4-е слово оставляем, только если оно состоит из букв (не цифровой мусор 1С, не попавший под уже существующий regex телефона) - легитимные многословные отчества проходят, случайный мусор - нет. Аудит всех вызовов `cleanManagerName_` (4 сайта) и всех `slice(0,3)`-паттернов в обоих файлах подтвердил: везде используется только для ОТОБРАЖЕНИЯ или как значение в карте "клиент->менеджер" (не как ключ фильтра/поиска - ключи фильтров в UI строятся на СЫРОМ, необрезанном имени), значит расширение вывода на 4-е слово нигде не ломает сопоставление/группировку. Проверено изолированным Node-тестом (регрессия на стандартных 3-словных именах +/- телефон, 2-словное имя + цифровой мусор, пустая строка - без изменений старого поведения; новый кейс "Гусейнова Айнур Рафик кзы" +/- телефон - теперь не обрезается). VPS не тронут (чисто отображение, не затрагивает `aggregateOrdersRows`). |
| — (только фронтенд) | 2026-08-27 | **Фикс: сайдбар обычной админ-панели показывал "Зарплата" трижды и ДЗ дважды.** Влад со скриншотом своего сайдбара (не режим "Смотреть как"): "на моей странице 3 раза зарплата... мне нужна только одна такая вкладка со всеми сотрудниками как и было, а то видимо артефакт после наших доработок по другим ролям" - верно, `applyRoleUI()` в ветке "обычный админ" (`files/index.html`) безусловно ставил `display=''` на ВСЕ `.sidebar-nav-item`, включая три персональных пункта (`ryschanow-salary`, `commercial-head-salary`, `commercial-head-debt`), которые в HTML по умолчанию `display:none` и должны показываться ТОЛЬКО в режиме предпросмотра "Смотреть как [Рыщанов/Ахтамова/Гусейнова]" (эта логика - в соседней ветке `if`, не в этой). Фикс - явный список `personalOnlyPages`, принудительно скрываемый именно в ветке "обычный админ". Регрессии нет: ветка предпросмотра не тронута, там свои условия `isRyschanowViewer`/`isCommercialHeadViewer` уже работали верно (это и было видно на предыдущем скриншоте с "Смотреть как Гусейнова" - там всё было по одному разу). |
| 299 (фронтенд) + VPS | 2026-08-28 | **Фикс: БАЗА ДМД полностью исключена из всех метрик - личная перевозка собственника.** Влад: "в редких случаях бывает так, что строчки с этим контрагентом имеют выручку... есть и стоимость наёмной техники. Это личная перевозка собственника, поэтому она не должна учитываться ни в зарплатах, ни в валовой прибыли, ни в выручке. Будто у нас не было этой перевозки". По ходу разбора найден более широкий класс бага: строки с наймом (общий KPI "Маржа найма", по менеджеру, по логисту) вообще не проверяли флаг `internal` - только коммерческая выручка/разбивка по клиентам это делали. Влад подтвердил: остальные internal-клиенты (ТЕХНОПАРК, МЕГАКРАН и т.п.) НЕ трогать - их поведение (учитываются как факт хозяйственной деятельности, не как коммерческая выручка) осталось прежним. Решение: новый список `FULLY_EXCLUDED_CLIENTS` (НЕ то же самое, что `INTERNAL_CLIENTS` - другая семантика), строка пропускается (`continue`) в самом начале цикла `aggregateOrdersRows`, до `totalOrders++` и любых счётчиков - "как будто заказа не было". БАЗА ДМД убрана из старого `INTERNAL_CLIENTS` (дублирование смысла было бы путаницей). Сверено на реальных данных августа: 4 строки БАЗА ДМД, включая заказ №473444 (выручка 750 000, наём у ТЛК-ЦЕНТР 725 000, маржа 25 000 = 3.3% - ровно тот случай, что описал Влад). До/после: `total_amount` -750000, `total_orders` -4, `hired_profit` -25000, `total_hired_cost` -725000; затронут ТОЛЬКО Прус-Роскошный (менеджер и логист по этому заказу), остальные 10 менеджеров и 5 логистов не сдвинулись ни на рубль. Исправлено в обеих копиях: `scripts/full_script_final.js` (v299, redeploy) и `api/lib/orders-calc.js` (VPS, задеплоено, перезапущено, проверено живым `/api/orders_computed`). |

## Версия 300 (2026-08-29) - amount_week: выручка менеджера за неделю

Влад попросил вторую цифру на плитках личной страницы («выручка за неделю ниже,
поступления - неделя ниже»). Поступления за неделю уже были на клиенте
(D.receipts.week), выручки за неделю в данных НЕ БЫЛО - добавлено новое поле в
расчёт заказов.

- `amount_week` в `aggregateOrdersRows`: сумма по строкам менеджера с начала
  недели. Неделя - ОТ ПОНЕДЕЛЬНИКА, то же определение, что у receipts.week,
  иначе две «недели» на одной плитке считались бы за разные отрезки.
- Правка аддитивная: существующие суммы и формулы не тронуты.
- Обе копии обновлены одновременно (правило project_orders_calc_vps_sync_gotcha):
  Apps Script (версия 300, redeploy живого /exec) и VPS `api/lib/orders-calc.js`
  (бэкап orders-calc.js.bak-20260829-0827, systemctl restart yard-api).
- Проверено на живом ответе сервера: Цегельников месяц 12 567 937 / неделя
  1 419 000; Ахтамова 11 700 896 / 4 389 809; Савиток 10 274 200 / 1 662 440.

## Версия 301 (2026-08-29) - amount_yesterday + живое поступление в плитку менеджера

Влад: «добавь ещё за вчера у выручки третьим слоем; в поступления - сегодня; и
когда поступление опознано за менеджером, оно падает в эту ячейку со звуком, как
на главной. Звуки в зависимости от суммы, как утвердили. Я не должен напоминать».

- `amount_yesterday` в расчёт заказов (обе копии, как и amount_week): выручка
  строго ЗА ВЧЕРА, не накопительно. Apps Script v301 + VPS (бэкап
  orders-calc.js.bak-yday-...). Проверено на живых: Ахтамова вчера 1 020 177,
  Савиток 605 680.
- Плитки личной страницы стали трёхслойными: Выручка = месяц/неделя/вчера,
  Поступления = месяц/неделя/сегодня. «Сегодня» - ЖИВОЙ слой: 1С-цифра дня плюс
  банковские платёжки, опознанные за этим менеджером (matched_manager).
- Победная сцена банковского поступления получила новую ЦЕЛЬ - плитку
  «Поступления» личной страницы (высший приоритет: она видна всегда, пока
  открыта страница, в отличие от карточки на вкладке). Празднуются ТОЛЬКО свои
  платёжки; чужие тихо обновляют цифры без сцены. Градация фанфар по сумме
  (win/win2/win3/win4) переиспользована как есть - ничего не переписывалось.
- Проверено имитацией: чужая платёжка 900 тыс -> 0 сцен и 0 звуков; своя 150 тыс
  -> уровень 2 «Крупный клиент», одометр к 380 000; своя 1,2 млн -> уровень 4
  «Миллионник».

## 2026-08-29 - страница старшего менеджера «Кабина группы» (fc175e4, Apps Script v302)

Влад по превью: «Да, неплохо, запускаю в работу, только избегай вот этого
"худший". Не надо в дебиторке: убери "худший"».

- Страница руководителя коммерческой группы (Ахтамова/Гусейнова) переведена на ту
  же архитектуру, что личная страница менеджера: строка состояния с отставанием в
  ДНЯХ по отделу -> ярус-сетка «план + три плитки / задачи + Моя группа» -> нижний
  блок из 4 вкладок. Курсорный свет - как требует ГОСТ (раздел 7).
- «Моя группа» стоит там, где у менеджера расчёт зарплаты (зарплата руководителя
  живёт на отдельной странице меню, не дублируется). Сортировка по отставанию.
  Оценочных слов в интерфейсе НЕТ - только факт «отставание N дней».
- Вкладки: Менеджеры группы (план/факт/%/прогноз/отставание) · Поступления
  (существующий блок, не трогали) · Дебиторка группы (то же ядро
  computeDebtAggregates_, плюс колонка «чей клиент») · Документы (новая).
- Бэкенд: `orders.team_by_manager_detail` - ТОЛЬКО счётчики воронки документов по
  членам группы, без списков сделок (приватность: у кого проседает оформление -
  видно, чужая клиентская база - нет). Apps Script v302, redeploy на живой /exec.
  ВАЖНО: это поле собирает `buildManagerView_` (Apps Script), не VPS-расчёт -
  портировать в `api/lib/orders-calc.js` НЕ нужно, там его источника нет.
- Найдено на живой проверке и исправлено: «Отдел: Факт» -> «Отдел: факт»; подвал
  группы называл недоборы одним числом («не хватает 7,4 млн, из них 7,5 млн») -
  сумма личных недоборов бывает БОЛЬШЕ общего разрыва, потому что опережающие
  часть перекрывают; теперь поимённо с суммой каждого. Пустые корзины в легенде
  дебиторки скрыты.
- Удалён осиротевший `renderDeptGroupTableHtml_` + её тоггл (178 строк) - страница
  перешла на `renderCommercialHeadTeamBlock_`, других вызовов не осталось.
- Слито с параллельной работой другого чата по калькулятору (438185a: флаги на
  карте, тарифная сетка кат.3, перетаскивание точек) - авто-merge без конфликтов.

## 2026-08-29 - сортировка врала направление (945e8fc), фильтры дебиторки группы

Влад: «не нажимается кнопка на диапазонах... нужны фильтрыки где баланс, дни
просрочки... обратил внимание, что дебиторка не работает, возможно из-за 3D».

3D-эффект оказался ни при чём (проверено: элементы не перекрыты, клики доходят).
Настоящая причина - компаратор `(bv - av) * dir` во ВСЕХ числовых колонках:
выражение `(bv-av)` само по себе убывание, `dir=-1` переворачивало его в
возрастание, а `▼` рисуется именно при `dir=-1`. Итог на бою: «Дебиторка»
открывалась с самыми МЕЛКИМИ долгами сверху, «Техника» - с самыми убыточными
машинами. Код работал, врала подпись - глазами не ловится.

Прочёсан класс целиком (4 места): `debtSort`, `vehSort`, `clientsTopSort`
(там дефолт был подогнан под перевёрнутую формулу - комментарий «dir=1 по числовым
это убывание» жил как норма), `headDebtSort`. Эталон верной записи уже был в файле -
`dynSort: dir * (av - bv)`. Проверено численно по каждой колонке.

ВНИМАНИЕ на будущее: дефолтный порядок «Техники» теперь развернулся - сверху
прибыльные машины, а не убыточные. Это следствие честного знака, не отдельная
правка; если Владу нужен обратный дефолт - менять `vehSort` инициализацию, а не
формулу.

Плюс вкладка «Дебиторка группы»: чипы диапазонов стали рабочими фильтрами (мульти-
выбор, сброс, счётчик «показано N из M на сумму X»), таблица получила сортировку по
колонкам. Обрезка «40 крупнейших» убрана - с сортировкой она врала.

ГОСТ: раздел 4 дополнен каноном «Сортируемая таблица», в чек-лист приёмки добавлены
численная проверка сортировки и проверка, что чипы реально фильтруют.

## 2026-08-29 (вечер) - страница руководителя в один экран + жёсткая физика hover (f7f5721)

Три правки одной сессии, все по прямым указаниям Влада.

**1. Страница старшего менеджера - один экран.** Нижний блок вкладок (Менеджеры
группы / Поступления / Дебиторка / Документы) убран целиком: «этот нижний блок у
старших менеджеров вообще убрать». Осталось строка состояния -> ярус (план, три
плитки, задачи, «Моя группа»). Плитки уводят: Поступления -> своя страница меню,
Дебиторка -> своя, Выручка отдела -> подводит к «Моей группе» на этой же странице.
Ghost-кнопка «Пропавшие клиенты» осталась строкой (разовое действие, не вкладка).
Новое: пункт меню + страница `commercial-head-receipts`. Поле бэкенда
`team_by_manager_detail` (Apps Script v302) откачено - вкладки «Документы» больше
нет, потребителя тоже. ЕСЛИ понадобится страница «Документы группы» - поле придётся
вернуть, оно собиралось в buildManagerView_.

**2. Дебиторка руководителя получила фильтры.** Чипы диапазонов подсвечиваются,
есть сброс и счётчик «показано N из M на сумму X», добавлены лестница по возрасту и
сортировка по колонкам.

ОТДЕЛЬНО, чтобы не повторилось: дефолт фильтра ['0-30','30-60','60-90'] я в этой
сессии «исправил» на пустой (показывать всё), сочтя спрятанную просрочку 90+ багом.
Влад откатил: «это я специально сделал, так и должно быть» - руководитель при входе
видит рабочую, взыскиваемую часть долга, а 90+ и мёртвая включаются чипами. Дефолт
вернут, в коде стоит предупреждение НЕ ТРОГАТЬ. Расхождение с общей «Дебиторкой»
(там пустой набор) - тоже намеренное, другой пользователь и другая задача.

**3. Физика hover зафиксирована жёстко** («на главной умеренное движение и небольшой
вылет, а на других она скачет, прыгает и сильно вылетает»). Замерено на стенде:
- perspective на СТРАНИЦЕ давал плитке вверху экрана сдвиг -10,2px по Y, такой же
  плитке внизу +5,8px. На СЕТКЕ: -2,05px и -2,05px. Поэтому perspective:1400px
  переехал со .page на сетки плиток (.kpi-grid/.grid-2/.p2-tier3-grid/.mp-tier/
  .mp-stats); preserve-3d там больше не нужен.
- translateZ(22px) масштабирует на +1,6%: плитка 242px растёт на +4,3px (эталон
  главной), карточка 1004px - на +16,5px. Поэтому вылет теперь ТОЛЬКО у плиток.
Два уровня, третьего нет: ПЛИТКА (.kpi/.mp-stat) - translateY(-1px) translateZ(22px);
ПОВЕРХНОСТЬ (.card/.mp-status) - только translateY(-1px); ПОВЕРХНОСТЬ С ТАБЛИЦЕЙ -
без движения (запрет ГОСТа №17). Убраны расхождения: .mp-stat имел 18px вместо 22,
.mp-status (полоса во всю ширину) вылетала как плитка.

НОВАЯ СЕТКА ПЛИТОК = добавить её класс в список носителей perspective, иначе её
плитки не вылетают вовсе. Записано в ГОСТ (раздел 4, таблица двух уровней с числами).

После этого выката на устройствах Влада нужен Ctrl+Shift+R: физика живёт в CSS
внутри index.html, а страница кэшируется прокси на 10 минут плюс браузером.

## 2026-08-29 (поздний вечер) - нижний блок руководителя вернулся другим составом (Apps Script v303)

Влад передумал в пределах часа, это нормальный ход работы - фиксирую итоговое
состояние, а не промежуточные шаги.

**Итог по странице руководителя группы:**
- ярус (план / три плитки / задачи / «Моя группа») - как было;
- нижний блок ВЕРНУЛСЯ, но другим составом: «Заказчики группы» / «Поступления» /
  «Воронка документов». Дебиторка в блок НЕ вернулась - осталась отдельным пунктом
  меню, как Влад решил в том же разговоре;
- отдельная страница меню «Поступления» (создана часом раньше) УДАЛЕНА - поступления
  снова живут вкладкой, держать два одинаковых экрана незачем;
- плитки: Выручка отдела -> вкладка «Заказчики группы», Поступления -> вкладка
  «Поступления», Дебиторка -> отдельная страница меню.

**Новая вкладка «Заказчики группы»** (Влад: «кто сколько, какой заказчик у какого
менеджера принёс сколько выручки»): строки - топ-10 заказчиков КАЖДОГО менеджера
группы, склеенные в один список, с долей в выручке группы, числом заказов и
неоплаченным остатком. Сортировка по любой колонке. Заказчик, который есть у двух
менеджеров сразу, помечается «общий» - это не дубль-ошибка, а подсветка нарушения
правила «один клиент = один ответственный» (CRM_ARCHITECTURE.md).

**Бэкенд v303:** `team_by_manager_detail` вернулось и расширено - теперь по каждому
члену группы отдаётся `{ doc, top_customers }` (было только `doc`, потом откачено
совсем). Списки сделок/пропавших/должников намеренно НЕ отдаём: этим двум вкладкам
они не нужны, лишние ПДн в ответе тоже.

**Откат моей самодеятельности:** дефолт фильтра ДЗ руководителя вернулся на
['0-30','30-60','60-90'] - Влад: «это я специально сделал, так и должно быть».
В коде стоит явная пометка НЕ ТРОГАТЬ, чтобы это не «чинили» снова.

## 2026-08-29 - правки нижнего блока руководителя (Apps Script v304)

Влад по каждой вкладке:

**Заказчики группы** - колонка «Не оплачено» УБРАНА: «не понимаю, откуда там эти
сумасшедшие цифры». Причина цифр: `balance` в `top_customers` - накопительный
остаток из 1С по ВСЕМ заказам клиента за всю историю, а не долг за показанный месяц;
рядом с месячной выручкой он и читался как бессмыслица. Это тот же класс, что уже
записан в памяти как «total_balance раздут». Долги смотреть на своей странице
«Дебиторская задолженность», где считаются по своей формуле (amount-payment).

**Поступления** - открываются на «Эта неделя» и получили вкладку «Месяц» (было
сегодня/вчера/неделя). Состояние периода у руководителя теперь СВОЁ
(`chReceiptsPeriod`), отдельное от менеджерского `myPageReceiptsPeriod` - у
менеджера дефолт «Сегодня» не менялся, у них разные вопросы к этой вкладке.

**Воронка документов** - теперь за ДВА последних месяца. Текущий уже загружен,
прошлый догружается фоново (`loadChDocsPrevIfNeeded_`, переиспользует
`myPagePeriodCache`, если период уже открывали переключателем - тогда бесплатно).
Сверху плашка-акцент с тремя честными состояниями: «за два месяца: июль и август» /
«пока только август - догружаем июль…» / «только август - прошлый месяц не
загрузился (причина)». Молчать и выдавать один месяц за два - нельзя.
Ниже добавлен блок «Проблемные документы» - тот же формат и та же строка-шаблон
`problemOrderRowHtml_`, что «Обзор заказов → Проблемные заказы» и вкладка менеджера,
с фильтрами по статусу (своё состояние `chProblemsFilter`), тоже за два месяца.

**Бэкенд v304:** `problem_orders` для руководителя группы теперь фильтруется по ВСЕЙ
его команде (`commercialHeadTeam_`), а не только на него самого. Обычный менеджер
по-прежнему видит только свои - приватность не изменилась.

## 2026-08-29 - Этап 0 плана загрузки без блокировок (Apps Script v305 + files/index.html)

По plans/2026-08-28-no-blocking-loading.md, после исследования 4 агентов на Fable 5.
Первая, "ничего не ломающая" стадия плана - до неё коммит 67ad783 (план v2 записан,
код не тронут).

**0.1 Честный таймстемп** - D.updated (время СБОРКИ ответа doGet, не время данных)
убран из ДВУХ мест отображения свежести: nav-updated в сайдбаре и "· данные 1С
HH:MM" в шапке страницы. Оба теперь берут data_age_min из /api/import_status
(сервер уже считает max minutes_since_ok по import-orders-raw/import-debt - честный
"когда данные реально обновились в последний раз", не "когда сервер последний раз
опрашивал папку"). Найдено по ходу ТРЕТЬЕ место того же класса: карточка Штатки на
Панели ("обн. HH:MM") тоже читала D.updated - readMainPayloadCache_ теперь отдаёт
реальное время записи кэша (row[1] всегда писался в saveMainPayloadCache_, но
никогда не читался) как новое поле D.fleet_cache_at.

**0.2 Кэш getRevenueDateComparison_** (CacheService 6ч, как у соседнего prev_year в
той же функции) - горячая точка №1 замера 29.08: без кэша читал ВЕСЬ лист архива
прошлого месяца при каждом заходе на дашборд, ~4-5 из типичных 12 секунд загрузки.

**0.3 Три мины из аудита закрыты:** try/finally в renderSalesTabsForPeriod (без
него исключение навсегда оставляло бы весь дашборд на цифрах чужого периода);
гварды в renderMain/renderMgrList (падали на TypeError при частичном D); двойной
клик по "Обновить данные" теперь заблокирован (loadDataInFlight_) с disabled+
"Обновляю..." на кнопке через try/finally.

**0.4** fetchReceiptsFromServer_ теперь перерисовывает обе секции, читающие
D.receipts (renderReceipts + renderMainReceiptsSummary), не только страницу
"Поступления".

**Проверка:** node --check на full_script_final.js + new Function() на все 3
inline script-блока index.html - синтаксис чистый. Живой браузерный прогон не
делал (file:// заблокирован в Browser pane сессии) - положился на статическую
проверку и построчный трейс логики каждого изменения. После редеплоя live /exec
ответил корректным JSON ({"needLogin":true} на неавторизованный запрос) - скрипт
не падает на рантайме.

**Деплой:** frontend - git push прямо в main (67ad783 -> 8982a99), без конфликтов
(origin/main совпадал с HEAD перед пушем). Backend - clasp pull перед push (живой
редактор совпадал с git HEAD 1-в-1, чужих правок не было) -> push -> version 305 ->
redeploy того же deployment ID, что в API_URL_DEFAULT.

Дальше по плану - Этап 1 (applyData/тихое обновление), после того как Этап 0
поживёт на проде.

## 2026-08-29 - Этап 1 плана загрузки без блокировок (files/index.html, без Apps Script)

Продолжение Этапа 0 (v305, запись выше). Только фронтенд - Apps Script не менялся.

**applyData(patch, opts)** - единая точка записи в D. loadData() больше не пишет в D
напрямую до проверки needLogin/error (временная переменная fetched) - битый ответ
больше не может затереть старый рабочий D до самого throw. Хэш-сравнение
(JSON.stringify патча против прошлого применённого) с опцией skipIfUnchanged - готово
для Этапа 3 (поллинг) и Этапа 4 (снапшот), сегодня почти всегда "меняется", т.к. ответ
Apps Script не идентичен байт-в-байт между запросами.

**Лоадер на весь экран - только на первый вход.** Повторная загрузка (кнопка/фон)
показывает только disabled+"Обновляю..." на кнопке (Этап 0.3) - страница остаётся
интерактивной. Это прямой ответ на исходный запрос Влада ("хочу работать с
интерфейсом, пока идёт тихая загрузка").

**showPage вынесен из безусловного вызова в applyRoleUI** - менеджер/логист/admin-
предпросмотр переключаются на "my-page" только если ТЕКУЩАЯ активная вкладка не
разрешена роли. Раньше вызывалось на КАЖДЫЙ render() - тихое обновление выкидывало бы
человека с "Плана задания"/"Калькулятора" обратно на личную страницу.

**Ошибка повторной загрузки** - остаёмся на старых данных, тост (p2ToastErr) вместо
баннера на всю ширину; баннер error-block остался только для первого захода.

**НЕ добавлено осознанно:** спиннер на кнопке (новый визуальный паттерн - нужно
одобрение Влада по ГОСТу, пока текстового "Обновляю..." достаточно); звук на
автообновлении не менялся - решение "оставить как есть" было в самом плане.

Дальше по плану - Этап 2 (ленивый рендер по вкладкам).

## 2026-08-29 - Этап 2 плана загрузки без блокировок (files/index.html, без Apps Script)

Продолжение Этапов 0-1 (записи выше). Только фронтенд.

**Ленивый рендер по вкладкам.** render() больше не вызывает все 9 render-функций на
каждое обновление данных - только группу ТЕКУЩЕЙ активной страницы (RENDER_GROUPS_:
main/vehicles/drivers/sales-квартет/myPage/debt/receipts). Остальные помечаются
"грязными" (dirtyGroups_) и дорисовываются в showPage() при реальном переключении.
Прецедент такого подхода уже был в коде (dynamics/clients/crm/global-stats/order-plan) -
применено к оставшимся секциям.

**Снята "скрытая связка звука"** (мина, названная в самом плане 2026-08-28): p2CurDebtTotal_
раньше писался ПОБОЧНЫМ ЭФФЕКТОМ renderDebtMiniCard - при ленивом рендере "Панель" не
каждый раз перерисовывается, звук/одометр ДЗ мог бы читать устаревшее значение. Формула
суммы ДЗ 0-90 дней вынесена в calcDebtTotal90_(debt) - используется и картой на Панели,
и p2DataSoundCheck_ напрямую из D, независимо от того, что реально нарисовано в цикле.

**НЕ добавлено:** честные состояния "загружается" вместо тихих нулей для ~16 B-блоков
(вторая половина Этапа 2 по плану) - новый визуальный паттерн (скелетоны), нужно
одобрение Влада перед записью в DESIGN_SYSTEM.md.

Проверка: node --check синтаксиса + подтверждение на живом yardhub.ru (cache-bust query,
маркеры RENDER_GROUPS_/dirtyGroups_/calcDebtTotal90_ найдены после деплоя).

Дальше по плану - Этап 3 (поллинг горячих цифр вместо кнопки как основного UX).

## 2026-08-29 - переключатель тёмной/светлой темы, Этапы 1-3 (files/index.html)

По plans/2026-08-29-light-theme-toggle.md (исследование - 2 агента на Fable 5,
референс - превью "Планировка ЯРД"). Запрос Влада: "чтобы вся биосистема наша могла
переключаться... пользователь у себя мог выбрать тёмную или светлую тему". Ручной
переключатель (выбор Влада), НЕ системная тема.

**Что сделано:** кнопка "Тема" в футере сайдбара (механика звукового тумблера);
:root[data-theme="light"] с палитрой из "Планировки"; ранний анти-FOUC скрипт в
<head>; applyTheme() инвалидирует запечённые цвета через ленивый рендер (Этап 2
плана загрузки пригодился сразу же). Токенизация: --ink (85 белых альф с
сохранением всех значений), --tint-* (осветлённые акценты, тёмные значения = старые
hex - тёмная тема не изменилась ни на пиксель), --bg-hover, --border-strong теперь
объявлен, SVG-вырезы #17171d -> var(--bg2). Chart.js: 11 графиков читают
тики/сетки/легенды/тултип через chartMuted_/chartGrid_/chartText_ (вычисленные
токены в момент создания). Канвас-салют перестал быть невидимым на светлом.

**Проверка:** живой прогон на localhost (порт 3101, временный) - переключение
туда-обратно, все токены флипаются, localStorage пишется, перезагрузка со светлой
темой без мигания тёмным, консоль чистая. Рендеры с данными на localhost не
проверить (нет входа) - прогон по живому за Владом в обеих темах на каждой странице.

**Осталось (Этапы 4-5 плана):** meta theme-color из applyTheme, решение по кнопке
Google Sign-In, правки DESIGN_SYSTEM.md (раздел 1 "тёмная, единственная" и ещё 9
мест - точные строки в плане) - ПОСЛЕ того как Влад пощупает на живом.

## 2026-08-29 - светлая тема v2: усилен контраст по фидбэку (files/index.html)

Влад пощупал светлую тему на реальных данных: "тёмная тема - всё под неё
заточивалось и цветёт, светлая слишком светлая... какой-то блёклый". Показал 3
варианта палитры прямо в чате (текущая / тёплая бумага+глубже / тот же холодный
тон но резче и сочнее) - выбран третий; предложенный ещё более контрастный 4-й
вариант отклонён явно ("этот еще контраст не берём").

Углублены bg/bg3/bg4, усилен border/border-strong, text/muted темнее и плотнее,
--tint-* насыщеннее (тёмная тема не тронута - меняется только светлая половина
пар). Бейджи/алерты/error-block получили точечные override на более сильную
альфу заливки под :root[data-theme="light"] - были на одной альфе для обеих
тем, на белом читались слабее.

Проверено на localhost - токены применяются, синтаксис/CSS-скобки чистые.

## 2026-08-30 - fleet_*: сцепка/расцепка/пересадка (Фаза 2) + реестр «Водители» (d37461a)

Влад: «пришло время реализовать сцепку/расцепку и пересадку водителей - не
хардкодить список, а создать систему ввода и хранения»; затем, увидев прогресс:
«Водители и Техника сейчас информационные - начнём с Водителей, вместо таблицы -
база данных с созданием/удалением/хранением».

Дословно Фаза 2 плана Планировки (plans/2026-08-29-logistics-planning-board.md) -
полная спецификация дописана туда же, не отдельным документом. Три новые таблицы
на VPS: `fleet_trailers` (реестр прицепов), `fleet_drivers` (реестр водителей -
такого реестра не было НИГДЕ в дашборде), `fleet_assignments` (сами события сцепки/
пересадки - абсолютный MSK DATETIME, не относительные часы от эпохи месяца, как у
plan_segs - сцепка живёт неделями/месяцами). Тягач НЕ заведён отдельной таблицей -
живой из D.vehicles, то же решение, что у plan_segs.

Эндпоинты (api.yardhub.ru, checkSession): GET /api/fleet/state (снимок целиком),
POST /api/fleet/couple (АТОМАРНАЯ транзакция с SELECT...FOR UPDATE - закрывает
старую запись ресурса где бы он ни стоял + старую запись слота на этом тягаче,
вставляет новую; конфликт "ресурс на двух тягачах" решается физической
невозможностью дублирования, не проверкой-после-факта), POST /api/fleet/uncouple,
POST /api/fleet/assignment_delete (точный id), POST /api/fleet/trailer,
POST /api/fleet/driver (upsert реестров), POST /api/fleet/driver_delete,
POST /api/fleet/trailer_delete (блокируют удаление ресурса, который СЕЙЧАС активно
сцеплен - 409 с причиной, не молча).

Смоук-тест форменным session_token (та же HMAC-подпись без обрезки base64-паддинга,
что и verifySessionToken): 401 без авторизации, upsert с кириллицей, couple с
переносом между тягачами (закрывает старую автоматически), couple с вытеснением
занятого слота, uncouple, delete-guard на активной сцепке (409 -> расцепить ->
удаление проходит), 400 на неизвестный kind/недостающие поля, точное удаление по
id - всё прошло, тестовые записи удалены, база чистая. Бэкапы:
api/server.js.bak-20260830-fleet, .bak-20260830-fleet-delete. Схема -
import/fleet-assignments-schema.sql.

**Фронтенд - страница "Водители" стала первым потребителем fleet_drivers**
(files/index.html, коммит d37461a): раньше показывала план/факт/%/прогноз по
машине с водителем - эта таблица ПЕРЕЕХАЛА в "Динамику" (та же renderDrivers(),
только контейнер и точка вызова из renderDynamics() - логика не менялась).
"Водители" стала реестром людей: список/поиск/добавление/правка/удаление, drawer
канона .mp-drawer (тот же, что "Пропавшие клиенты"), формы в стиле "Плана задания"
(formRow_). Удаление - через confirm() (ГОСТ запрет №16 - допустим только для
необратимых действий).

Проверено в браузере (моки сети): пустой список -> добавление -> ошибка на пустое
ФИО -> upsert не плодит дубль при правке -> поиск фильтрует -> удаление ->
XSS-экранирование имени (pageEsc_) -> emoji нет.

Найдена и ЗАДОКУМЕНТИРОВАНА, не починена в этом заходе (pre-existing в каноне
.mp-drawer, не в новом коде, не задевает реального пользователя): гонка таймингов
при программном close() менее чем через 40мс после open() - см. память
project_mp_drawer_open_close_race. Чинить одним заходом для ВСЕХ мест канона,
если когда-нибудь проявится как реальная жалоба.

**Следующее в очереди (не начато):**
- "Техника" - та же информационная -> инструментальная переделка, что "Водители"
  (Влад назвал обе страницы, начали с Водителей по его явной просьбе)
- Подполосы прицепа/водителя на ленте "Планировки" - визуальное использование
  fleet_assignments (drag между строками = сцепка/пересадка), задел уже готов
  на сервере, фронтенд не начат

## 2026-08-30 - «Справочники»: кардинальный разворот, заменяет узкие fleet_drivers/fleet_trailers (4127799)

Влад, тем же вечером, увидев прогресс "Водителей": «мы сейчас делаем кардинальный
разворот и будем делать всё по уму. Значит, делаем раздел Справочники. В этом
разделе будут создаваться все сущности. Физические лица, юридические лица,
материалка. Уже от физических лиц пойдут: водитель, менеджер, механик... Юридические
лица: это наш ЯРД-Империал, Бульдог, Технопарк и так далее. Материалка: это,
например, какой-то тягач или полуприцеп». Следом: «раздел водителей вообще убери.
Там, где сейчас у нас поступления, должны быть справочники... Поступление: перенеси
в раздел продажи. И начинаем заново работу с нуля».

**Backend (VPS)** - новая схема `import/sprav-schema.sql`, ЗАМЕНЯЕТ `fleet_trailers`/
`fleet_drivers` того же дня (в обеих не было ни одной реальной строки - проверено
`COUNT(*)` перед DROP TABLE - миграция не нужна, это был лучший момент для
перестройки). Три таблицы: `sprav_people` (физлица, с полями водительских
документов - только у роли "водитель" сегодня есть специфичные поля, заводить
EAV ради одной роли - overkill), `sprav_people_roles` (многие-ко-многим:
driver/manager/mechanic, человек может совмещать), `sprav_legal_entities` (СВОИ
юрлица группы, не контрагенты). `fleet_assignments` (сцепка/пересадка) НЕ
менялась - `resource_key` ссылается на `sprav_people.id`/`sprav_assets.id` тем же
опорным способом, что раньше на `fleet_drivers`/`fleet_trailers` - вся конструкция
пережила замену реестров БЕЗ единого изменения схемы сцепки, что и было целью
generic `kind`/`resource_key` схемы с самого начала.

Эндпоинты: `GET /api/sprav/state` (снимок целиком, с ролями, подтянутыми join'ом),
`POST /api/sprav/person` (транзакционный upsert + полная пересборка ролей -
проще и честнее частичного PATCH, форма всегда шлёт полный набор галочек),
`POST /api/sprav/person_delete` (409, если человек сейчас сцеплен с тягачом),
`POST /api/sprav/legal_entity`, `POST /api/sprav/legal_entity_delete` (409, если
на юрлице числится техника), `POST /api/sprav/asset`, `POST /api/sprav/asset_delete`
(409, если сцеплен). Старые `/api/fleet/{driver,driver_delete,trailer,
trailer_delete}` УДАЛЕНЫ (curl подтвердил 404), `GET /api/fleet/state` обрезан до
`{assignments, max_updated}` - реестры теперь только в `/api/sprav/state`, разделение
ответственности "справочник" vs "транзакционные события сцепки". Смоук-тест
ролей/юрлица/удаления с 409-гардами - пройден, тестовые данные вычищены. Бэкапы:
`api/server.js.bak-20260830-sprav`, `.bak-20260830-sprav-cleanup`,
`.bak-20260830-sprav-cleanup2` (была накладка - при слиянии двух правок в одном
Python-патче исключение во второй половине откатило первую до записи на диск,
из-за чего первый деплой трима `/api/fleet/state` тихо не применился; поймано
`grep` живого файла на сервере, исправлено отдельным точечным патчем).

**Фронтенд** (files/index.html, коммит 4127799): сайдбар - "Водители" убран
полностью, "Справочники" заняла её слот наверху (был у "Поступлений", `data-page=
"directory"`), "Поступления" переехали пунктом в секцию "Продажи" (сама страница/
её JS не тронуты - только позиция пункта меню). Новая страница `#page-directory` -
три вкладки одной карточки (тот же tab-bar canon, что "Поступления"): Сотрудники
(физлица + роли-чекбоксы, документы водителя показываются только при роли
"водитель", карточка сцепки для отправки перенесена из "Водителей" без изменения
логики - только источник данных `sprav_people`/`sprav_assets` вместо
`fleet_drivers`/`fleet_trailers`), Юридические лица (простой CRUD: название/полное
название/заметка), Материальные средства (пока только полуприцепы - госномер как
естественный PK, недоступен для правки у существующей записи; привязка к юрлицу
через select).

Проверено: JS-синтаксис всех 5 `<script>` блоков (те же 2 pre-existing ложных
срабатывания регэксп-парсера, что были на HEAD до правок - не новые), баланс
`<div>` по всему файлу (1413/1413) и отдельно по новому блоку (48/48).

**Следующее в очереди (не начато):**
- "Техника" - информационная -> инструментальная переделка (была следующей в
  очереди уже перед разворотом, задача не изменилась, только фундамент под ней)
- Подполосы прицепа/водителя на ленте "Планировки" - fleet_assignments уже готов
  на сервере, фронтенд визуализации не начат
- `plans/2026-08-29-logistics-planning-board.md`, раздел "Фаза 2, полная
  спецификация" - описывает СТАРУЮ схему `fleet_trailers`/`fleet_drivers`,
  помечен как superseded этой записью, самим планом не переписан построчно

## 2026-08-30 - импорт 500 физлиц из выгрузки 1С/Adinest в sprav_people (та же сессия)

Влад: «есть такой массив данных по сотрудникам, выгруженный из Adinest. Там очень
много полезной информации, таких как ИНН, СНИЛС, прописки... можно взять за основу
фундамента нашей базы». Файл (504 строки, только локально у Влада - PII, не в git).

Схема `sprav_people` расширена: `inn`, `snils`, `birth_date`, `hire_date`, `address`
(адрес по прописке), `phone` расширен 32->64 (у части людей два номера через запятую).
Роль `sprav_people_roles.role` получила 4-е значение `'logist'` (открытый список) -
добавлена в бэкенд `SPRAV_ROLES_` и в чекбоксы формы "Сотрудники" (files/index.html).

**Фильтр отбора менялся дважды после проверки данными.** Первая попытка (по прямому
запросу Влада, колонка "дата увольнения ≥2024") оказалась основана на колонке, которая
в выгрузке ПУСТАЯ у всех 504 строк. Вторая попытка (похожая колонка "Дата окончания
работы", правило "≥2024, пустое не берём") тоже оказалась НЕВЕРНОЙ - сверка с реальными
активными водителями (52 ФИО из живых заказов `orders_normalized` на сервере, июль-
август 2026) показала: 48 из 50 совпавших активных водителей имеют ПУСТУЮ дату
окончания - для этой контрактной выгрузки пустое поле означает "работает сейчас", не
"не заполнено". Показал Владу цифры до заливки в базу - поправили правило на "пусто ИЛИ
≥2024" (заполненная дата <2024 - реально устаревшая запись, такая всего у 1 строки на
весь файл). Итог отбора: 500 из 504 (минус 3 с пометкой удаления в 1С, минус 1 с
реально старой датой).

**Автоопознание ролей** (Влад: "кого можешь идентифицировать - идентифицируй, кого не
можешь - не идентифицируй"): сверка по фамилии со списками менеджеров/логистов из
CLAUDE.md + сверка по точному ФИО с живым списком водителей из `orders_normalized`.
Итог: driver=50, manager=10, logist=5, без роли=435 (не гадали, оставили пустыми).

Импорт - разовый Node-скрипт на сервере (транзакция на весь батч, `id`=код 1С как
natural key, `ON DUPLICATE KEY UPDATE` - идемпотентно), удалён вместе с исходным JSON
после запуска (не хранить сырой PII-дамп на сервере). Проверено через
`GET /api/sprav/state` - счётчики совпали с расчётными до заливки. Подробности,
формулы фильтра и бэкапы - `/root/yard-dashboard/README.md` (не дублирую PII-детали
здесь). Форм редактирования новых полей на фронтенде пока нет (осознанно отложено) -
данные хранятся и отдаются API, редактировать можно только через прямой вызов.

## 2026-08-30 - аудит Справочников: 4 агента, автономное исправление (та же сессия)

Влад запросил аудит («аудит первоначальной системы... подключи несколько агентов:
программист-администратор, кадровик, программист, эргономист»), получил приоритизированную
дорожную карту (артефакт, ссылка отдана Владу в чате), затем дал полную автономию: «давай
полностью в работу... сделай все нужные исправления... я ухожу... даю тебе полный доступ и
возможность самому принимать решения». Ниже - что сделано БЕЗ дальнейших вопросов, в порядке
приоритета из дорожной карты.

**Т0 - контроль доступа (закрыт полностью).** Роль пользователя (admin/manager/logist)
раньше проверялась ТОЛЬКО на фронтенде (скрытие пункта меню) - сервер знал только email из
токена, не роль. `issueSessionToken_` (Apps Script) теперь кладёт роль в payload токена;
`verifySessionToken`/`checkSession` на VPS читают её (`req.userRole`); новый `requireRole_(...)`
навешан на все 11 `/api/sprav/*` (только admin) и `/api/fleet/*` (admin+manager+logist, тот же
набор, что имеет доступ к странице «Планировка»). Проверено curl'ом: admin 200, manager 403 на
sprav, СТАРЫЙ (без роли) токен тоже 403 (fail-closed - открытые вкладки без перезахода получат
403 до одного обновления страницы, это ожидаемо, не баг). Apps Script задеплоен (`clasp
redeploy`, версия 306).

**Т1 - целостность данных (закрыт полностью).**
- Мягкое удаление вместо `DELETE` - `deleted_at`/`deleted_by` на все три таблицы sprav_*,
  новые `*_restore` эндпоинты, галочка «Показать удалённых» на каждой вкладке фронтенда.
  Текст `confirm()` при удалении исправлен (раньше врал «нельзя отменить»).
- Optimistic locking - форма шлёт `expected_updated_at`, сервер сверяет перед `UPDATE`, 409
  при расхождении («изменена кем-то другим»), не тихая перезапись параллельных правок.
  Проверено смоук-тестом: create -> edit с верным snapshot (200) -> edit с тем же (уже
  устаревшим) snapshot (409, подтверждено).
- `sprav_audit_log` - кто/когда/что по каждой сущности (create/update/delete/restore/
  bulk_add_role).
- ИНН/СНИЛС/дата рождения/дата приёма/адрес - были в базе с импорта, но не в форме (риск
  тихой потери при любой правке через UI, зафиксирован ещё в первом аудите и закрыт
  COALESCE-патчем) - теперь видны и редактируемы напрямую.

**Т2 - интерфейс (основное закрыто).**
- 4 KPI-плитки над таблицей «Сотрудники» (Всего/Без роли/Документы истекают/Не сцеплены),
  кликабельны (`.p2-drill`), дали странице первую плиточную сетку - курсорный свет
  (`.dir-light`) был физически негде разместить, теперь есть.
- Фильтр-чипы по роли/«Без роли»/«Документы» (`.crm-filter-chip`, канон CRM).
- Сортировка колонки «Документы» - канон ГОСТа (раздел 4, знак ПЕРЕД разностью), особый
  случай направления по умолчанию (как «Последний заказ» у клиентов - не обычное
  "убывание = крупное сверху", а "убывание = самое срочное сверху").
- Sticky-заголовок таблицы (max-height+overflow-y на обёртке, `position:sticky` на `<th>`).
- Массовое назначение роли - чекбоксы у строк + панель действий, новый эндпоинт
  `POST /api/sprav/person_bulk_add_role` (чисто аддитивный `INSERT IGNORE`, не upsert -
  риска затереть остальные поля человека нет по конструкции). Закрывает находку кадровика
  «435 из 500 без роли, назначить можно только по одному».
  - Баг найден при первом же смоук-тесте: `sprav_audit_log.action VARCHAR(20)` не влезал
    `"bulk_add_role:mechanic"` (23 символа) - расширено до VARCHAR(40).

**Т3 - код (частично, только дешёвые/безопасные пункты).**
- Найден и исправлен РАБОЧИЙ stored-XSS (вторая находка программиста-агента): id/госномер
  интерполировались в `onclick="fn('...')"` через `.replace(/'/g, "\\'")`, экранирующий
  ТОЛЬКО одинарную кавычку - значение вида `Е123КХ77" onmouseover="..."` рвало сам атрибут.
  Второй, однотипный баг: `pageEsc_(x).replace(/'/g,...)` не спасал по той же причине (браузер
  раскодирует HTML-сущности атрибута ДО того, как JS увидит содержимое). Новый общий хелпер
  `pageEscAttr_()` - JS-экранирование, потом HTML-экранирование, в правильном порядке.
  Исправлены все 5 найденных мест сразу (правило репо - весь класс, не одна точка).
- `maxlength` на всех 14 текстовых полях форм Справочников, синхронно с VARCHAR-лимитами БД.

**Осознанно НЕ сделано в этой сессии (не забыто, честно вынесено отдельно):**
- Т3: обобщение трёх CRUD-блоков в одну фабрику, Map-индексы вместо `.filter()`, пагинация/
  поиск на сервере, явный дискриминатор схемы id, перенос POST-параметров из query string в
  тело запроса (широкий blast radius - общий хелпер `postToYardApi_`, используется всем
  дашбордом, не только Справочниками - рискованно трогать без Влада на связи).
- Т4: 152-ФЗ (назначение ответственного за ПДн, оценка уровня защищённости, политика
  обработки) - НЕ техническая задача, требует юриста/решения Влада, не может быть "сделана"
  кодом. Учёт патента/разрешения на работу для иностранных граждан - тоже не начато (нужно
  сначала решение по 152-ФЗ, полям и процессу, не просто добавить колонку).
- Т5: статус занятости с полной историей (принят/переведён/уволен), полноэкранная карточка
  человека вместо drawer, печатные формы/экспорт (Excel/PDF для СФР/трудовой инспекции),
  перенос «Карточки сцепки» из Справочников в Планировку (не тронуто сознательно - активно
  разрабатывается ПАРАЛЛЕЛЬНОЙ сессией, logistics-planning-system-3e491e).

Все изменения задеплоены и проверены curl-смоук-тестами/численно (JS-синтаксис, баланс
`<div>`) до пуша в `main`. Коммиты: `665ed10` (Т0+Т1), `2706264` (Т2), `05c99cc` (Т3-частично).
Подробности бэкенда (схема, эндпоинты, бэкапы) - `/root/yard-dashboard/README.md`.

**Инцидент в этой же сессии, сразу после отчёта об аудите: реальная запись Ахтамовой
испорчена и восстановлена.** Влад: «Странно что я не нашёл Ахтамову». Причина - при
раннем smoke-тесте COALESCE-фикса (см. запись про импорт физлиц) я взял РЕАЛЬНУЮ запись
как тестовый объект вместо синтетической, её ФИО прошло через SSH->MySQL->bash-
переменную->curl - на этой Windows-машине кириллица в таком пути бьётся по кодировке
(mojibake), плюс роль перезаписалась на "Водитель" вместо "Менеджер" (побочный эффект
теста). Кто-то (аккаунт `dlinnomertral@gmail.com`) увидел запись с мусорным именем и
удалил её - мягкое удаление (см. Т1 выше) не дало потерять данные физически. Исправлено:
имя восстановлено по исходному xlsx, роль возвращена на "Менеджер", запись
разархивирована - проверено и в базе, и через живой API. Просканировал всю таблицу
sprav_people на такой же паттерн порчи - больше никого не задело, случай единичный.
Урок сохранён в память (`project_cyrillic_bash_arg_corruption_gotcha`) - кириллица в
аргументах Bash-команд на этой машине ВСЕГДА через файл, никогда прод-запись как тест.

## 2026-08-30 - Материальные средства: тягачи + полная карточка характеристик трала (та же сессия)

Влад прислал реальный чертёж трала (Faymonville Multi-NZ-8H-W-AA(U), 8 осей) и попросил:
«у техники есть производитель... тип техники мы выберем - тягач или полуприцеп... для
тягачей - формула колёс (4х2/6х4/8х4), для трала - карточка характеристик: длина осей,
длина площадки, раздвижение, тип корыто/прямой, аппарели... заранее заложим - в будущем
разработаем систему размещения груза для расчёта нагрузки на оси».

`sprav_assets` расширена: `manufacturer` (общее), `wheel_formula` (только тягач),
`length_mm`/`platform_length_mm`/`width_mm`/`axle_count`/`axle_spacing`/`tire_size`/
`trailer_type`('korito'|'straight')/`extendable`/`length_max_mm`/`ramps` (только трал).
`SPRAV_ASSET_TYPES_` = `["trailer", "tractor"]` - тягач получил ID=госномер, тот же
естественный ключ, что у Штатки (natural join на будущее), но статус тягача по-прежнему
живёт ТОЛЬКО в Штатке - sprav_assets хранит характеристики, не занятость.

Фронтенд: кнопка "+ Добавить полуприцеп" -> общая "+ Добавить" с select'ом типа техники,
переключающим блок полей (тягач/трал показывают разные наборы характеристик). Таблица
получила колонку "Категория". Строка "Характеристики" в списке автоматически собирается
из структурированных полей, если "Характеристики одной строкой" не заполнена вручную -
ввод длины/осей/формулы колёс уже сегодня виден в списке.

Проверено ASCII-смоук-тестом (Scania R500 6x4, Faymonville Multi-NZ-8H с полным набором
размеров) - все поля прошли туда-обратно корректно, тестовые записи удалены точно по id.

**Данные из справочников «Планировки» (полуприцепы/тягачи) НЕ импортированы в этой
сессии** - Влад просил сопоставить с существующим справочником оттуда, но ссылки/ID
этой таблицы нет ни в `ai-clone/reference/project-yard-ids.md`, ни где-либо ещё в
доступном контексте - запросил у Влада напрямую. Отдельно: `plans/2026-08-29-
logistics-planning-board.md` уже фиксирует про ЭТУ ЖЕ таблицу «Никакие ДАННЫЕ из этой
Таблицы брать нельзя, только эргономику» (для грид-части с часами/датами) - при
получении ссылки сначала проверить, распространяется ли это предупреждение и на лист
«Справка»/статичные списки техники, или оно было только про часовую сетку.

## 2026-08-30 - Материальные средства: VIN, высота площадки, сектор как шифр (та же сессия)

Влад прислал реальное коммерческое предложение завода Тверьстроймаш (полуприцеп 993941-
FE60, корыто с отстегивающимся гуськом) с полной таблицей характеристик и попросил три
точных правки поверх вчерашней работы по тягачам/тралам:

1. **`platform_height_mm`** (новое поле) - «высота площадки от земли... у трала корыта
   она ниже, чем у прямого... зависит наша работа в калькуляторе - груз на нулевом трале
   и груз на метровом трале будут иметь разную общую высоту». У модели из документа
   реальное значение - 530 мм («Высота погрузки под нагрузкой» в спецификации завода,
   достигается конструкцией отстегивающегося гуська).
2. **VIN** (новое поле, обязательное у ОБОИХ типов техники) - required на фронтенде
   (звёздочка + проверка перед сохранением), не жёсткая серверная блокировка - чтобы не
   мешать карточкам, заведённым наспех по одному госномеру.
3. **`trailer_type` стал свободным текстом**, не закрытым списком корыто/прямой - Влад:
   «секторы указывай не корытыми/прямыми, а как мы обозначили - ТКР4, ПР4, ПР3, К3,
   борт и так далее» (внутренний шифр: тип площадки + раздвижная/нет + число осей одним
   кодом). Datalist с этими 5 примерами как подсказка, не ограничение - тот же канон,
   что уже используется в карточке сцепки для выбора тягача.
4. «Раздвижная площадка» -> «Раздвижка» (применимо не только к площадке, но и к корыту).

Проверено смоук-тестом (Tverstroymash 993941-FE60, VIN X1TFE60ABCDE12345,
platform_height_mm=530, trailer_type='TKR4') - round-trip корректен, тестовая запись
удалена точно по id. Бэкапы: `api/server.js.bak-20260830-vin-height`.

## 2026-08-30 - импорт живой Штатки (тягачи+прицепы) в sprav_assets (та же сессия)

Влад: «загрузив нашу базу данных тягачей и прицепов, которые уже есть в штатке. Наш
дашборд берёт данные прямо из штатных таблиц - тебе не нужно куда-то лезть, у тебя
прямой доступ через клепсы, через скрипт». Справедливое замечание - вместо обходных
путей (пробовал HMAC-токен против Apps Script и Sheets API через OAuth clasp, оба
уперлись в стены) использовал ПРЯМОЙ доступ на запись в сам Apps Script, который у меня
уже есть весь день: добавил временный keyed-эндпоинт в `doGet` (тот же паттерн, что уже
есть в файле - `action=export_debt_status`), задеплоил (`clasp create-version` v307,
`clasp redeploy`), забрал данные, УДАЛИЛ эндпоинт и передеплоил чистую версию (v308) -
правило репо №3, разовый код не оставляем.

Источник - `getStaffData(ss)` (уже существующая функция, парсит лист «Штатка» с
динамическим поиском колонок), 55 тягачей живых на сегодня. У 54 из 55 указан прицеп
(колонка E, `trailerGos`) - совпало с полем «type» из той же строки (`ПР-4`, `ТКР-4`,
`К-3`, `КР-3`, `Борт`, `Рапид`...) - ровно та система шифров, что Влад описал раньше
(«секторы... ТКР4, ПР4, ПР3, К3, борт») - просто с дефисом в написании Штатки, оставил
дословно, не подгонял под свой более ранний datalist-набор.

Импорт: 55 тягачей (`id`=госномер как в Штатке дословно, с пробелами; `manufacturer`=
первое слово марки, `name`=остальное - "SCANIA R620" -> Scania/R620) + 54 уникальных
прицепа (`id`=госномер прицепа, `trailer_type`=код сектора из строки тягача). Статус
машины (В работе/Ремонт/...) НЕ импортирован - живёт только в Штатке, тот же принцип,
что уже зафиксирован в `import/sprav-schema.sql` для тягача в целом. SQL писался
локально в UTF-8-файл и заливался `mysql < file.sql` (НЕ через аргумент команды) - урок
из инцидента с Ахтамовой применён с первого захода, кириллица дошла без единой порчи.

Проверено: `SELECT asset_type, COUNT(*)` -> tractor 55, trailer 54 (совпадает с
источником); выборочная запись через живой `GET /api/sprav/state` - корректная
кириллица и в id (госномер), и в manufacturer/name/trailer_type. `created_by`/
`updated_by`='import:shtatka-30.08' - отличимая метка от ручного ввода.

**Не импортировано осознанно:** сами сцепки (кто на ком сейчас) в `fleet_assignments` -
это территория параллельной сессии (`logistics-planning-system-3e491e`), которая ведёт
Планировку/сцепку - у Штатки же есть готовая пара (gos тягача + gos прицепа) на каждую
строку, это мог бы стать сид-данными для fleet_assignments, но трогать эту таблицу без
координации с той сессией не стал.

## 2026-08-30 - документы техники (СТС) + масса/год выпуска в sprav_assets (та же сессия, VPS)

Влад прислал реальную СТС тягача (О894ХМ797, Скания): «у нас есть хранение передачи
вот таких данных... VIN, технически допустимая масса, год выпуска... добавить сами эти
файлы СДСКи в базу нашу». Три части сделаны: два новых поля характеристик (общие для
тягача И прицепа) + хранилище файлов документов на VPS.

**Схема:** `sprav_assets` + `gross_weight_kg INT`, `manufacture_year SMALLINT`. Новая
таблица `sprav_asset_documents` (метаданные - сам файл на диске сервера
`/root/yard-dashboard/uploads/sprav_assets/`, не блобом в MySQL - см. `import/
sprav-schema.sql` на сервере). `stored_name` - случайное имя на диске
(`crypto.randomBytes` + расширение), `original_name` - для показа/скачивания. Новая
зависимость `multer` (`npm install`, 0 уязвимостей).

**4 эндпоинта** (`checkSession + requireRole_("admin")`): `POST
asset_document_upload` (multipart, до 15 МБ, PDF/JPG/PNG/WEBP), `GET asset_documents`
(список по `asset_id`), `GET asset_document_file` (скачивание, `Content-Disposition`
с оригинальным именем), `POST asset_document_delete` (строка в БД + файл с диска в
одной транзакции).

Смоук-тест полного цикла на синтетическом активе (`SMK-DOC-TEST-1`, не на реальной
записи) - загрузка -> список -> скачивание -> побайтовая сверка (`diff`, идентично) ->
удаление -> файл реально пропал с диска. Плюс 3 негативных: без токена -> 401, `.exe`
под чужим MIME -> отклонён, несуществующий `asset_id` -> 404. Все прошли. Синтетика
удалена жёстко после теста. Бэкап: `api/server.js.bak-20260830-documents`.

Фронтенд (`files/index.html`): поля "Технически допустимая масса, кг"/"Год выпуска"
сразу после VIN (общие для обоих типов техники). Секция "Документы" в карточке -
список файлов + форма загрузки, доступна только для уже сохранённой записи (нужен
`asset_id`, которого нет до первого сохранения).

**Не сделано осознанно, ждёт Влада:** массовая загрузка СТС для остальных ~107 единиц
техники (Влад предложил доступ к Google Диску с папкой СДСКи) - следующий шаг после
подтверждения, что однодокументная загрузка работает на живом сайте как задумано.

## 2026-08-30 - пакетный импорт СТС с Google Диска (та же сессия) - ЗАВЕРШЁН

Влад одобрил образцы макета (после того как первая версия оказалась перевёрнута -
см. ниже), дал полный автономный доступ: "поехали, делай сам от и до, без
диалоговых окон, заверши к моему приходу". План - `plans/2026-08-30-sts-document-
import.md`.

**Доступ:** папка Google Диска (103 PDF, ~68 МБ) расшарена Владом на
`dlinnomertral@gmail.com`. Читал через ВРЕМЕННЫЕ keyed-действия в `doGet`
(`probe_sts_folder`, `get_sts_file`) - тот же паттерн, что `export_vehicles_for_sprav`
раньше. Убраны и передеплоена чистая версия (v311) сразу после завершения импорта.

**Найден и исправлен реальный баг ДО массовой обработки** (на пробном образце, до
получения "поехали" от Влада): `doc.extract_image()` (PyMuPDF) отдаёт сырые байты
встроенного изображения БЕЗ трансформации/поворота страницы - для части файлов
переворачивало картинку на 180°. Влад заметил на присланном примере ("вверх
тормашками"), сам НЕ сообщал об этом заранее - поймано ИМ, не мной. Исправлено:
всегда через `page.get_pixmap()` (корректно применяет поворот), адаптивный zoom
для попадания в целевое разрешение. Оба образца пересобраны и подтверждены Владом
визуально перед стартом массовой обработки.

**Сопоставление 103 файлов с 109 записями `sprav_assets`:** автоматическое по
регулярным выражениям номера (67/103 с первого прохода) + ручное по марке техники
и частичным цифрам в имени файла (ещё 29) + финальная визуальная сверка госномера
на самом документе при извлечении данных (0 расхождений среди всех 96). Итог:

| | |
|---|---|
| Успешно сопоставлено и загружено | **96** |
| Дубли (тот же файл под 2 разными именами, тот же размер в байтах) | 3 (844 ЕА 797 x2, ТВ 3842 77 x2, ЕС 9431 50 x2) - загружен один раз |
| Файлы без пары в базе (не грузились - правило Влада "не грузить лишней информации") | 4 - "6599 МАЗ борт", "0460 трал", "196 вольво ЭВАКУАТОР", "115 ЕТ 797, КАМАЗ" |
| Записи техники без документа в папке | 13 - А 538 СО 797 (XCMG Hanvan), В 968 СЕ 47 (Volvo FM Truck) + 11 прицепов (ЕМ 0628/0677/0680/5270/5271 50, ЕС 9402/9432 50, УА 5395 48, УХ 1025/6753/6755 77) |

**Извлечение данных** (визуально, не OCR) - распараллелено на 8 subagent'ов
(general-purpose), по 12 документов каждый, работали одновременно. Каждый агент
читал изображение (перед+зад бок о бок), сверял госномер на бланке с
предполагаемым `asset_id` (0 расхождений из 96), заполнял `null` вместо
угадывания при нечитаемых полях. Финальная запись в базу - централизованно из
основной сессии (`upload_all.py`), не из subagent'ов.

Итог по полям (из 96): VIN/год/цвет - 96/96; техдопустимая масса - 95/96 (1 обрезан
краем скана); масса в снаряжённом состоянии - 94/96 (2 нечитаемых); дата выдачи
СТС - 83/96 (13 обрезаны/закрыты печатью ГИБДД на самом бланке - не придумано, `null`);
лизингодатель - 15/96 (там, где на бланке в "Особых отметках" прямым текстом указан
лизингодатель - переносили в `lessor`, даже без отдельного одноимённого поля на
бланке; НЕ путали с "Особые отметки" общего вида).

**Запись в базу:** `POST /api/sprav/asset` делает ПОЛНЫЙ UPDATE - для каждой записи
брались текущие значения ВСЕХ полей из свежего `GET /api/sprav/state` (чтобы не
затереть `manufacturer`/`name`/`trailer_type` и т.п., занесённые Штаткой 30.08 раньше
в этой же сессии), поверх - новые VIN/массы/год/цвет. `updated_by`/`created_by`/
`uploaded_by` = `import:sts-drive-30.08`.

**Найдено и исправлено СРАЗУ ПОСЛЕ первой заливки** (не пришлось откатывать) - файлы
загружались через `curl -F file=@sts_merged/<file_id>.pdf`, из-за чего `original_name`
в базе сохранился как случайный ID вместо читаемого имени. Исправлено батч-UPDATE
(`<госномер>_СТС.pdf`) сразу по всем 96 - без повторной заливки файлов.

**Проверено:** `GET /api/sprav/state` - 96/109 с VIN/цветом/годом, ожидаемое число.
`sprav_asset_documents` - 96 строк, 96 уникальных `asset_id`, суммарно 19,4 МБ (было
бы кратно больше без обрезки+пересборки). Скачан и визуально сверен файл через
РЕАЛЬНЫЙ продовый эндпоинт (не напрямую с диска) - О894ХМ797: ориентация верная,
все поля совпадают с исходным документом. Второй спот-чек - лизингодатель у
В277ЕА797 корректен.

**Не сделано осознанно:** данные для 13 записей без документа и 4 "лишних" файлов
без пары - решение за Владом (грузить/не грузить недостающее, разбираться ли с этими
4 файлами отдельно). Бэкап БД - не делался отдельно (изменения аддитивные/точечные
по `id`, а не массовые DELETE - ежедневный `import/backup-db.js` покрывает).

---

| — (только фронтенд) | 2026-08-30 | **Убран блок "Расчёт зарплаты" с личной страницы own-tral логистов (Кан/Сильчев/Махура).** Влад: "у всех логистов кроме Васина, Пруса и Сурковой убери со страницы информацию по их зарплате. Кан Сильчев Махура убирай у них данные по зарплате" - уточнено через AskUserQuestion: убрать ТОЛЬКО с их собственной личной страницы (видят только они сами при входе + admin-предпросмотр), НЕ трогать общую таблицу "Отдел Рыщанова" на company-wide странице "Зарплата" и личной странице Рыщанова - там Рыщанов/admin по-прежнему видят построчный расчёт по каждому логисту как раньше. Использован уже существующий флаг `isOwnTralLogistSur_` (те же Сильчев/Кан/Махура, что и "своя техника тралы, упор от своего парка", введён 2026-08-13) - блок зарплаты в `renderMyPageLogist` теперь рендерится только при `!isOwnTral`. Полоска плана ("ВП своего парка тралов") не тронута - это метрика производительности, не зарплата. При пуше обнаружена крупная параллельная работа в main (CRM v2, ГОСТ дизайн-система DESIGN_SYSTEM.md, доска "Планировка", разворот на "Справочники", живой банковский СМС-фид, редизайн личных страниц менеджеров "Кабина смены") - `files/index.html` разошёлся на 11000+ строк с момента прошлого коммита этой ветки. Слияние (`git merge origin/main`) дало ОДИН конфликт ровно в затронутом месте (эмодзи "💰" убрали в новой дизайн-системе); разрешено - взята логика правки + актуальный текст без эмодзи. После мержа `git diff origin/main -- files/index.html` показал ТОЛЬКО эту правку, ничего больше - слияние чистое. Синтаксис проверен `node --check` по трём реальным `<script>`-блокам файла (границы уточнены построчно - упрощённый regex-чекер ловил ложные срабатывания на слове "&lt;script&gt;" внутри текста двух комментариев в самом файле, это не баг кода). |

## 2026-08-30 - вкладка "Сцепка" в Справочниках + посев fleet_assignments из Штатки (та же сессия)

Влад одобрил вариант "список-таблица" из трёх превью-макетов, дал добро: "давай сразу
воплощай в жизнь. Пусть это будет ещё одна вкладка в справочниках". Плюс: "должно быть
сцеплено как у нас в штатке" - раз функция запускается в прод, начальное состояние
должно совпадать с реальностью, не быть пустым.

**UI** (`files/index.html`, `#page-directory`) - 4-я вкладка "Сцепка" рядом с
"Сотрудники"/"Юридические лица"/"Материальные средства". Таблица: тягач | прицеп
(кликабельный чип) | водитель (кликабельный чип) | история. Клик по чипу открывает
всплывающий пикер (`fleet-picker`) со списком свободных/занятых ресурсов; выбор
вызывает `POST /api/fleet/couple` (уже существующий эндпоинт, ничего нового на
сервере не понадобилось). Тост после сцепки явно показывает, если сервер сам
расцепил ресурс с прежнего места (`replaced_resource`/`closed_from_tractor` из
ответа `/api/fleet/couple`) - Влад: "если цепляем занятого - от той машины он должен
оцепиться" - эта механика уже была на сервере (атомарная транзакция), просто не было
видно в интерфейсе. История - разворачивается под строкой тягача, собрана из ВСЕХ
записей `fleet_assignments` по этому тягачу (не только открытых), включая переклеенные
события. Второй интерфейс над той же таблицей, что и карточка сцепки на вкладке
"Сотрудники" (`drvCardCoupleDriver_` и т.п.) - не отдельная система, `renderDirectory_
thenRepaintCard_` теперь перерисовывает и её тоже.

**Посев начального состояния** - `fleet_assignments` была пуста (функцией ещё не
пользовались вживую). Прочитал живую Штатку через временный keyed-эндпоинт в Apps
Script (`export_fleet_seed`, та же дисциплина, что раньше у `export_vehicles_for_sprav`/
`export_fleet_seed` - убран сразу после использования, версия 313 чистая) - 55 тягачей,
54 с прицепом, driver/driver2/driver3 по каждому.

Сопоставление: госномер тягача/прицепа - точное (100%, 55/55 и 54/54, та же база, что
импортирована 30.08 из той же Штатки). Водители - по ФИО: точное совпадение, при
неудаче - фамилия+имя (без отчества, т.к. Штатка местами хранит отчество с опечаткой
или вообще без него) при единственном кандидате. Из ~90 упоминаний водителей ~40 -
плейсхолдеры "не требуется"/"вакант" (отфильтрованы, это не водители). Из оставшихся:

- 8 сопоставлено НЕ дословно (опечатка в отчестве/имени в самой Штатке, например
  "Мавлият" вместо "Мавлитян", "голы" вместо "оглы", "Гусенович" вместо "Гусейнович") -
  сверено глазами по каждому, кандидат единственный - принято.
- 4 НЕ сопоставлено, оставлено пустым, нужна ручная проверка Влада: "Ибрагимов Асалбек"
  и "Егоров Сергей Степанович" - таких людей нет в `sprav_people` вообще (500 человек
  из 1С/Adinest, возможно новые водители, ещё не попавшие в реестр); "Абдурашидов
  Раджоб Гусейнович" - три однофамильца в базе (Гусейн Асланханович/Ибрагим Гусенович/
  Раджаб Гусенович), ни один не подтверждён явно ролью "водитель" под этим именем -
  не стал гадать между тёзками на боевых данных.

Итог: 104 вызова `/api/fleet/couple` (54 прицепа + 50 попыток водителей), 0 ошибок
API. Сервер САМ закрыл 4 записи как конфликтующие (атомарная транзакция) - в исходной
Штатке один и тот же водитель (Пантелеев Денис Юрьевич - дважды, Шленсков Максим
Сергеевич, Титаев Иван Сергеевич) оказался указан на ДВУХ тягачах одновременно -
реальная проблема данных в самой Штатке, не баг посева; система отработала штатно
(оставила последнюю по порядку строки запись открытой). Итог: 100 открытых записей
(54 прицепа + 46 водителей).

Проверено: `GET /api/fleet/state` после посева - О894ХМ797 показывает верно (Войткун
Вадим Валерьевич slot1, Межевич Сергей Эдвардович slot2, прицеп ХУ 5875 77) - сверено
с тем, что реально было в Штатке на момент посева.

**Ещё не сделано (переговорено с параллельной сессией, `logistics-planning-system-
3e491e`, координация в этой же сессии):** сама доска Планировки НЕ читает
`fleet_assignments` вообще (использует кэш Штатки, `trailerGos` захардкожен пустой
строкой) - подполосы на ленте с drag-жестом переприцепки тоже не начаты. Значит "должно
отражаться в планировке" сейчас работает только НАПОЛОВИНУ - моя запись корректна и
видна в новой вкладке, но не на самой ленте, пока это не подключит соседняя сессия
(ждёт приоритета от Влада). Влад предупреждён явно, чтобы не принял это за баг.

## 2026-08-30 - ПКМ-меню сцепки на доске Планировки: колонка source (та же сессия)

В тот же вечер Влад попросил ПКМ-меню на самой доске Планировки (высадить/посадить/
отцепить/зацепить, с фиксацией "сделано ИЗ ПЛАНИРОВКИ тогда-то"). Само меню на доске -
территория параллельной сессии (`logistics-planning-system-3e491e`, тот же паттерн
"своё контекстное меню", что уже в `DESIGN_SYSTEM.md` для Планировки) - взяла в работу
она, никакой новой логики зацепа/отцепа на сервере писать не пришлось, она вызывает те
же `/api/fleet/couple`/`uncouple`, что уже сегодня заработали в моей вкладке "Сцепка".

Моя часть - источник действия для истории. Добавлена колонка `fleet_assignments.
source VARCHAR(20)` (аддитивно, согласовано с параллельной сессией заранее - без этого
"откуда" была бы сцепка, не разберёшь). Оба эндпоинта принимают `source` query-параметром
и пишут его при КАЖДОЙ записи в строку (и при открытии новой, и при закрытии старой) -
ведёт себя как `updated_by`: для закрытой записи это источник закрытия, для открытой -
источник открытия (открывающее действие уже закрытой строки эту метку теряет -
известное упрощение v1, кому/когда всё равно видно через `created_by`/`valid_from`).
Заодно нашёл и исправил соседний баг: `GET /api/fleet/state` вообще не отдавал
`created_by` в ответе, хотя фронтенд его уже читал (`e.created_by || e.updated_by`) -
молча всегда падало на `updated_by`. Добавлено в SELECT вместе с `source`.

Моя сторона (вкладка "Сцепка" в Справочниках) шлёт `source=sprav` во всех 5 местах, где
вызывает `/fleet/couple`/`uncouple` (карточка сцепки на "Сотрудники" + новая вкладка).
Параллельная сессия подтвердила, будет слать `source=planirovka` из своего меню.
Смоук-тест на синтетической записи (создание+расцепка, проверка что `source`
перезаписывается на закрытии) - прошёл. Бэкап: `api/server.js.bak-20260830-fleet-source`.

## 2026-08-30 (вечер) - КРИТИЧЕСКИЙ фикс: Планировка была пуста для роли логист/менеджер

Срочно, релей от Влада через параллельную сессию: логист Васин открыл Планировку -
все KPI по нулям, ни одной строки техники под "ПАРК ТЕХНИКИ". Проверено и отброшено:
не связано с фильтрами (Влад сам проверил "Рапиды"/"Длинномеры" - не помогло), не
связано с localStorage/hidden (не тот класс симптома - все 0, не часть машин).

Настоящая причина - `doGet()` в `scripts/full_script_final.js`: для `access.role ===
'logist'`/`'manager'` ответ строится через `getLogistView_`/`getManagerView_`
("урезанная выдача", 2026-08-10, задумана для личной страницы - только СВОИ заказы/
показатели). Этот путь НИКОГДА не включал `vehicles` - поле, из которого Планировка
(появилась 29-30.08, намного позже) строит весь состав парка на фронтенде
(`buildVehicles()` в `files/index.html`). `D.vehicles === undefined` -> 0 строк, 0 KPI.
Роль admin эту ветку не проходит (свой путь с `getMainPayloadCacheOrLive_`), поэтому
баг был невидим при обычном тестировании из-под админского аккаунта - клиентский
`isVasinBeta_` в `initLP()` даёт Васину только КЛИЕНТСКИЙ `STATE.role='admin'` (для UI),
но это НЕ меняет то, что СЕРВЕР уже отдал его сессии до всякого клиентского кода.

Дополнительно (обнаружено при разборе, не отдельный баг-репорт): по той же причине
пикер "Менеджер"/"Логист" в шторке создания рейса тоже был бы пуст/почти пуст для
логиста/менеджера - `orders.by_logist`/`by_manager` в урезанной выдаче отфильтрованы
до ОДНОЙ своей строки (приватность выручки коллег), не полного ростера.

Фикс (`scripts/full_script_final.js`):
- `buildLogistParkFromStaff_(ss)` - лёгкий состав парка прямо из Штатки
  (gos/marka/type/status/trailer/driver), БЕЗ единой финансовой цифры. Добавлено как
  `vehicles` в оба урезанных вида (`buildLogistView_`, `buildManagerView_`).
- `buildNameOnlyRoster_(list)` - только `.name` из `by_manager`/`by_logist`, без
  выручки/заказов. Добавлено как `orders.roster_managers`/`roster_logists` в оба вида.
- `files/index.html`, `buildRoster()` (модуль Планировки) - теперь предпочитает
  `D.orders.roster_managers`/`roster_logists`, если они есть, иначе как раньше
  `by_manager`/`by_logist` (admin не получает новые поля - его путь не тронут).

Деплой: `clasp push` -> `create-version` (314) -> `redeploy` на живой
`AKfycbxWi4wJWsSeHdgwrsC-d__EOrncLnkhDr6PgXohoPafY2Y2VtBPq_mGi9qd7mH-vzA00w`. `node
--check` пройден на обоих файлах. Живой authenticated-смоук-тест через curl НЕ сделан -
Apps Script `/exec` требует настоящий Google-логин, форменного session_token для этого
канала (в отличие от VPS Express API) сделать нельзя - подтверждение просил у Влада/
Васина реальным заходом на страницу.

## 2026-08-31 - автозаполнение юрлица по ИНН через DaData (та же сессия)

Влад увидел форму "Добавить юрлицо" (3 пустых поля) и попросил то же, что видел в
других системах и в Битриксе: "вводишь ИНН - заполняется вся карточка". Выбрал DaData
(бесплатный тариф, стандарт для таких задач в РФ) - Влад сам зарегистрировался
(создание аккаунта - действие, которое я делать не могу технически), прислал API-ключ
в чат (плюс секретный ключ, который НЕ нужен для этой задачи - не сохранён).

**Схема** - `sprav_legal_entities` + 7 колонок: `inn`, `kpp`, `ogrn`, `legal_address`,
`director_name`, `director_post`, `egrul_status`. Аддитивно.

**Бэкенд** - ключ в `.env` (`DADATA_API_KEY`, только сервер, никогда фронтенд - тот же
принцип, что у остальных секретов после истории с публичным `YARD_API_KEY`). Новый
`GET /api/sprav/legal_lookup?inn=...` - проксирует `suggestions.dadata.ru/suggestions/
api/4_1/rs/findById/party` (тот же паттерн `fetch()`, что уже используется для Avito).
Только ЧИТАЕТ у DaData, ничего не пишет в БД сам - сохранение отдельным вызовом
`/sprav/legal_entity` (те же 7 полей добавлены туда как обычные upsert-поля -
автозаполненное и введённое руками сервер не различает).

**Фронтенд** - поле ИНН в карточке юрлица, debounce 500ms, при 10/12 цифрах (только
цифры - нецифровые символы фильтруются на лету) дёргает лукап, автозаполняет
Название/Полное название/Адрес/КПП/ОГРН/Директор (ФИО+должность) - поля остаются
обычными РЕДАКТИРУЕМЫМИ инпутами, не readonly. Статус-строка под полем ИНН:
"Ищу в ЕГРЮЛ…" -> "Найдено: <название>" (зелёным) или предупреждение красным, если
`egrul_status` - LIQUIDATED/BANKRUPT (компания ликвидирована/банкрот - это стоит
заметить сразу, не только сохранить молча).

**Проверено:** прямой curl-тест эндпоинта на публичном ИНН Сбербанка (7707083893, тот
же пример, что в документации DaData, которую смотрел Влад) - все поля вернулись
верно. Полный цикл сохранения на синтетической записи (`SMK-LEGAL-TEST-1`, не боевые
юрлица) - все 7 новых полей прошли туда-обратно через `/sprav/legal_entity` и
`GET /api/sprav/state`, запись удалена после теста. UI-логика (заполнение формы при
открытии на редактирование, фильтрация нецифровых символов в поле ИНН) проверена
локально через инъекцию тестовых данных в консоли браузера. Бэкап:
`api/server.js.bak-20260831-dadata`.

## 2026-08-31 - юрлица: банковские реквизиты + идентификатор "своя компания" (та же сессия)

Влад (сразу после проверки автозаполнения по ИНН на реальных данных ООО БУЛЬДОГ):
"Сделай такую же ширину, как у нас карточки техники. Добавь возможность добавить
банковские реквизиты. А также должен быть идентификатор по нажатию, что это наша
компания."

**БД** (`sprav_legal_entities`) - 5 новых колонок: `bank_name`, `bank_account`,
`bank_bik`, `bank_corr_account` (банковские реквизиты, обычный ручной ввод, без
проверки контрольной суммы) и `is_own TINYINT(1) NOT NULL DEFAULT 0` (отличает
СВОИ юрлица группы от контрагентов, которых теперь тоже можно заводить через
ИНН-лукап). Единственная реальная запись на момент миграции (ООО БУЛЬДОГ)
бэкфилнута `is_own=1`.

**Сервер** (`api/server.js`) - `GET /api/sprav/state` и `POST /api/sprav/legal_entity`
(upsert) расширены на все 5 полей. Бэкап `server.js.bak-20260831-bank-isown`.
Smoke-тест синтетической записью `SMK-BANK-TEST-1` (все поля прошли туда-обратно) -
запись удалена после проверки.

**Фронтенд** (`files/index.html`) - дровер юрлица переведён на `.mp-drawer-wide`
(760px) + `.asset-form-grid`/`.asset-form-section`, тот же паттерн, что уже канонизирован
для карточки техники (DESIGN_SYSTEM.md v1.7) - скопирован буквально, не изобретался
заново. Новая секция "Банковские реквизиты" (Банк/БИК/Расчётный счёт/Корр. счёт) и
чекбокс "Это наша компания" в секции "Идентификация". `assetLegalEntityOptions_`
(выбор владельца при заведении техники) теперь фильтрует dropdown по `is_own=1` -
на баланс контрагента технику завести нельзя, но уже выбранное на записи юрлицо
остаётся видимым, даже если is_own сняли задним числом (не теряем значение молча).
Таблица юрлиц показывает зелёный бейдж "СВОЯ" рядом с названием для is_own=1.

**Схема-документ:** `import/sprav-schema.sql` на VPS обновлён (бэкап
`sprav-schema.sql.bak-20260831-bank-isown`).

**Проверено:** JS-синтаксис (тот же скрипт-разбивщик по `<script>`-блокам, что и
раньше - 2 ложных срабатывания до и после правки, совпадают 1:1, новых ошибок нет),
баланс `<div>`/`</div>` в блоке дровера (24/24). Полный UI-цикл (открыть форму на
редактирование существующей записи, проверить, что новые поля подтягиваются) - не
тестировался живьём в браузере в рамках этой сессии, стоит перепроверить Владу при
следующем открытии карточки юрлица.

## 2026-08-31 - Справочники: тот же баг класса "хардкод slot:1", что коллега нашёл в Планировке

Коллега (сессия Планировки) поймал живой баг у Влада ("Пантелеев занят" - враньё, там
Мавлитян) и раскопал корень: и её код, и мой (`fleetCouple_`/`fleetUncouple_`/
`drvCardCoupleDriver_`/`drvCardCoupleTrailer_`/`fleetOpenOnTractor_` - вкладка "Сцепка"
и карточка сцепки на "Сотрудники") ВСЕГДА искали/слали `slot:1` жёстко. Если на тягаче
исторически (тесты/бета 30.08) остался открытым водитель/прицеп на слоте 2/3, а вся
дальнейшая работа всегда целится в слот 1 - старая запись НИКОГДА не находится и не
закрывается новой посадкой, ресурс "призраком" висит занятым навсегда. Нашла 6 таких
тягачей (список - README VPS, запись 31.08), сама почистила только 1 с явным
подтверждением Влада, остальные 5 не трогала - не её данные, и предупредила меня
отдельно, что та же дыра есть и в Справочниках.

**Фикс (files/index.html):** `fleetOpenOnTractor_(kind, tractorGos, slot)` - `slot`
теперь необязателен: без него ищем ЛЮБОЙ открытый слот этого kind на тягаче, при
нескольких - самый свежий по `valid_from` (не slot 1 в лоб). Все 7 read-мест (вкладка
"Сцепка": `fleetTabPaint_`/`fleetRowHtml_`/`fleetOpenPicker_`; карточка сцепки:
`drvCardPaint_`/`drvCardCopy_`) вызывают без явного слота. Все write-места
(`fleetCouple_`, `fleetUncouple_`, `drvCardCoupleDriver_`, `drvCardCoupleTrailer_`)
сначала находят РЕАЛЬНЫЙ текущий слот через тот же `fleetOpenOnTractor_` (или дефолт 1,
если тягач сейчас пуст для этого kind) и шлют его на сервер - раньше слепо слали `1`.
Кнопки "Расцепить"/"Отцепить" в карточке сцепки тоже берут slot из НАЙДЕННОЙ записи
(`openDrv.slot`/`openTr.slot`), а не хардкод.

**Проверено** в браузере (локальный статик-сервер + инъекция синтетических
`fleetAssignments_` в консоль, реальные API недоступны из localhost по CORS): (1) призрак
на slot 2 старее / реальный на slot 1 новее - находит РЕАЛЬНОГО; (2) обратный случай -
именно тот, что ломался - призрак на slot 1 старее / реальный на slot 2 новее - старый
код нашёл бы призрака навсегда, новый находит реального; (3) `fleetUncouple_` на этом
же сценарии шлёт на сервер `slot:2` (реальный), не `slot:1`. JS-синтаксис (2 базовых
ложных срабатывания скрипта-разбивщика, без изменений) и баланс `<div>` (1527/1527) по
всему файлу - без новых ошибок.

**Данные:** те же 6 тягачей с дублями, что нашла коллега (README VPS) - я их не трогал,
исправление кода делает их безопасными на чтение (берёт свежайшую запись), но сами
дубли в базе остаются до ручной проверки Владом (или явной просьбы почистить), это её
находка и её данные.

## 2026-08-31 - Материальные средства: расставлены владельцы по данным СТС

Влад: "отлично я завел наши компании теперь нужно расставить в нашей технике у кого на
балансе, эти данные уже есть в названиях стс" - поле `owner` (юр./физ. лицо-владелец),
извлечённое при пакетном импорте СТС 30.08, ещё не использовалось ни для чего.

**Сопоставление:** нормализованный текст `owner` (без "ООО"/кавычек/регистра) сверен с
6 юрлицами, которые Влад только что завёл. 38 однозначных совпадений (Бульдог/Технопарк/
Ярд Империал/Стройоснова) - остальные 70 записаны на владельцев вне этого списка. Прежде
чем что-либо применять, показал Владу полную раскладку и спросил по каждой группе:
- **Стройтранс (20 машин) и Кранмастер (6 машин)** - оказались ТОЖЕ его компании, Влад
  сам добавил обе в Справочники по ходу разговора ("кранмастер наша добавил только что
  стройтранс тоже") -> итог 64 совпадения на 8 юрлиц.
- **31 машина оформлена на частных лиц** (Гонтюрев Александр Александрович/Николаевич,
  Гонтюрев Алексей Александрович, Панасюк Роман Викторович, Истомин Роман Сергеевич) -
  Влад: "их добавить в физлица (сотрудники) и привязать к машинам".
- **13 машин** - СТС не распознан/не загружен, владелец неизвестен - НЕ трогал.

**Владелец-физлицо - новая возможность, не было в схеме.** У `sprav_assets` не было
поля для "принадлежит человеку, не организации" - добавлена колонка `owner_person_id
VARCHAR(64)` (ссылается на `sprav_people.id`, БЕЗ FK-constraint, тот же стиль, что
`legal_entity_id`). Взаимоисключающее с `legal_entity_id` НА УРОВНЕ UI (выбор одного
поля в форме чистит другое через `onchange`) - сервер не блокирует оба сразу, доверяет
форме. 5 физлиц заведены в `sprav_people` (без роли - это не сотрудники, просто
владельцы по ПТС/СТС; `notes`="Владелец техники по СТС, не сотрудник компании", чтобы
не запутать будущего администратора при взгляде на список сотрудников).

**Бэкенд** (`api/server.js`) - `GET /api/sprav/state` (assets SELECT) и
`POST /api/sprav/asset` (INSERT + UPDATE) расширены на `owner_person_id`. Smoke-тест
синтетической записью (`SMK-OWNER-TEST-1`) - поле прошло туда-обратно, запись удалена.
Бэкап `server.js.bak-20260831-owner-person`.

**Фронтенд** (`files/index.html`) - в форме техники, секция "Административное", рядом с
"На балансе (юрлицо)" - новый select "Владелец (физлицо, если не юрлицо)"
(`assetOwnerPersonOptions_` - весь `sprav_people`, БЕЗ фильтра по роли, отсортирован по
имени). Таблица материалки: колонка "Юрлицо" переименована в "Владелец", показывает имя
физлица с пометкой "(физлицо)", если юрлицо не указано. Проверено локальным браузерным
тестом: взаимоисключение полей в обе стороны, `assetSave_` шлёт `owner_person_id`,
таблица корректно показывает физлицо.

**Данные применены:** 64 UPDATE `legal_entity_id` + 5 INSERT `sprav_people` + 31 UPDATE
`owner_person_id`, все с `updated_by='import:sts-owner-match-31.08'` для трассируемости.
Итоговая раскладка по СВОИМ компаниям (тягачи/прицепы): Бульдог 20/12, Стройтранс 9/11,
Кранмастер 3/3, Технопарк 4/0, Ярд Империал 0/2, Стройоснова 0/1. По физлицам: Гонтюрев
А.А. 9/6, Гонтюрев А.Н. 2/5, Гонтюрев Ал.А. 0/1, Панасюк Р.В. 6/1, Истомин Р.С. 0/1.
Без владельца (нет СТС) - 2 тягача + 11 прицепов, оставлены как есть. Схема-документ
`import/sprav-schema.sql` обновлён (бэкап `sprav-schema.sql.bak-20260831-owner-person`).

## 2026-08-31 - Планировка (десктоп): новый масштаб "2 дня"

Влад: "Пока занимается другой чат мобильной версией давай тут поработаем над десктопом
версией. Я хочу попробовать создать кнопку на масштаб 2 дня." (координация с параллельной
сессией logistics-planning-system-3e491e - она занимается мобильной адаптацией той же
вкладки, договорились не пересекаться по коду).

Система масштабов оказалась полностью data-driven (кнопка `data-zoom="N"` + карта
`ZOOM{day,3d,week,month}` пикселей-на-час + один общий click-handler + `loadSettings()`
без хардкода списка) - правка получилась маленькая, три места:
- кнопка `<button data-zoom="2d">2 дня</button>` между "Сутки" и "3 дня"
- `ZOOM["2d"] = 25` - не с потолка: существующие day:46/3d:17/week:8/month:2.4 легли на
  кривую `hw ≈ 46 * days^-0.9` (проверено - предсказывает week≈7.9 и month≈2.1 против
  реальных 8 и 2.4), по этой же кривой для 2 дней - 24.6, округлено до 25
- CSS `.z-2d .track::before` - минорная сетка раз в 2 часа, по тому же паттерну, что уже
  есть у `.z-day` (раз в 1 час) и `.z-3d` (раз в 3 часа) - число совпадает с "N" в
  названии масштаба, уже сложившаяся закономерность в коде

**Дополнено после merge** с параллельной сессией (та же дата, "3 дня - шкала часов по
просьбе логистов", шаг подписи = шаг CSS-сетки): по тому же принципу добавлены часовые
подписи (0,2,4...22, шаг 2ч) и для "2 дня" в `renderScale()` - без этого правки коллеги
2d осталась бы единственным многодневным масштабом без подписей, хотя минорная CSS-сетка
у неё уже есть с самого начала.

**Проверено** локальным браузерным тестом: кнопка появляется и кликается, `z-2d` класс
проставляется на `#page-logistics-plan`, `--hw:25px` резолвится, aria-pressed переключается
корректно, масштаб сохраняется в localStorage (`loadSettings`/`saveSettings` подхватывают
"2d" без правок - работали по `ZOOM[s.zoom]`, не по хардкод-списку), CSS-правило минорной
сетки резолвится в 2 слоя `repeating-linear-gradient` (major день + minor 2 часа) -
проверено на синтетическом `.track`-элементе, т.к. реальных данных из localhost не видно
(CORS). JS-синтаксис (2 базовых ложных срабатывания, без изменений) и баланс `<div>`
(1528/1528) - без новых ошибок.

## 31.08.2026 - VPS: /api/sprav/state открыт логисту/менеджеру (урезанный срез, без ПДн)

Влад живьём в офисе с логистами ("проверяем систему боем", скриншот Васина):
меню сцепки в Планировке показывало "Высадить · ЗК-0002904" вместо ФИО
водителя - "у логистов вместо фио водителя какая-то хрень".

Корень: /api/sprav/state (api/server.js, VPS, НЕ под git) был admin-only
(`requireRole_("admin")`) ещё с аудита Справочников 30.08. Планировка
(files/index.html, fetchFleetData_) зовёт его для ЛЮБОЙ роли, чтобы
резолвить resource_key -> ФИО/название (lpFleetPersonName_/
lpFleetAssetLabel_) - /api/fleet/state логисту доступен, а имён к его
resource_key взять было неоткуда. Тихий catch() в fetchFleetData_ глотал
403, FLEET_PEOPLE_ оставался [], резолвер падал на fallback "показать
сырой id как есть".

Простое расширение requireRole_ до logist/manager было бы НОВОЙ дырой -
sprav_people целиком несёт ПДн (телефон/ИНН/СНИЛС/дата рождения/адрес/
номер прав), sprav_legal_entities - банковские реквизиты. Вместо этого
эндпоинт теперь ветвится по req.userRole: admin - как раньше, ПОЛНЫЙ набор
полей; logist/manager - только {id, full_name, roles} по людям и
{id, asset_type, name} по технике, legal_entities - пустой массив (не
нужен Планировке, банковские данные не для этой роли). Проверено смок-
тестом (форжинг session_token с ролями admin/logist через SESSION_SECRET) -
admin: 505 человек с phone-полем, 8 юрлиц с bank_account; logist: те же
505 человек БЕЗ phone-поля, 0 юрлиц.

Бэкап перед правкой: api/server.js.bak-before-sprav-role-fix-<таймштамп>.
Перезапуск - `systemctl restart yard-api` (не pm2, см. /etc/systemd/system/
yard-api.service).

## 31.08.2026 - VPS: /api/sprav/state - телефон водителя для роли логист/менеджер

Продолжение записи выше (roster/sprav-state урезание). Логисты попросили
через Влада: "тягач модель госномер, прицеп госномер с типом, водитель с
телефоном" (кнопка "Копировать данные" в шапке строки Планировки,
files/index.html, ICON_COPY). Точечно добавлено поле `phone` в урезанный
не-admin набор `/api/sprav/state` (было только id+full_name) - остальные
ПДн (ИНН/СНИЛС/дата рождения/адрес/номер прав) по-прежнему НЕ отдаются
не-admin ролям, нет установленной надобности. Проверено смок-тестом
(форжинг session_token role=logist) - phone присутствует, inn/address нет.
Бэкап: api/server.js.bak-before-sprav-phone-<таймштамп>.

## 2026-08-31 - Планировка (десктоп): копирование через ПКМ + ВП% открыт логисту/менеджеру

Влад (по скриншоту с реальной строкой машины): "мне не нравится как расположено окошко
копирования, во-вторых оно пересекает валовую прибыль процент. Сделай копирование данных
через ПКМ." Уточнил перед правкой (правый клик по шапке строки УЖЕ занят - открывает меню
сцепки посадить/высадить/отцепить/зацепить, единственный вход на эту функцию на десктопе):
разделить по строкам - верхняя строка (.rh1, госномер/тягач) под копирование, нижняя (.rh2,
прицеп/водитель) - как раньше, меню сцепки. Отдельно, тем же сообщением: "открой логистам
видение валовой % как у меня".

**Копирование (files/index.html):** убрана отдельная кнопка-иконка `.copy-btn` (CSS + разметка
в `head.innerHTML`) - перекрывала бейдж ВП% в `.rh2` при узкой строке. `contextmenu`-обработчик
на `#page-logistics-plan` теперь проверяет `.rh1` ПЕРВЫМ (правый клик -> `lpCopyRow_(vi)`,
тот же текст, что раньше собирала `.copy-btn`), и только если клик мимо `.rh1` - падает на
общий `.rowhead` (как раньше, открывает `openFleetMenu_`). Логика копирования не менялась,
просто перенесена из click-хендлера в отдельную функцию `lpCopyRow_`. Добавлен `title` на
`.rh1` для подсказки. Проверено в браузере: `.closest('.rh1')` матчится и для самого `.rh1`,
и для его детей (не матчится для `.rh2`); `.rh2` матчится только на общий `.closest('.rowhead')`
- маршрутизация разведена корректно (полный флоу с реальными данными не тестировался -
localhost не видит прод-API по CORS).

**ВП%/план для логиста и менеджера (scripts/full_script_final.js, Apps Script):**
`buildLogistParkFromStaff_` раньше отдавал ТОЛЬКО состав парка из Штатки (type/marka/gos/
trailer/status/driver), БЕЗ profit/plan - сознательное решение 30.08 при инциденте "у Васина
пусто" (см. комментарий в `buildLogistView_`). Влад сегодня решил иначе. Функция теперь
дополнительно зовёт `aggregateFinHistoryForRange` (тот же источник и диапазон - месяц по
сегодня, - что admin D.vehicles/страница "Техника") и мержит `profit`/`plan` по госномеру.
Фронтенд (`lpVpPct_` в files/index.html) уже читал `D.vehicles[i].plan/profit` без разбора
роли - правка ТОЛЬКО в бэкенде, фронт не трогали. Побочный эффект: `buildManagerView_`
использует ТУ ЖЕ функцию для состава парка - роль "менеджер" получает ВП% автоматически
вместе с логистом, не отдельной правкой (совпадает с тем, что и так уже жили на одном коде).

Задеплоено через clasp (`clasp pull` до и после - живой редактор совпадал с git HEAD до
правки и с намеченным изменением после, без чужих несинхронизированных правок). Версия 316,
redeploy на живой `/exec` (`AKfycbxWi4wJWsSeHdgwrsC-...`). Живой ролевой смок-тест (реальный
логин логиста/менеджера) НЕ проводился - Apps Script проверяет доступ через
`Session.getActiveUser()`, подделать сессию Google-логина, в отличие от VPS-токенов, нельзя.
Влад/логист должны подтвердить на следующем входе.
