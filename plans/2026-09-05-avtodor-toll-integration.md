# Интеграция с личным кабинетом Автодора (платные дороги)

Влад, 05.09: хочет тянуть данные о проездах по платным дорогам из личного
кабинета Автодора (`lk.avtodor-tr.ru`) - официального API нет, "по тому же
принципу как подключились к навигации" (см. `import/omnicomm-poller.js` на
VPS - логин/пароль в `.env`, тот же паттерн).

## Источник данных

Аккаунт `r.ryshchanov@yard-imperial.ru`, вход - обычный Keycloak OIDC
(`auth-lkmp.avtodor-tr.ru`), без капчи/2FA. Учётные данные - `.env` на VPS,
`AVTODOR_LOGIN`/`AVTODOR_PASSWORD`.

Экспорт транзакций (URL получен от Влада напрямую из DevTools 05.09):
```
GET https://lk.avtodor-tr.ru/api/client/bs/summary/contract-transactions
    ?format=csv&contractId=3150227&dateFrom=YYYY-MM-DD&dateTo=YYYY-MM-DD
```
`contractId=3150227` - фиксирован (единственный договор компании).

**Формат CSV** (пример - `contract-transactions.csv`, 01.09-05.09.2026):
```
Дата;ПВП\РВП выезда;Полоса;Электронное средство;Класс;Сумма тарифа, ₽;Скидка, %;Оплачено, ₽
```
- Одна строка = один проезд ЧЕРЕЗ ОДИН ПУНКТ трассы, не рейс целиком (один
  реальный рейс = несколько строк подряд, по числу пунктов на маршруте).
- Машина идентифицируется ТОЛЬКО номером транспондера ("Электронное
  средство"), гос.номера в файле нет.
- Числа - русский формат (`1 260,00`), даты `ДД.ММ.ГГГГ ЧЧ:ММ:СС`.
- Уникального ID транзакции нет - для перезаливки использовать
  `atomicReload()` (полная замена окна, как `import-receipts.js`), не
  инкрементальный upsert по составному ключу.

## Привязка транспондер -> машина

Соответствия на стороне Автодора нет - завели у себя. Готово (05.09,
коммит `fc08179` в репо + VPS git `f1adee2`):
- `sprav_assets.transponder_id VARCHAR(30) NULL` - новая колонка.
- Поле "Транспондер Автодора" в Справочники -> Материальные средства ->
  карточка тягача (`asset-transponder-field` в `files/index.html`).
- **Ждём от Влада**: заполнить это поле для реальных тягачей (хотя бы
  несколько - для первой сверки джойна).

## Что уже сделано (05.09, ГОТОВО)

1. Логин-флоу разобран, скриптован и ПРОВЕРЕН ЖИВЬЁМ - ручной разбор
   редиректов Keycloak, куки, POST на form action с session_code/execution
   в query. `import/avtodor-trips.js` на VPS, git `ee42cd2`.
2. `sprav_assets.transponder_id` - готово (репо `fc08179`, VPS `f1adee2`).
3. MySQL-таблица `avtodor_transactions` (растущий журнал, НЕ снимок):
   `id` (auto), `txn_at DATETIME`, `checkpoint VARCHAR(150)`, `lane
   TINYINT`, `transponder_id VARCHAR(30)`, `vehicle_class TINYINT`,
   `tariff_rub DECIMAL(10,2) NULL`, `discount_pct TINYINT NULL`,
   `paid_rub DECIMAL(10,2)`, `imported_at DATETIME`. Дедуп - `UNIQUE KEY
   (transponder_id, txn_at, checkpoint, lane)` + `ON DUPLICATE KEY UPDATE`
   (НЕ `atomicReload()` - это журнал, полная замена стёрла бы историю до
   окна текущего запроса).
4. Cron - каждые 6 часов, окно по умолчанию 10 дней (запас на задержку
   проводки у Автодора).
5. Бэкфилл 01.08-05.09.2026 проверен живьём: 1346 строк, 50 транспондеров,
   1 907 661 руб. Повторный запуск того же окна не задвоил.

## Что осталось

1. **Разбивка по гос.номерам** - CSV даёт только транспондер, не машину.
   Страница "Поездки" в ЛК показывает машину рядом с транспондером (Влад
   прислал скриншот 05.09), но это НЕ в этом CSV, НЕ в экспорте раздела
   "Транспондеры" (там только статус устройства, без привязки). Реальный
   JSON-эндпоинт, которым рендерится страница "Поездки", не нашёлся
   перебором нескольких вероятных путей (все 404) - нужен либо
   DevTools-дамп настоящего запроса от Влада (Network tab на странице
   Поездки, ответ с текстом марки машины), либо просто ручное заполнение
   `sprav_assets.transponder_id` в Справочниках (Влад/логисты знают, какой
   транспондер физически стоит в какой машине).
2. Куда выводить на дашборде - НЕ решено, Влад пока не уточнял (отдельная
   карточка "Платные дороги" vs. вливание в существующий блок расходов
   Отчёта "Парк"). Отдельный шаг, после того как разбивка по машинам решена.

## Безопасность

Пароль Влада НИКОГДА не вводится ассистентом лично (ни в форму сайта, ни
где-либо ещё) - перенос из локального `.env` в серверный сделан отдельной
командой, которую Влад выполнил сам (SSH-пайп, значение не показывалось в
чате). См. `../ai-clone/feedback/` при необходимости зафиксировать это как
общее правило.
