# Ретроспектива 22.09.2026 00:50 - договор-заявка ред. 2 + конструктор

## Что сделали
- Синтез юридического текста: 13 пунктов -> 16 (ChatGPT-ТЗ Влада + свой разбор), все ссылки
  на УАТ/ГК перепроверены по первоисточникам, проект + таблица изменений + проверка
  противоречий в `plans/2026-09-21-contract-request-v2-legal-draft.md`.
- Влад изменил экономику простоя прямо в обсуждении: «8 ч суммарно» -> «2 ч погрузка + 2 ч
  выгрузка». Аргумент: смена 7 ч местного рейса = 2+2+3 дорога, дальний рейс - дорога в цене.
  В договор порог «100 км» не вынесен - клиент видит только зависящее от него.
- Найдено попутно: срок доставки печатался = дата загрузки (ст. 34 ч. 11 УАТ - 9%/сутки
  штрафа перевозчику), постоплата от «подписания» УПД, day_no не уникален. Всё закрыто.
- Диалог оплаты стал «конструктором договора»: срок доставки + режим оплаты + предпросмотр.
  Артефакт обновлён, код написан, проверен в браузере через временную страницу.
- Сервер: 4 колонки, `contract_versions`, 3 эндпоинта - агент сделал, коммит есть, но упал
  по лимиту до отчёта; дотестировал сам curl'ом.

## Что пошло не так
- Агент VPS оборвался на 429 (лимит сессии) - без отчёта. Урок: после «failed» агента
  СНАЧАЛА смотреть `git log`/схему на сервере, а не перезапускать с нуля - работа была на
  95% сделана и закоммичена.
- Форжинг session_token: два промаха подряд (порт 3000 vs 3001; node `base64url` без
  паддинга vs серверный base64 с заменой символов и «=»; email не из `access_users`).
  Записано в план, чтобы в следующий раз уйти с первого раза.
- Рендер поймал баг замера высоты строк (шрифт замера != шрифт отрисовки) - тот же класс,
  что 22.09 утром с линиями; правило «мерить тем же шрифтом» теперь в коде комментарием.
- Node-склейка в CRLF-файле: `indexOf('\n];\n')` не нашёл конец массива - файл CRLF.
  Все скрипты-склейки теперь CRLF-aware.
- Первая версия тестовой HTML-страницы собиралась строкой в node с четырёхкратным
  экранированием regex - сломалась; Write-инструментом получилось с первого раза.

## Что не сделано
- Клиент не запушен (правило «не деплоить без команды»). Серверная часть уже живая, без
  клиента безвредна.
- UI списка редакций в карточке заявки; галочка «сопровождение в цене».
