# План: Аналитика в «Задании» - хранение данных по статусам/менеджерам/технике

**Дата:** 2026-09-20
**Автор:** Клод (Зодчий)
**Статус:** черновик

## Зачем

Влад просит вкладку «Аналитика» в «Задании» (уже показана превью - переключатель
`#op2-switch`, третья кнопка) считать не только «стоимость подтверждённых по дням», но и
стоимость **не подтверждённых** и **отбитых** заявок, плюс явно спросил организовать
**хранение**: когда что было отбито, сколько подтверждено, сколько закрыто (done), сколько
у какого менеджера тралов, сколько у какого менеджера длинномеров - и вести это «ещё»,
то есть растущей историей, а не разовым снимком.

**Как выглядит успех MVP:** вкладка «Аналитика» на живых данных (не иллюстративных, как в
превью) показывает верные счётчики/суммы по статусам и по менеджерам ЗА ТЕКУЩИЙ момент -
без единой новой таблицы в БД. Отдельно - написан честный ответ Владу и спроектирован (не
реализован) путь для графиков-трендов за прошлые месяцы, если/когда он их попросит.

## Главный вывод разведки (важно проговорить Владу прямо)

Влад сам угадал точно: **«я думаю, оно само будет организовано хранение этих данных» - для
почти всего списка это ПРАВДА уже сегодня**, отдельная новая таблица не нужна:

| Что просил Влад | Уже есть в БД | Как достать |
|---|---|---|
| Сколько подтверждено сейчас | `plan_orders.status='confirmed'` | `SELECT COUNT(*) ... WHERE status='confirmed'` |
| Сколько закрыто (done) сейчас | `plan_orders.status='done'` | то же, `status='done'` |
| У какого менеджера сколько тралов/длинномеров | `plan_orders.equipment_type` + `manager_email/manager_name` | `GROUP BY manager_name, equipment_type` |
| Когда что было отбито | `plan_orders_history` (`order_id, action, snapshot, detail, changed_by, changed_at`) - уже пишет КАЖДУЮ смену статуса, включая `"confirmed -> cancelled"` в `detail` | `SELECT * FROM plan_orders_history WHERE action='status' AND detail LIKE '%-> cancelled%'` |
| Стоимость по статусам (подтв./не подтв./отбой) | `plan_orders.price` + `status` | `SUM(price) ... GROUP BY status` |

Единственное, для чего живого запроса **недостаточно** - график/тренд «как это менялось по
месяцам» (не «сколько сейчас», а «сколько было в июле»). Причина - задокументированная в
проекте дыра (`CLAUDE.md`, «Дыра в архивации `_archive`»): старые заявки уезжают в
`orders_archive`/`orders_normalized`, и это НЕ обновляется само при закрытии месяца. Живой
запрос к «сегодняшним» таблицам для прошлых месяцев может не совпасть с тем, что видел бы
Влад в момент, когда тот месяц был текущим. **Вот здесь и только здесь нужен снимок** - тот
же принцип, что уже работает в проекте для Штатки/Истории_финансов (`park_reports`,
`snapshot_type`), не изобретение нового паттерна.

## Что НЕ делаем в v1

- Никакой новой таблицы в БД - все актуальные вопросы Влада закрываются живыми запросами
  к `plan_orders`/`plan_orders_history`.
- Никакого графика/тренда за прошлые месяцы - только спроектирован путь (Фаза 2), сам
  снимок не пишется, cron не заводится.
- Экспорт/выгрузка аналитики (Excel, PDF) - не просили.
- Разбивка «по логистам» (только по менеджерам была явно запрошена; логисты - когда
  попросит отдельно).
- Права/фильтры «кто что видит» сверх уже решённого (вкладка целиком - admin-only, как
  сам `#op2-switch`).

## Фаза 0 - Подготовка (не код)

- [ ] Свериться с реальными значениями `equipment_type` в `plan_dictionary` (уже известны
      из этой же сессии: Трал, Длинномер, Faymonville (60+ т), Панелевоз, Тент, Машина
      прикрытия) - разбивка «по менеджерам» в ТЗ явно называет только Трал/Длинномер,
      уточнить у Влада при показе, нужны ли остальные 4 типа в этой же таблице или это
      сознательно только два самых частых.
- [ ] Проверить на реальной БД (не в теории), что `plan_orders_history.detail` для отбоя
      действительно везде в формате `"<было> -> cancelled"` - согласованность формата
      важна для `LIKE`-фильтра ниже, посмотреть 10-20 живых строк перед тем, как строить
      запрос на этом допущении.
- [ ] Решить период по умолчанию для «по менеджерам» (в превью использован «текущий
      календарный месяц») - подтвердить у Влада или оставить с возможностью переключить
      период позже (v2).

**4 вопроса:** (1) решает - без этого шага можно построить запрос, который в проде не
совпадёт с реальным форматом текста; (2) эффективно - 20 минут ручной проверки дешевле
часа отладки на живых данных задним числом; (3) не усложняет - это ровно проверка, не
код; (4) лишнее - нет, всё по делу.

## Фаза 1 - MVP (~1-1.5 часа, живые запросы, без новой таблицы)

- [ ] Новый серверный роут `GET /api/orders/analytics` (`api/lib/plan-orders.js`, тот же
      файл, что уже отдаёт `/orders/counts`) - параметры `from`/`to` (даты вчера/сегодня/
      завтра считает клиент и присылает диапазон, как уже делает `loadCounts()`).
      Возвращает по каждому дню диапазона: `{date, by_status: {confirmed:{count,sum},
      unconfirmed:{count,sum}, cancelled:{count,sum}, done:{count,sum}}, by_type:
      {<equipment_type>: {y,t,m}}}` (форма для типов - 1:1 с уже утверждённым превью).
- [ ] Отдельный узел ответа `by_manager` - `GROUP BY manager_name, equipment_type` по
      `status='confirmed'` за период (месяц по умолчанию, см. Фаза 0) - `[{manager, тралы,
      длинномеры}]`, сортировка на клиенте (как уже сделано в превью - по убыванию).
- [ ] «Когда что отбито» - отдельный лёгкий узел или отдельный роут (решить при
      реализации, не блокирует остальное) - `SELECT order_id, changed_at, detail FROM
      plan_orders_history WHERE action='status' AND detail LIKE '%-> cancelled%' AND
      changed_at BETWEEN ? AND ?` - список событий отбоя за период, не агрегат.
- [ ] Роут - `...gate` с ролью только `admin` (та же admin-only логика, что уже прячет
      `#op2-switch` для не-admin - дублировать проверку на сервере, не полагаться только
      на скрытую кнопку в интерфейсе).
- [ ] Клиент (`order-plan-v2.js`) - третий экран `#op2-scr-analytics`, рендер по образу
      утверждённого превью (артефакт https://claude.ai/artifact/MpTDTn3hicChW41mASoFKg),
      теперь на живых данных вместо иллюстративных чисел. Правило «тип без заявок за все
      три дня не показывается» (уже утверждено Владом на превью) - переносится 1:1.
- [ ] Кнопка «Аналитика» - третья в существующем `#op2-switch`, видна только `admin` (та
      же строка, что уже прячет сам `#op2-switch` для остальных ролей).

**4 вопроса:** (1) решает - закрывает буквально весь список, который назвал Влад, кроме
тренда по месяцам; (2) эффективно - переиспользует существующие таблицы/паттерн роута
(`/orders/counts`), не изобретает новый способ считать заявки; (3) не усложняет - ни одной
новой таблицы, ни одного cron; (4) лишнее выкинуто - экспорт, права сверх admin-only,
разбивка по логистам, тренд за месяцы - всё в списке «не делаем в v1» выше.

## Фаза 2 - Рост, НЕ для v1 (снимок для тренда за прошлые месяцы)

*Только если Влад явно попросит график/тренд «как менялось со временем», не раньше.*

- [ ] Новая таблица `plan_orders_analytics_daily` (имя рабочее, уточнить при
      реализации) - агрегат ЗА ДЕНЬ, не сырые заявки:
      `snapshot_date, manager_name, equipment_type, status, orders_count, price_sum,
      created_at`. Один снимок = одна строка на комбинацию день×менеджер×тип×статус -
      это уже агрегат, не копия `plan_orders` целиком (тот же принцип экономности, что
      `park_reports` - снимок метрик, не дублирование источника).
- [ ] Периодичность записи - **раз в день** (не раз в час/10 минут, как у `park_reports`
      для парка) - агрегаты день/менеджер/тип/статус не «утекают» внутри суток так, как
      утекает состояние машины; ежедневного среза достаточно для графика по месяцам.
      Писать поздно вечером (после 22:00, когда основной поток заявок дня уже прошёл) -
      по аналогии с уже работающими cron-импортами (`import/`-скрипты на VPS).
- [ ] Кто пишет - новый скрипт в `import/` на VPS (тот же механизм, что остальные
      `*-cron.js` в `import/`), а не серверный роут `api/server.js` - согласованно с тем,
      как в проекте уже разделены «живой API» и «фоновая запись снимков».
- [ ] На клиенте - переключатель периода (месяц/квартал/год) для секции «По менеджерам» и
      линейный график динамики стоимости/счётчиков по статусам - конкретный визуальный
      язык графика проектировать отдельным превью, когда до этого дойдёт (не сейчас).
- [ ] Ретроспективное восстановление снимков за уже прошедшие месяцы (до того, как
      скрипт завёлся) - отдельная разовая задача, не автоматическая, наступит на те же
      грабли, что уже дважды случались с `receipts_archive`/`orders_archive`
      (`project_archive_tables_monthly_resync_gap`) - учесть явно, не забыть молча.

**4 вопроса:** (1) решает будущую проблему (тренд), которой пока НЕТ - Влад её не
запрашивал явно, только предположил «будем вести ещё»; (2) эффективно - агрегат, не
дублирование сырых заявок, минимум лишнего объёма; (3) усложняет ли для v1 - ДА, поэтому
отложено целиком; (4) лишнее выкинуто - почасовые снимки (не нужны для месячного тренда),
автоматическое ретро-восстановление (ручной разовый шаг вместо кода).

## Критерии готовности (v1)

- [ ] Вкладка «Аналитика» открывается только у admin, у остальных ролей кнопки в
      `#op2-switch` не видно (как уже проверено на существующем переключателе).
- [ ] Числа на вкладке совпадают с ручным подсчётом по тем же данным (`SELECT COUNT(*)
      ... WHERE status=...` напрямую в БД - построчная сверка, тот же принцип, что уже
      применяется в проекте при переносе расчётов на сервер).
- [ ] Тип техники без заявок за вчера/сегодня/завтра не показывается (перенос правила,
      утверждённого на превью).
- [ ] Ничего не падает при пустом дне/пустом менеджере (0 заявок - не ошибка).

## Риски

- Формат `detail` в `plan_orders_history` может оказаться не всегда одинаковым (ручные
  правки статуса через другие пути) - фильтр `LIKE '%-> cancelled%'` может пропустить
  редкие случаи. Смягчение - Фаза 0 проверяет реальные данные ДО того, как запрос уйдёт в
  прод.
- «Сколько подтверждено сейчас» по факту отвечает на вопрос «сейчас», а не «на конец того
  дня» - если Влад откроет «Аналитику» вечером и статусы уже поменялись днём, число будет
  отличаться от того, что было бы утром. Это ожидаемо для живого запроса (не тренда) -
  стоит явно сказать Владу при показе, чтобы не удивлялся при сравнении со своими
  утренними заметками.

## Открытые вопросы

- Разбивка «по менеджерам» - только Трал/Длинномер (как в превью) или ВСЕ 6 типов
  техники? (Фаза 0)
- Период по умолчанию для «по менеджерам» - месяц (как в превью) или другой? (Фаза 0)
- Нужен ли Фазе 2 отдельный ход прямо сейчас, или ждём, пока Влад явно попросит тренд?
  (рекомендация Зодчего - ждать, не строить заранее)

---

## Куда бить первым

Начать с Фазы 0 (проверка реального формата `plan_orders_history.detail` на живых
данных) - без неё Фаза 1 рискует построить запрос на неверном допущении. Затем сразу
серверный роут `/api/orders/analytics` (Фаза 1) - самая ценная часть, закрывает буквально
весь список Влада.

**Честная оценка времени:** сам код (роут + рендер на живых данных вместо
иллюстративных) - около часа, это действительно простая задача (переиспользование
существующих таблиц и уже утверждённой вёрстки превью). Но проверка на реальных данных
(Фаза 0 + построчная сверка в критериях готовности) - отдельный, не менее важный
тайм-слот, торопиться с ним не стоит - именно на этом шаге ловятся расхождения вроде
разного формата `detail` или неожиданно пустых статусов.

Фаза 2 (снимок для тренда) - НЕ трогать, пока Влад явно не попросит график «как менялось
по месяцам». Пре-мортем этого решения (что может пойти не так на реальных объёмах) имеет
смысл прогнать через «Око Саурона» отдельно, если/когда Фаза 2 станет актуальной - сейчас
рано.
