# Полный уход от Google Apps Script на сервер (api.yardhub.ru)

**Статус: план составлен 01.09.2026 (Fable 5, 4 параллельных агента-исследователя),
прошёл Ревизора (5 правок) и Око Саурона (пре-мортем, 3 инварианта + 4 правки до старта).
Ожидает одобрения Влада. Реализация не начата.**

## ТРИ ИНВАРИАНТА (Око Саурона - нарушать нельзя ни на одном шаге)

1. **"Этап закрыт = старый путь ВЫКЛЮЧЕН"** (не "новый заработал"). И с сегодняшнего дня
   в Apps Script не добавляется НИЧЕГО нового - только фолбэки и сторож. Иначе - вечный
   гибрид: два мира, каждая правка в двух копиях, навсегда.
2. **"Ни одной строки миграции в server.js до git на VPS"** - П.1 повышен до блокера
   (затирание кода параллельными сессиями случилось дважды 01.09).
3. **"У Влада всегда две двери и одно зеркало":** break-glass admin-токен в .env с
   инструкцией на случай сломанного входа + Apps Script-логин живёт весь переходный год
   как вторая дверь + read-only Google-зеркало 3-4 ключевых таблиц MySQL (cron раз в
   день) - привычный Владу аудит глазами сохраняется.

## КРИТИЧЕСКОЕ ОТКРЫТИЕ РЕВИЗИИ (правит текущую систему, не только план)

**Sheets-фолбэк для Заказов/ДЗ/Поступлений уже МЁРТВ.** Email-рассылки этих трёх отчётов
остановлены 17-19.08 (подтверждено Gmail-диагностикой 01.09) - листы заморожены на
середине августа. "Тихий фолбэк на лист" при падении сервера молча покажет цифры
двухнедельной давности как живые. Реальная страховка этих потоков - уже НЕ Sheets, а
бэкапы VPS и его uptime. Фикс (вне очереди, до любых этапов): freshness-проверка в
фолбэк-ветках Apps Script - лист старше N часов -> флаг stale_fallback -> плашка на
фронте "данные от [дата], сервер недоступен".

## Зачем (решение Влада, 01.09.2026)

"Это ещё раз подтверждает что нам нужно уходить от скрипта". Три подтверждения за один день:
1. Лимит платформы Google - 200 версий проекта - остановил деплои посреди рабочего дня.
2. Трёхчасовой кэш Apps Script (`Кэш_главной_страницы`) сделал невозможным "мгновенно"
   для простой операции (скрыть машину) - пришлось городить живой фильтр поверх кэша.
3. Apps Script HEAD не под git - две параллельные сессии дважды затёрли код друг друга.

Плюс стратегия и так утверждена с 2026-08-18 ([[project_server_migration_strategy]]).

## Фактическое состояние (сводка 4 отчётов разведки, 01.09.2026)

**Уже на сервере (Apps Script - только прокси/фолбэк):** заказы+зарплаты+воронка
(`orders_computed`), ДЗ целиком, поступления, деньги парка (`park_history`/`park_summary`),
водитель по машине, department-исключения, калькулятор+нумерация КП (100%, фолбэка нет),
статусы ДЗ (двойная запись). Страницы целиком на сервере: CRM, Справочники, Планировка,
Навигация, База клиентов, банковские СМС.

**Держит на скрипте:**
- Выдача session_token и роли (вход) - сервер умеет проверять токен, но не выдавать.
- Главный payload (`D`) - питает Панель/Продажи/Зарплаты/Технику/ДЗ + `D.vehicles` для
  Планировки и Навигации.
- Штатка (тип/статус/марка/план машины) - корень: пока она в Sheets, нужен и кэш, и
  email-канал "Отчет парк".
- Динамика (`vehicle_history`), Глобальная статистика (`global_stats`) - 100% скрипт.
- Персональные срезы: `my_page_period`, `vehicles_period`, `own_tral_prev_month`,
  `manager_lost_customers`, `manager_profile`.
- ИИ-задачи (kie.ai + кэш в листе), Telegram-отчёт, Bitrix-маркетинг.
- "План задание" - CRUD во внешней Google Таблице логистов (самый объёмный кусок).
- Планы менеджеров (`Планы_менеджеров`) - Влад вводит руками, накладываются поверх ЛЮБОГО
  расчёта, UI ввода на сервере нет.

**Инфраструктурные ограничения VPS:** 1 ГБ RAM (OOM уже был), диск 82% занят, телематика
+20К точек/день без ротации, код не под git.

## Порядок этапов

Принципы (все - требования Влада, накопленные за миграцию): мягкий переход (старый путь
не удаляется, пока новый не сверен), построчная сверка на каждом шаге, история не
стирается, бэкап перед каждой правкой на VPS.

### Этап 0 - СРОЧНОЕ, до 8 сентября + гигиена (не ждёт остального)

- [x] **0.1 Конверсия эпох plan_segs - ДЕДЛАЙН 7 сентября.** РАЗРЕШЕНО 01.09 (координация
      с сессией Планировки): выбран дизайн "фиксированная эпоха навсегда" - `EPOCH0_ =
      2026-08-01T00:00:00 MSK`, миграции ДАННЫХ не будет, меняется только код (точка
      отсчёта перестаёт пересчитываться на каждой загрузке; видимое окно ленты сдвигается
      отдельно). План той сессии: `plans/2026-09-01-plan-segs-fixed-epoch.md`. Деплой -
      их зона, обещали сообщить перед деплоем. Осталось нам: убедиться до 07.09, что
      задеплоено (пункт остаётся под наблюдением, чекбокс - про решение дизайна).
      **КОНТРАКТ для 0.2 (согласован, не изменится):**
      `реальная_дата = 2026-08-01T00:00:00+03:00 + start_hour * 3600 сек` - формула
      актуальна и до, и после их деплоя.
      Известная аномалия данных (их зона, разберут до деплоя): сегмент `s1-ab5e6f84`
      (В 325 МХ 797, repair, start_hour=-1442, length≈110 дней) - если снимок 0.2 отразит
      её странно в первые дни, это она, не наш баг.
- [x] **0.2 `vehicle_status_daily` - ежедневный снимок статусов машин (прямое требование
      Влада: "с 1 сентября история по статусам должна сохраняться относительно Планировки").
      СДЕЛАНО 01.09**: таблица создана, скрипт `import/snapshot-vehicle-status.js`
      задеплоен, cron `0 9 * * *` (12:00 МСК). Бэкфилл 30.08-01.09 прогнан и проверен
      (санити-проверка прошла на всех трёх днях, суммы = размеру ростера 53 машины).
      Cron на VPS по образцу `snapshot-debt-history.js`: для каждого тягача из
      `sprav_assets` (минус department-исключённые) фиксировать статус дня по правилам
      renderKpis (trip -> В работе; repair/maint -> Ремонт; driveroff-сегмент ИЛИ нет
      открытой driver-записи -> Нет водителя; иначе -> Без заказа). Снимок "вчера" -
      финальный прогон в ~12:00 следующего дня (логисты дозаполняют утром), после -
      заморозка, правки ленты задним числом историю не меняют. Чем раньше запустим, тем
      меньше дыра - данные Планировки полны с 30-31.08, пробный расчёт агента сошёлся
      (56 машин, 31.08: 26 работа/12 ремонт/14 без водителя/4 без заказа).
      Вариант "считать на лету из plan_segs" ОТВЕРГНУТ: ось времени нестабильна (эпоха)
      и содержимое переписывается задним числом (47 удалений за 2 дня) - история, которая
      меняется при каждом запросе, не история.
      **Гварды (Ревизор, 🔴):** (а) дизайн согласован 01.09 - контракт зафиксирован в 0.1
      (эпоха 2026-08-01 навсегда, формула не изменится - строить можно сразу); (б)
      санити-проверка в самом cron: сумма статусов за день == числу тягачей ростера,
      иначе снимок НЕ пишется и алертится; (в) в каждой строке снимка хранить
      использованную эпоху (защита на случай, если контракт когда-нибудь всё же сменят).
- [ ] **0.3 Динамика -> vehicle_status_daily.** Новый `/api/vehicle_status_history`
      (checkSession + requireRole_('admin') - страница admin-only, как и action в
      Apps Script), фронтенд: для дат >= 01.09 - сервер, для дат раньше -
      `vehicle_history` из Apps Script (История_финансов), шов по дате - тот же приём, что
      у водителя по машине. Деньги в карточке машины уже с сервера.
      **Шов определений (Ревизор, 🟡):** до 31.08 статусы - из Штатки (ручная колонка),
      после - из правил Планировки ("вакант" = Нет водителя и т.д.) - числа на границе
      скакнут ОЖИДАЕМО. Сноска в UI Динамики: "с 01.09 - по данным Планировки".
- [ ] **0.5 (вне очереди) Freshness-гвард мёртвого фолбэка** - см. "Критическое открытие"
      выше: в Sheets-фолбэках getOrdersData/getDebtData/getReceiptsData проверять возраст
      данных листа, старое отдавать с флагом stale_fallback, фронтенд показывает плашку.
- [ ] **0.4 Гигиена:** убрать забытый `export_debt_status` (ключ в открытом коде с 19.08);
      укрепить fleet_assignments (DELETE -> закрытие valid_to; добавить action='create' в
      plan_segs_history) - согласовать с сессией Планировки.

### Этап 1 - Вход и роли на сервере (снимает главный блокер отключения doGet)

- [x] **1.1 + 1.3 СДЕЛАНО 01.09.** Таблица `access_users` в MySQL (email, name, role).
      Постоянный мост `action=access_list` в Apps Script (ключ - существующий
      YARD_API_KEY, не новый секрет) + cron `import-access-list.js` (*/5 мин, санити-гвард:
      0 строк или падение >50% -> НЕ применять). Влад продолжает править лист "Доступ" как
      раньше - зеркало, не UI (Око Саурона: UI выкинут из v1). Спец-флаги (ryschanov/
      commercial_head/own_tral/column_head/vasin) - ОТЛОЖЕНЫ, остаются хардкод-списками в
      коде до отдельного решения (не блокируют 1.2b).
- [ ] 1.2 `/api/auth/google`: проверка Google id_token (JWKS Google, проверка aud/iss/exp/
      email_verified; aud = существующий GOOGLE_CLIENT_ID), поиск в access_users, выдача
      ТОГО ЖЕ HMAC-токена (SESSION_SECRET уже общий - формат менять не надо). Логи входов -
      таблица `access_log` вместо листа.
- [x] **1.2b Живой отзыв доступа - СДЕЛАНО 01.09** (Ревизор, закрыл и СУЩЕСТВУЮЩУЮ дыру):
      `checkSession` в api/server.js теперь сверяет email с `access_users` (кэш 60 сек в
      памяти процесса, fail-open при сбое самой проверки). Отзыв доступа - до ~5 минут
      (частота cron) + 60 сек кэша, было до 48 часов. Проверено на живых данных (COUNT
      реального email = 1, несуществующего = 0), сервер перезапущен, health OK.
- [ ] 1.3b Break-glass (Око, инвариант 3): долгоживущий admin-токен в .env + однострочная
      инструкция Владу "как войти, если вход сломан" (вставить в localStorage).
- [ ] 1.4 Фронтенд: логин пробует `/api/auth/google` первым, фолбэк на Apps Script
      (мягкий переход). Заодно: session_token из query-string в заголовок
      `Authorization` (сейчас токен попадает в логи/историю браузера) - сервер принимает
      оба способа на переходный период.
- [ ] 1.5 Двойная запись доступа на переходный период: правка в UI пишет и в MySQL, и в
      лист (или: лист остаётся read-only зеркалом до этапа 6).

### Этап 2 - Штатка на сервер (корень зависимости; КООРДИНАЦИЯ с другим чатом)

Миграция Штатки в sprav_assets уже идёт в другом чате. Наша часть после/вместе с ней:
- [ ] 2.1 Согласовать с той сессией модель: где живут СТАТУС машины (В работе/Ремонт - сейчас
      колонка AF Штатки, живой документ) и ПЛАН ВП. Вероятная модель: статус - из
      Планировки/fleet_assignments (уже фактически там), план - поле sprav_assets или
      отдельная таблица планов.
- [ ] 2.2 `/api/fleet_summary` (checkSession): аналог getStaffData+getFleetStatus+
      getRepairsData+staffMarkas с сервера. Сверка с Sheets-версией на живых данных
      (verify-функция, как verifyServerOrdersCalc).
- [ ] 2.3 aggregateFinHistoryForRange/getSummaryData: убрать обязательное чтение Штатки
      (тип уже есть в vehicle_types, план - из 2.1). После этого email-канал "Отчет парк"
      можно отключать в 1С (последняя причина его жизни).
- [ ] 2.4 **Кэш_главной_страницы - УДАЛИТЬ целиком** (существует только из-за медленной
      Штатки). "Мгновенно" станет свойством системы, а не заплаткой.

### Этап 3 - Главный payload с сервера

- [ ] 3.1 `/api/main_payload` (checkSession, role-aware): собирает то же, что doGet без
      action - orders/summary/debt/receipts уже готовы (agregated эндпоинты есть),
      vehicles/drivers - из этапа 2, history - История_показателей -> таблица (или из
      vehicle_status_daily агрегатом; разовый импорт старых 30 дней из листа для графика),
      periods - period_snapshots уже на сервере, access_log - из этапа 1. Для
      manager/logist - урезанные view (порт getManagerView_/getLogistView_ - использовать
      orders_computed; фильтрация по роли СТРОГО на сервере, как сейчас в Apps Script, не
      на фронте). **Кэш (Ревизор, 🟡):** in-memory кэш 60-120 сек в процессе Node (не
      Redis) - иначе на 1ГБ RAM пересчёт на каждый вход = приглашение к OOM; апгрейд RAM
      (П.2) - предпосылка этого этапа, не пожелание.
- [ ] 3.2 Фронтенд: loadData() пробует `/api/main_payload` первым, Apps Script - фолбэк.
      Построчная сверка JSON двух источников на всех ролях ДО переключения по умолчанию.
- [ ] 3.3 Глобальная статистика: `month_summary` уже в MySQL (8 месяцев) -
      `/api/global_stats` + порт saveMonthSummary_ в cron. Сверка.
- [ ] 3.4 Периодные/личные срезы: `/api/vehicles_period` (= park_history + fleet_summary),
      `/api/my_page_period`, `/api/own_tral_prev_month`, `/api/manager_lost_customers`,
      `/api/manager_profile` (клиентская аналитика уже на сервере, доперенести агрегат
      мега-базы: client_orders_raw 112К строк уже в MySQL - пересобрать агрегат там),
      + добавлено Ревизором (выпадали): `/api/vehicle_orders_history` (график в карточке
      машины - orders_normalized+archive уже в MySQL), `/api/long_haul_detail` (сводка
      длинномеров), вывод available_periods из period_snapshots, проверка что
      debt_changes_period покрыт compare_days_back в debt_computed.
- [ ] 3.5 **Бэкфилл старых месяцев парка (Ревизор, 🟡):** январь-май по машинам живут
      только в Sheets (Данные_1С_история/История_финансов) - разовый перенос в
      park_reports (snapshot_type='archive', приём отработан на июне/июле) ДО этапа 6,
      иначе выбор старого месяца на "Технике" умрёт вместе с doGet.

### Этап 4 - Планы менеджеров, ИИ, Telegram, прочее

- [ ] 4.1 UI ввода планов менеджеров (admin, Справочники или Зарплата) -> manager_plans
      MySQL (таблица уже есть, но была "мёртвой копией" - оживить и сделать источником;
      двойная запись в лист на переходный период).
- [ ] 4.2 ИИ-задачи: порт generate*AiTasks на сервер (kie.ai ключ в .env, кэш на день -
      таблица ai_tasks_cache). 4 набора промптов переносятся как есть.
- [ ] 4.3 Telegram (send_telegram_logists + дайджест): порт sendTelegram на сервер, токен
      в .env.
- [ ] 4.4 Bitrix-маркетинг (`marketing`): порт запроса на сервер. ВНИМАНИЕ: bitrix24.ru
      напрямую с VPS может не работать (память project_bitrix24_via_apps_script_vpn -
      причина, почему это в Apps Script) - проверить доступность С СЕРВЕРА до порта; если
      недоступен - оставить в Apps Script до решения, это не блокирует остальное.

### Этап 5 - "План задание" (самый объёмный, отдельный под-план)

CRUD во внешней Google Таблице логистов (31 дневная вкладка, парные строки, ~2.5 мин
чтения месяца). Таблица - рабочий инструмент менеджеров и остаётся им НАВСЕГДА
([[feedback_gradual_transition_sheets_to_dashboard]]) - значит НЕ "перенести данные", а
решить архитектурно: (а) оставить этот кусок в Apps Script как последний остаток, (б)
сервер пишет в Google Таблицу через Sheets API (сервисный аккаунт), (в) Планировка
постепенно вытесняет "План задание" функционально. Решение - отдельным планом с Владом,
НЕ блокирует этапы 0-4 и 6 (doGet можно сузить до order_plan-only).
**Око Саурона: вариант (а) объявлен ДЕФОЛТОМ** - doGet, суженный до order_plan+сторож, это
валидный ФИНИШ миграции, не недоделка. Этап 5 не стартует до 26/26 в Приложении А -
барьер от "засасывания" (самый тяжёлый кусок топил бы этапы 3-4).

### Приложение А - инвентарь ВСЕХ точек doGet (чек-лист закрытия; этап 6 стартует при 23/23)

| # | action / точка | Серверный аналог | Статус на 01.09 |
|---|---|---|---|
| 1 | (без action) главный payload, 3 роли | `/api/main_payload` (этап 3.1) | нет |
| 2 | order_plan (+hot/rest) | этап 5 (решение отдельно) | нет |
| 3 | order_plan_create | этап 5 | нет |
| 4 | order_plan_update | этап 5 | нет |
| 5 | order_plan_set_status | этап 5 | нет |
| 6 | calc_save | `/api/calc/save` | **ГОТОВ** (100%, фолбэка нет) |
| 7 | calc_history | `/api/calc/history` | **ГОТОВ** |
| 8 | kp_issue | `/api/calc/kp_issue` | **ГОТОВ** (сам doGet - лишний прокси, фронт может звать сервер напрямую - дешёвая победа) |
| 9 | vehicle_history | `/api/vehicle_status_history` (0.3) + История_финансов до 01.09 | нет |
| 10 | vehicle_orders_history | нужен `/api/vehicle_orders_history` (orders_normalized+archive уже в MySQL) | нет - ВЫПАЛ из этапов, добавлен в 3.4 |
| 11 | available_periods | вывод из period_snapshots + orders_normalized месяцев | нет - ВЫПАЛ, добавлен в 3.4 |
| 12 | orders_period | `/api/orders_period` | **ГОТОВ** (двухуровневый) |
| 13 | global_stats | `/api/global_stats` (3.3, month_summary) | нет |
| 14 | send_telegram_logists | этап 4.3 | нет |
| 15 | debt_set_status | `/api/debt/set_status` | **ГОТОВ** (двойная запись; осталось перестать писать лист) |
| 16 | debt_set_comment | `/api/debt/set_comment` | **ГОТОВ** (то же) |
| 17 | marketing (Bitrix) | этап 4.4 (проверить VPN-доступность с VPS!) | нет |
| 18 | vehicles_period | `/api/vehicles_period` (3.4) | нет |
| 19 | debt_changes_period | ПРОВЕРИТЬ: `/api/debt_computed?compare_days_back=` покрывает? - выяснить и записать | не проверено - ВЫПАЛ |
| 20 | client_analytics | `/api/client_analytics` | **ГОТОВ** (фронт уже зовёт сервер; doGet-путь - удалить) |
| 21 | manager_profile | `/api/manager_profile` (3.4, агрегат мега-базы в MySQL) | нет |
| 22 | my_page_period | `/api/my_page_period` (3.4) | нет |
| 23 | long_haul_detail | нужен серверный аналог (сводка длинномеров, admin/logist) | нет - ВЫПАЛ, добавлен в 3.4 |
| 24 | manager_lost_customers | `/api/manager_lost_customers` (3.4) | нет |
| 25 | own_tral_prev_month | `/api/own_tral_prev_month` (3.4) | нет |
| 26 | generate_ai_tasks | этап 4.2 | нет |

### Этап 6 - Отключение и чистка

- [ ] 6.0 **Критерий завершения (Око, смерть №10):** Приложение А 26/26 + 2 недели
      нулевых фолбэк-срабатываний -> Влад назначает КАЛЕНДАРНУЮ дату отключения заранее
      (на этапе 3, не в конце). Без назначенной даты этап 6 не существует - "почти готово
      навсегда" и есть провал.
- [ ] 6.1 Месяц (минимум 2 недели) параллельной работы: фронтенд на сервере, Apps Script -
      живой фолбэк. Мониторить фолбэк-срабатывания (лог на фронте/сервере). Для каждого
      переносимого секрета (SESSION_SECRET, kie.ai, Telegram) - тест-запрос ДО
      переключения; для мелких фич - сравнение вывода старого и нового пути 1:1.
- [ ] 6.2 Отключить email-рассылки 1С: "Отчет парк" (последняя живая - подтверждено
      Gmail-диагностикой 01.09). ПЕРЕД этим - закрыть дыру ежемесячного пересбора
      _archive-таблиц ([[project_archive_tables_monthly_resync_gap]]) - cron архивации
      месяца N-2, иначе класс бага "пропавший август" будет повторяться.
- [ ] 6.3 Сузить doGet до остатков (order_plan если вариант (а) этапа 5; сторож) либо
      удалить целиком.
- [ ] 6.4 **Сторож (`watchdogCheckDataFreshness`) НЕ переносить на сервер** - он специально
      снаружи, чтобы заметить падение самого сервера (урок 19.08: 182 падения подряд).
      Оставить в Apps Script (крошечный скрипт без деплой-цикла - лимит версий не мешает)
      или вынести на третью площадку (UptimeRobot/cron на другой машине).
- [ ] 6.5 Удалить ~30 разовых диагностик, мёртвый importManagerReport, verify-функции
      (после последней сверки), YARD_API_KEY из публичного index.html (аудит-план Этап 1),
      поле переопределения api_url в UI.

### Параллельно (инфраструктура, предпосылки - начинать сразу)

- [x] П.1 **Git на VPS - БЛОКЕР этапа 1 (Око Саурона, инвариант 2)** - СДЕЛАНО 01.09,
      локальный репозиторий `/root/yard-dashboard/.git`, протокол в CLAUDE.md. Код сервера под
      версионирование (баре-репозиторий на VPS или пуш в GitHub private). Затирание кода
      параллельными сессиями случилось дважды за 01.09. Протокол деплоя через git вместо
      scp+bak. Ни одна строка миграции не пишется в server.js до этого пункта.
- [ ] П.0 **Read-only зеркало (Око, инвариант 3):** cron выгружает 3-4 ключевые таблицы
      MySQL в Google-таблицу "Зеркало сервера" раз в день - Влад сохраняет привычный
      аудит цифр глазами, система не становится чёрным ящиком.
- [ ] П.5 **Судьба каждого листа (Око, смерть №6):** при отключении чтения листа - явная
      пометка на самом листе ("АРХИВ с [дата], дашборд это не читает") + согласовать с
      Владом персонально, какие листы остаются рабочим инструментом людей (Штатка!).
- [ ] П.6 **Сторож-правила:** "0 новых строк канала за 24ч -> Telegram" (по import_runs,
      каждый FTP-канал отдельно - страховка единственности FTP после отключения email) +
      метрика памяти/свопа VPS.
- [x] **П.2 (частично) Апгрейд тарифа - СДЕЛАНО 01.09**: Beget 2 ядра/4ГБ/40ГБ (990₽/мес,
      был 1 ядро/1ГБ). Снимает RAM-гейт этапа 3.1 заранее. Заодно подключены официальные
      Telegram-уведомления Beget об инцидентах хостинга - частично закрывает П.6 (падение
      самого хостинга видно без своего мониторинга). ОСТАЛОСЬ: ротация
      omnicomm_vehicle_track, чистка ~5МБ JSON-мусора в api/lib/ - не блокируют, делать по
      мере необходимости (диск теперь 40ГБ, запас большой).
- [ ] П.3 Бэкапы: проверить, что ежедневный backup-db.js влезает в диск с ростом БД.
- [ ] **П.4 Репетиция восстановления (Ревизор, 🔴, до этапа 3):** один прогон
      восстановления из бэкапа (в локальный MySQL/Docker), шаги задокументировать в
      README сервера. После этапа 6 VPS - единая точка отказа, а прецедент уже был
      (24.08 - приостановка хостинга за неоплату). Заодно: спросить Влада про
      автоплатёж/напоминание биллинга Beget - минута разговора против дня простоя.

## Оценки трудоёмкости (гадание +-2x, для приоритизации)

| Этап | Оценка |
|---|---|
| 0.2-0.3 снимок+Динамика | 1-2 дня |
| 0.4-0.5 гигиена+freshness | 0.5 дня |
| 1 вход и роли | 2-3 дня |
| 2 Штатка (наша часть, после чужой миграции) | 2-4 дня |
| 3 главный payload + срезы | 4-7 дней |
| 4 планы/ИИ/Telegram | 2-3 дня |
| 5 План задание | отдельное решение, не оцениваем |
| 6 отключение | 1 день + 2-4 недели наблюдения |
| П.1-П.4 инфраструктура | 1-2 дня |

## Правки Ревизора (внесены 01.09.2026)

🔴 1. Признана смерть Sheets-фолбэка Заказов/ДЗ/Поступлений (email мёртв с 17-19.08) -
секция "Критическое открытие" + пункт 0.5 (freshness-гвард).
🔴 2. Живой отзыв доступа - пункт 1.2b (stateless HMAC 48ч = существующая дыра на прямых
эндпоинтах, не только будущая).
🔴 3. Инвентарная таблица всех 26 точек doGet (Приложение А) - 4 action'а выпадали из
текста (vehicle_orders_history, available_periods, debt_changes_period, long_haul_detail).
🔴 4. Гварды снимка статусов (0.2): согласование дизайна с сессией Планировки, санити-
проверка суммы, эпоха в строке.
🔴 5. Репетиция восстановления из бэкапа (П.4) + вопрос автоплатежа Beget.
🟡 6-9. Бэкфилл старых месяцев (3.5), сноска о шве определений на Динамике (0.3),
in-memory кэш main_payload + RAM как предпосылка (3.1), оценки трудоёмкости.

## Порядок и оценка

Этапы 0-1 независимы, можно параллельно. Этап 2 ждёт координации с чатом Штатки.
Этап 3 ждёт 1-2. Этап 4 - после 3 (кроме 4.4 - в любой момент). Этап 5 - отдельная
ветка. Этап 6 - финал. П.1-П.3 - немедленно.

Критический путь: 0.1 (чужая зона, дедлайн 07.09) и 0.2-0.3 (требование Влада, дыра в
истории растёт с каждым днём).

## Правила безопасности миграции (несводимые)

1. Каждый переключаемый источник - "сервер первым, тихий фолбэк" минимум 2 недели.
2. Каждый порт расчёта - verify-функция со сверкой на живых данных ДО переключения.
3. Каждая правка на VPS - бэкап + node --check + запись в README сервера.
4. Ничего не удалять из Sheets/Apps Script до этапа 6 - только перестать читать.
5. Координация: Штатка и Планировка - активные зоны других чатов, любое пересечение -
   через SendMessage до правок.
